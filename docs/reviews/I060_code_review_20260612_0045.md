必要なファイルの確認が完了しました。レビューを作成します。

---

```
# コードレビュー [I060] ── [2026-06-12 00:00]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `CLAUDE.md` の参照先一覧が `docs/runbooks/` 配下の実ファイルと一致している（漏れ・誤参照ゼロ） | ⚠️ 一部不足 | Blocker | ファイルシステム上の `CLAUDE.md` は全14本を網羅しており内容は正しい。**しかし本ブランチの diff に CLAUDE.md の変更は一切含まれていない**（後述） |
| 2 | CLAUDE.md が参照する外部実体（hooks等）が現存し記述と整合している | ⚠️ 一部不足 | Blocker | 同上。ファイル内容は正しいが diff に含まれない |
| 3 | `docs/runbooks/*.md` 各ファイルが現行の実体と整合している | ⚠️ 一部不足 | Blocker | D5〜D15 の修正はファイルシステム上では適用済み（pre-commit.md shellcheck追記・issue-flow.md テンプレ参照修正・backend-check.md curl注記追加 等）。**しかし本ブランチの diff にこれらの変更は含まれない** |
| 4 | runbook 間の重複・矛盾箇所が解消され、片寄せ先が明示されている | ⚠️ 一部不足 | Blocker | ファイル内容は正しい（D8: レビューファイル命名統一・D9: retro 必須統一 等）が diff に含まれない |
| 5 | 乖離一覧（修正前後の差分まとめ）が計画書または PR 本文に記録されている | ✅ 実装済み | - | `plan_I060` §3 に D1〜D15 の乖離一覧が記録済み |
| 6 | 新規 runbook 追加時の CLAUDE.md 同期手順が `template-sync.md` に追記されている | ⚠️ 一部不足 | Blocker | `template-sync.md` のステップ2.5に手順は存在するが diff に含まれない |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| **Blocker** | 要件適合性・変更安全性 | **実装が本ブランチにコミットされていない。** `git diff origin/develop...HEAD` の出力は `docs/issues/open/I060.md`（イシューファイル作成）の1ファイルのみ。計画書 Steps 1〜6 に相当する変更（CLAUDE.md 参照先追記・PostToolUse追記・全14 runbook リライト・D1〜D15 全修正）が本ブランチのいかなるコミットにも含まれていない。 | git diff 全体 | 実装変更を本ブランチにコミット・プッシュし、`/code-review` を再実行すること |
| **Blocker** | 変更安全性・プロセス | **ファイルシステム上では D1〜D15 相当の変更が既に適用済み**（CLAUDE.md: 14本列挙 + PostToolUse行あり／template-sync.md: ステップ2.5あり／pre-commit.md: shellcheck行あり／issue-flow.md: テンプレ参照修正済み 等）。これらが本ブランチの diff に現れないということは、① develop への直接 push（絶対ルール3「develop/main への直 push 禁止」違反）、または ② ローカルでの変更が未コミット、のいずれかである。どちらの経路で適用されたかを確認・解消すること。 | CLAUDE.md / docs/runbooks/* 全体 | 変更経路を確認し、正しくブランチにコミット・PR 経由でのマージに切り替えること |
| Medium | ベストプラクティス | イシューファイル（`I060.md`）の diff 上の内容（85行）と、レビューコンテキストに注入された現在のファイル内容に差異がある（diff 版には「Draft PR: #125」が不在）。イシューファイルの更新もコミット・プッシュが必要。 | docs/issues/open/I060.md | イシューファイル更新分を追加コミット |
| Low | ベストプラクティス | D15 対象の danger-ops.md は全文リライト後も内容が非常に短い（16行）。計画書で「機能追加・拡充はスコープ外」と明示されているため問題なしだが、レビュアーとしてセマンティクス変更なしを確認済み。 | docs/runbooks/danger-ops.md | 対応不要（スコープ外と明示済み） |

## 高リスク判定
判定: No
該当条件: なし（コード変更なし、ドキュメントのみ）

---

Blocker: 「Blocker が残っています。実装変更を本ブランチにコミット・プッシュしたうえで `/code-review [I060]` を再実行してください。」

---

### 補足：ファイルシステム上の変更内容の正確性（参考）

差分から切り離して「内容の正しさ」を先行確認した結果、D1〜D14 に相当する変更はファイルシステム上で正しく適用されていることを確認した。コミット後の再レビューは内容面の確認を主眼に簡略化して構わない。

| 項目 | ファイル上の状態 |
|------|----------------|
| D1: CLAUDE.md 14本網羅 | ✅ |
| D2: PostToolUse 追記 | ✅ |
| D3: template-sync.md ステップ2.5 | ✅ |
| D4: tasks/templates 削除 | ✅ |
| D5: pre-commit.md shellcheck 追記 | ✅（.pre-commit-config.yaml の実体とも一致） |
| D6: issue-flow.md テンプレ参照修正 | ✅ |
| D7: error_log テンプレ参照削除 | ✅（「テンプレートなし」旨を正しく記述） |
| D8: レビューファイル命名統一 | ✅ |
| D9: retro 必須統一 | ✅（workflow.md / issue-flow.md 両方「必須」） |
| D10: docker compose 統一 | ✅（legacy 配下のみ旧形式・スコープ外） |
| D11: 非実在スクリプト削除 | ✅ |
| D12: curl deny 注記追加 | ✅ |
| D13: onboarding posttooluse 追記 | ✅ |
| D14: onboarding grill-me 追記 | ✅ |
| D15: 文体リライト | 確認済み範囲は問題なし |
```
