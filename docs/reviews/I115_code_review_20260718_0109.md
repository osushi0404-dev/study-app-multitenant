## 決定論ゲート実行結果（code-review.sh 自動注入）

(決定論ゲート宣言なし)
- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

必要なファイルが確認できました。レビューを実施します。

```
# コードレビュー [I115] ── 2026-07-18 11:30

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 未認証で `/api/subjects/public/?slug=personal` と `/api/organizations/subjects/public/?slug=personal` の両ルートが 200 を返す | ✅ 実装済み | - | `get_permissions()` に `self.action == 'public'` 完全一致で `AllowAny` 分岐を追加。TC-AUTO-01 parametrize（×2ルート）が 8 passed 記録済み |
| 2 | 未認証で存在しない slug を叩いた場合の既存挙動（空リスト 200）は不変 | ✅ 実装済み | - | TC-AUTO-03 両ルート parametrize で検証・8 passed に含む。`public` 内の返却コード不変更 |
| 3 | admin/非admin の既存認可が退行しない（I104 回帰テストが引き続き PASS） | ✅ 実装済み | - | TC-AUTO-04: `problems/tests` 全体回帰で 76 passed（既存 68 件 + 新規 8 件）。I104 既存テスト 5 件も PASS に含まれる |
| 4 | 両ルートの未認証 200 回帰テストが追加され、修正前は 401 で FAIL・修正後は PASS を確認（false-green でない） | ✅ 実装済み | - | TDD RED: 実装前 8 failed 記録済み。false-green 注入検証 2 種（クエリ全件化・フィールド追加）とも RED 確認・Edit で復元済み |
| 5 | 新規登録画面で未認証で科目リストが表示されることを目視確認 | ✅ 実装済み | - | manual_test No.4 Human OK（登録ステップ2「科目選択」に 11 件表示・エラーなし・2026-07-18） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | テスト妥当性 | `test_public_legacy_slug_resolves_personal`（TC-AUTO-05）が `setup` fixture を受け取っているが、org_a/org_b の返却値を一切使用していない。DB アクセスは `@pytest.mark.django_db` で確保済みであり、`setup` は余分な DB 操作（2組織6件）を走らせるだけ。機能上の問題はなく false-green/false-red は発生しないが、将来テストを読む人が「なぜ setup が必要？」と誤解するリスクがある | `test_I115_public_unauth.py:63` | 改善提案（対応任意）。引数から `setup` を除去するか、「DB 初期化目的ではなく `db` fixture 相当として `setup` を流用」とコメントを添えれば明確になる |
| Low | セキュリティ/情報露出 | `Organization.objects.create()` でフィクスチャを生成する際に `is_active` を明示していない。`Organization.is_active` のデフォルトが `True`（`accounts/models.py:50` 確認済み）なので現状は正常動作するが、将来デフォルト値が変更された場合に `public` の ORM フィルタ `is_active=True` で空リストが返り TC-AUTO-01 の `len == 2` assert が暗黙的に失敗する。記録された 8 passed の実績があるため現状問題なし | `test_I115_public_unauth.py:23-24`（setup fixture） | 改善提案（対応任意）。`Organization.objects.create(..., is_active=True)` と明示するとフィクスチャの意図が自明になる |

（Blocker・High 指摘なし）

## 高リスク判定
判定: Yes
該当条件: 認証・認可変更（`IsAuthenticated` → `AllowAny` への権限クラス変更）・外部公開 API の変更（未認証アクセス可能エンドポイントの復旧）

---

✅ コードレビュー完了。`/test [I115]` を実行してください。

VERDICT: OK
```
