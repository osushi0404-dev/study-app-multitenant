全ファイルを読み込みました。レビューを開始します。

---

```
# 計画書レビュー [I096] ── 2026-07-02 14:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `wt-remove.sh` に「スクリプト CWD が撤去対象 WT 内か」の事前チェックがない。ユーザーが `cd wt-foo && DANGER_OK=1 bash scripts/claude/wt-remove.sh foo` を実行した場合、`docker compose down -v`（サブシェル）は成功するが、その後 `git worktree remove "$WT_PATH"` を CWD が対象 WT 内のまま実行するため git が拒否してエラーになる。コマンド自体は失敗し作業消失には至らないが、エラーが git の生メッセージになりユーザーが混乱する恐れ。`[ "$(pwd -P)" != "$(cd "$WT_PATH" && pwd -P)" ] \|\| { echo "..."; exit 2; }` 等の explicit check を推奨。 | plan §4-2 `wt-remove.sh` 末尾 | 修正推奨 |
| Warning | BP | `--env-source` を値なしで末尾に渡した場合（例: `wt-new.sh app 001 x --env-source`）、`${2:-}` は空文字を返し、`shift 2` で残 args 数が 1 しかないため `shift count out of range` エラーで `set -e` が発火し、ユーザーへのメッセージが出ないまま exit する。`shift 2` 前に `[ -n "${2:-}" ] \|\| { echo "..."; exit 2; }` で非空チェックを推奨。 | plan §4-1 `wt-new.sh` `while` ループ内 `--env-source` 分岐 | 修正推奨 |
| Warning | P4 | TC-R6（primary 保護）は通常の命名規則（primary は `wt-` prefix なし・WT は `$BASE/wt-$TRACK`）では `WT_PATH = PRIMARY` が自然に成立しないため、テストのセットアップ方法が未明記。`auto_test.md` には「primary を指す path」とあるが、テスト環境でそれを実現する手順（例: primary clone を `$TMP/wt-primary` 等に配置し `wt-remove.sh primary` を呼ぶ）が記載されていない。TC は実装できるが、担当者が独自判断でセットアップを決める必要があり再現性が低下する。 | docs/tests/open/I096_auto_test.md TC-R6 | 修正推奨（テスト仕様にセットアップ方法を補記） |
| Warning | BP | `wt-remove.sh` の `docker compose down -v` が非ゼロ終了した場合（docker デーモン停止・compose.yml 欠落等）、`set -euo pipefail` で script が exit し `git worktree remove` が実行されない。ユーザーには worktree が残留した理由が不明瞭。計画・runbook どちらにも「docker が落ちているときの対処案内」がない。`( cd ... && docker compose down -v ) \|\| { echo "[wt-remove] down -v 失敗。手動対応後 git worktree remove $WT_PATH を実行してください。"; exit 2; }` 等のガイダンス付きエラーを推奨。 | plan §4-2 `wt-remove.sh` DANGER_OK ブロック | 修正推奨 |
| Info | モダン | `git show-ref --verify --quiet "refs/heads/$BRANCH"` は `git fetch origin` より前に実行されるため、remote にのみ存在するブランチ（他者が push 済みだが自分が未 fetch）は衝突検出されない。フェッチ後に remote-tracking refs も確認する（`git ls-remote --heads origin "$BRANCH"`）ことでより厳密になるが、シングル開発者 worktree 管理というユースケースでは影響軽微。 | plan §4-1 `wt-new.sh` 衝突事前検査 | 改善提案（対応任意） |
| Info | BP | `set -euo pipefail` 下で `[ -n "$(git -C "$WT_PATH" status --porcelain)" ]` の `$()` 内で git が異常終了した場合、親シェルの `set -e` はトリガーされず空文字 → `[ -n "" ]` false → 未コミットチェックがスキップされる（conservative fail でなく silent pass）。実用上 git repo が突然破損するケースは稀だが、`git -C ... status` の終了コードを別途チェックする実装が理想（`git_status="$(git -C "$WT_PATH" status --porcelain 2>&1)" && ...` 等）。 | plan §4-2 `wt-remove.sh` 撤去前チェック (1) | 改善提案（対応任意） |

## 高リスク判定
判定: No
該当条件: なし（Django/React アプリコード変更なし。開発ハーネスのスクリプト追加のみ。`docker compose down -v` は既存 danger-ops 枠組み（DANGER_OK=1 ゲート）で整合済み）
推奨: `/implement [I096]` を実行してください。

VERDICT: OK
```
