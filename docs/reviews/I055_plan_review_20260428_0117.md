すべてのファイルを読み込みました。レビューを実施します。

---

# 計画書レビュー [I055] ── [2026-04-27 00:00]

## 判定: 差し戻し（Blocker 3件）

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | 要件適合性 | イシューの受け入れ条件では `--audit-level=high` だが、計画書では `--audit-level=critical` に変更されている。イシュー文書との乖離があり、どちらが正式な要件か判定できない。計画書がイシューの受け入れ条件を変更しているのであれば、イシュー文書を更新するかユーザー承認済みの根拠を明記する必要がある。 | I055.md（AC行3）vs plan_I055.md（受け入れ条件3行目・ステップ8） | 実装前に修正必須 |
| Blocker | 要件適合性 | イシューの受け入れ条件に「コーディング規約ドキュメント（`rules/` 配下）に linter が自動チェック vs 手動確認の区分を明示する」とあるが、計画書の受け入れ条件（9項目）では「コーディング規約ドキュメント冒頭に linter が自動強制する範囲と参照先を追記」とだけ記載されており、**手動確認の区分の明示**という要件が欠落している。「linter が何を自動チェックするか」だけでなく「何が手動確認対象か」を記載する要件が消えている。 | I055.md（AC行8）vs plan_I055.md（受け入れ条件9項目） | 実装前に修正必須 |
| Blocker | ベストプラクティス / テスト妥当性 | 自動テスト TC-03 の期待値が「全フックが pass（Ruff または flake8・bandit・ESLint が実行されること）」とあるが、**コミットブロックの検証**（lint エラーがある場合に pre-commit がコミットを止めること）が自動テストに含まれていない。手動テスト No.6 でのみ「Human」担当として記載されているが、イシューの AC 第1項は「エラーがあればコミットがブロックされる」が要件の核心であり、この検証が自動テストとして設計されていない（手動テスト No.6 はコマンドではなく実際の git commit 操作を要求しており、Claude が代替できるかの判断も不明確）。TC-03 に lint エラーファイルを使ったブロック検証ケースを追加するか、手動テスト No.6 を Claude 実施可能な形式（`--dry-run` 等を使ったコマンドベース）に具体化する必要がある。 | I055_auto_test.md TC-03 / I055_manual_test.md No.6 | 実装前に修正必須 |
| Warning | ベストプラクティス | ステップ6 の bandit pre-commit フック設定で `args: ["-c", "backend/setup.cfg"]` を指定しているが、bandit の `-c` オプションは設定ファイルのパスを指定するためのもので、`setup.cfg` を直接サポートするかは bandit バージョンに依存する（bandit 1.7.x は `--configfile` で TOML/YAML を想定）。`setup.cfg` の `[bandit]` セクションを bandit が正しく読み込めるか事前確認が必要。ステップ3の設計にも `-c setup.cfg` が効くかの検証ステップがない。 | plan_I055.md ステップ3・ステップ6 | 修正推奨 |
| Warning | ベストプラクティス | ステップ6 の ESLint pre-commit フック設定で `language: node` を使用しているが、この場合 pre-commit は `node_modules` を分離された環境に展開しようとする。実際には `language: system` + `entry: cd frontend && npx eslint` の形式の方が既存の `frontend/node_modules` を直接使えて WSL2 環境での差異が少ない。`language: node` では `additional_dependencies` に ESLint やプラグインをすべて列挙する必要があるが、計画書にそのリストがない。 | plan_I055.md ステップ6（ESLint フック） | 修正推奨 |
| Warning | モダンなウェブアプリ開発 | pip-audit ステップ4で「既知の問題には `--ignore-vuln VULN-ID` を付与する方針を決定してから CI に追加する」とあるが、現在ベースライン実行結果（既知 CVE の一覧・無視対象リスト）が計画書に記載されていない。CI 追加時に何件の CVE が存在し、どれを除外するかが不明のまま実装に進むと、CI が常に失敗するか、無制限に `--ignore-vuln` が増殖するリスクがある。ベースライン実行結果と除外方針を計画書に追記することを推奨する。 | plan_I055.md ステップ4 | 修正推奨 |
| Info | Claude Code BP | `.claude/skills/implement/SKILL.md` の変更はスコープに含まれるが、計画書には変更内容（削除/書き換えする文言）が具体的に記載されていない。ステップ9に「手動 lint・audit 実行指示を削除し…に置き換え」とあるが、どの文言を削除しどう書き換えるかの案が計画書に示されていない。実装者判断に委ねる形になっており、意図が伝わらない可能性がある。 | plan_I055.md ステップ9①、SKILL.md の変更対象箇所 | 改善提案 |
| Info | モダンなウェブアプリ開発 | ステップ7 の `detect-secrets` JWT 対応で `args: ['--baseline', '.secrets.baseline', '--list-all-plugins']` の追加を検討しているが、`--list-all-plugins` は引数として `.pre-commit-config.yaml` に渡すものではない（コマンドラインツールの情報確認オプションであり、実際のシークレットスキャン実行には使わない）。誤った設定が混入する可能性があるため記述の正確性を確認すること。 | plan_I055.md ステップ7 | 改善提案 |

## 高リスク判定

判定: No
該当条件: なし

---

差し戻しの Blocker 3件を修正後、再度 `/plan-issue-review I055` を実施してください。

**Blocker 修正ポイントの要約:**
1. **[B1] audit-level 乖離**: イシューAC(`--audit-level=high`) vs 計画書(`--audit-level=critical`) の齟齬を解消し、変更理由を計画書に明記するか、イシュー文書を更新してください。
2. **[B2] 手動確認区分の欠落**: コーディング規約ドキュメントへの「手動確認対象の区分明示」がイシューACに含まれているが、計画書の受け入れ条件から消えています。復元または計画書に代替対応を明示してください。
3. **[B3] コミットブロック検証の欠如**: pre-commit によるコミットブロック動作の検証が、自動テストに存在しません。TC-03 に lint エラー付きファイルを使ったブロック確認ステップを追加するか、手動テスト No.6 を Claude 実施可能なコマンドベース（`pre-commit run --files <dummy-file>`等）に具体化してください。
