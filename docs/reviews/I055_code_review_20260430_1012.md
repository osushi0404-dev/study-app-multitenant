# コードレビュー [I055] ── 2026-04-30 10:00

## 受け入れ条件の照合

以下はイシュー AC（I055.md）と計画書 AC（plan_I055.md）の両方を参照している。計画書 AC が承認済みであり、計画書 AC を主たる判定基準とする。

| # | 受け入れ条件（plan_I055.md） | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `git commit` 時に Ruff / bandit / ESLint が自動実行されエラーがあればコミットがブロックされる | ✅ 実装済み | - | `.pre-commit-config.yaml` に ruff / bandit / eslint フック追加済み |
| 2 | CI の lint ジョブは `pre-commit run --all-files` を実行する（個別ツールステップを削除） | ✅ 実装済み | - | `pre-commit/action@v3.0.1` 使用、旧 flake8/bandit 個別ステップ削除済み |
| 3 | CI の `frontend-lint` ジョブで `npm audit --audit-level=critical` が実行される | ✅ 実装済み | - | `ci.yml` に `npm audit --audit-level=critical --omit=dev` 追加済み |
| 4 | CI の `backend-lint` ジョブで `pip-audit` が実行される | ✅ 実装済み | - | `pip-audit -r requirements.txt` ステップ追加済み |
| 5 | bandit の除外設定が `backend/.bandit`（INI 形式）に集約され CI・pre-commit の両方に適用されている | ⚠️ 一部不足 | Medium | `backend/.bandit` は作成済みだが、pre-commit が repo root から bandit を呼び出す場合に `backend/.bandit` が自動検出されるかどうか計画書の検証手順（`pipx run bandit -r backend/` での "Found project level .bandit file" 確認）が実施された証跡がない。テスト結果未記録。機能上は tests が pre-commit フィルタで除外されており B101 が非テストコードに存在しないため実害は現時点では低い |
| 6 | `detect-secrets` の JWT 検出状況を確認し、対応方針を `docs/runbooks/pre-commit.md` に記録している | ✅ 実装済み | - | `.secrets.baseline` に `JwtTokenDetector` 追加済み、`pre-commit.md` に対応方針記録済み |
| 7 | `/implement` SKILL.md 手順2 から手動 lint・audit の実行指示が「pre-commit / CI が自動実行」に書き換えられている | ✅ 実装済み | - | SKILL.md 手順2 の書き換え確認済み |
| 8 | `docs/runbooks/pre-commit.md` のフック一覧が最新の状態に更新されている | ✅ 実装済み | - | 全フック（ruff / bandit / eslint）一覧に追記済み |
| 9 | コーディング規約ドキュメント冒頭に「linter が自動強制する範囲」と「手動レビュー対象」が明示されている | ✅ 実装済み | - | `rules/ultimate_django_coding_standards.md` および `rules/react-coding-standards-integrated.md` の冒頭に追記済み |
| 10 | Backend テスト: 25 passed 以上（ベースライン維持） | ⚠️ 一部不足 | Medium | `docs/tests/open/I055_auto_test.md` の結果欄が「（実施後に記入）」のまま未記録。CI pass の確認が取れていない |
| 11 | Frontend テスト: 7 passed 以上（ベースライン維持） | ⚠️ 一部不足 | Medium | 同上 |
| 12 | CI の全ジョブが pass する | ⚠️ 一部不足 | Medium | テスト結果・CI pass 証跡が未記録 |

## コード品質・セキュリティ指摘

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | P3 データ整合性・変更安全性 | Pillow のバージョンが計画書の「10.3.0」から「12.2.0」へ大幅に逸脱している（メジャーバージョン 2 世代ジャンプ）。CVE-2026-25990 / CVE-2026-40192 への対応として実施されたことはコミットメッセージに記載されているが、計画書（plan_I055.md の upgradeテーブル）が更新されておらず計画との差分が記録されていない。コーディング規約「計画書に書いていない実装は禁止」の観点から計画書への追記が必要 | `backend/requirements.txt`（Pillow==12.2.0）、`docs/plans/open/plan_I055.md`（Pillow 10.3.0 のまま） | 計画書の upgrade テーブルを 12.2.0 に更新し、2026 CVE 対応の旨を記録する |
| Medium | P4 テスト妥当性 | `docs/tests/open/I055_auto_test.md` の結果欄がすべて「（実施後に記入）」のまま空白。`docs/reviews/open/I055_review.md` も「未記入」。テストが実施されたかどうかが検証できない状態でコードレビューが依頼されている | `docs/tests/open/I055_auto_test.md`、`docs/reviews/open/I055_review.md` | テスト（自動・手動）を実施し結果を記録してからコードレビューに再投入する |
| Medium | ベストプラクティス | `backend/.bandit` の自動検出が pre-commit 実行環境（repo root からの呼び出し）で機能するかどうか、計画書ステップ3の検証コマンド（`pipx run bandit -r backend/ ... 2>&1 \| head -8` → "Found project level .bandit file" の確認）が実施された証跡がない。bandit 1.7.x の auto-detect は CWD ベースであるため、pre-commit が repo root から個別ファイルを渡す場合に `backend/.bandit` が認識されない可能性がある。テスト TC-11（`pre-commit run bandit --all-files` で exit code 0）の結果未記録 | `.pre-commit-config.yaml`（bandit フック）、`backend/.bandit` | TC-11 を実施し bandit フックが B101 skip を含め正しく動作することを確認・記録する |
| Medium | ベストプラクティス | `frontend/src/Login.tsx` の変更（`<a href="#">` → `<button type="button">`）は I055 のスコープ（`.pre-commit-config.yaml` / CI / lint 設定等）に明示されていない。--max-warnings 0 を達成するために必要だった場合、計画書の「影響範囲」または「ステップ内容」への記録が必要。現在の実装はアクセシビリティ上正しいが計画書との差分として記録されていない | `frontend/src/Login.tsx`、`docs/plans/open/plan_I055.md` | 計画書の影響範囲または計画との差分として Login.tsx の変更理由を記録する（実装自体は正しい） |
| Low | P5 運用性 | `backend/setup.cfg` が空ファイル（0 バイト）として残存している。`[flake8]` セクションを削除して Ruff に移行した結果だが、空の設定ファイルを残しても混乱の元になる。削除するか、今後の用途がある場合はコメントを記載する | `backend/setup.cfg` | 不要であれば `git rm backend/setup.cfg` する |
| Low | ベストプラクティス | `backend/core/management/commands/watch_errors.py` の `_analyze_error` メソッド内で `from io import StringIO` と `from django.core.management import call_command` がメソッド内ローカルインポートになっている。コーディング規約（PEP8 / Ruff E402）ではファイル先頭のインポートを推奨しており、遅延インポートが必要な技術的理由（循環インポート等）がない場合はファイル先頭に移動するのが適切 | `backend/core/management/commands/watch_errors.py` L116-117 | ファイル先頭に `from io import StringIO` と `from django.core.management import call_command` を移動する |
| Low | セキュリティ | `docs/runbooks/pre-commit.md` の detect-secrets 記述では `JwtTokenDetector` が有効と記載されているが、`.pre-commit-config.yaml` の detect-secrets フックの args に `--force-use-all-plugins` は含まれていない。フックは `--baseline .secrets.baseline` のみで起動しており、ベースライン内の `plugins_used` リストが参照される仕組みになっている。この動作（ベースライン経由でプラグインが有効になる）の説明が `pre-commit.md` に明示されておらず、将来のベースライン再生成時に `JwtTokenDetector` が抜け落ちるリスクがある | `docs/runbooks/pre-commit.md`、`.pre-commit-config.yaml`、`.secrets.baseline` | `pre-commit.md` に「プラグイン設定はベースライン経由で管理されるため、ベースライン再生成時は `--force-use-all-plugins` を必ず使用する」旨を追記する |

## 高リスク判定

判定: No
該当条件: なし

---

**レビューサマリー**

- Blocker 件数: 0 件
- High 件数: 0 件
- Medium 件数: 4 件
- Low 件数: 3 件
