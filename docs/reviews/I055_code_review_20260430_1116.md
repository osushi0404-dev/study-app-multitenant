# コードレビュー [I055] ── [2026-04-30 15:00]

## 受け入れ条件の照合

| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `git commit` 時に Ruff / bandit / ESLint が自動実行され、エラーがあればコミットがブロックされる | ✅ 実装済み | - | `.pre-commit-config.yaml` に Ruff（`--fix`）、bandit、ESLint フックを追加。Ruff は自動修正後に差分がある場合 exit 1 でブロック |
| 2 | CI の `frontend-lint` ジョブで `npm audit --audit-level=critical` が実行される | ✅ 実装済み | - | `ci.yml` の `frontend-lint` ジョブに `npm audit --audit-level=critical --omit=dev` を追加済み |
| 3 | CI の `backend-lint` ジョブで `pip-audit` が実行される | ✅ 実装済み | - | `pip-audit -r requirements.txt` を `backend-lint` ジョブに追加 |
| 4 | bandit の除外設定が `backend/.bandit`（INI 形式）に集約され、CI の CLI 引数と一致している | ✅ 実装済み | - | `backend/.bandit` 新規作成。`ci.yml` の bandit CLI 引数を削除し、`pre-commit/action` 経由に統一 |
| 5 | `detect-secrets` の JWT 検出状況を確認し、対応方針（追加 or 不要）を記録している | ✅ 実装済み | - | `JwtTokenDetector` が `.secrets.baseline` の `plugins_used` に含まれることを確認。`docs/runbooks/pre-commit.md` に記録済み |
| 6 | `/implement` SKILL.md 手順2 から手動 lint・audit の実行指示が「pre-commit / CI が自動実行」に書き換えられている | ✅ 実装済み | - | `SKILL.md` の Backend/Frontend lint 手動手順が「pre-commit と CI が自動実行する」に置き換え済み |
| 7 | `docs/runbooks/pre-commit.md` のフック一覧が最新の状態に更新されている | ✅ 実装済み | - | Ruff・bandit・ESLint・detect-secrets JWT 対応方針を追記済み |
| 8 | コーディング規約ドキュメントに linter / 手動確認の区分が明示されている | ✅ 実装済み | - | `rules/` 2ファイルの冒頭に「自動強制範囲」「手動レビュー対象」セクションを追加 |
| 9 | CI の全ジョブが pass する | ⚠️ 一部不足 | Medium | テスト結果が `I055_auto_test.md` と `I055_manual_test.md` に未記録（「実施後に記入」「OK / NG」のまま）。CI が実際に pass しているか文書で確認できない |

## コード品質・セキュリティ指摘

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | P8. コスト・保守負荷（計画書ルール遵守） | ステップ2A-0 の「修正後に再チェック:」コマンド（`ruff check backend/ ...`）がステップ本文内に残っており、新設した `plan-writing-rules.md` のルール（「検証コマンドをステップ本文に書かない」）に違反している。本イシューでそのルールを制定したにもかかわらず計画書本文が遵守されていない。ステップ3の同様の記述（`pipx run bandit ...`）も同じ問題。ただしこれらは実装完了済みのステップの記述であり、今後新規計画書への適用が主目的のため、高リスクではない | `docs/plans/open/plan_I055.md` ステップ2A-0（行114-118）、ステップ3（行157-162） | 修正推奨 |
| Medium | 要件適合性・ドキュメント正確性 | `docs/runbooks/pre-commit.md` のフック一覧で bandit の説明が「MEDIUM 以上を対象」と記載されているが、`.pre-commit-config.yaml` の bandit フックに `args: [-ll]` の指定がないため、実際は LOW 以上（すべての severity）が対象になる。`-ll` なしがプランの設計方針（Low 発見も検知して意識的な判断を強制する）であることは正しいが、ドキュメントの記述が実際の動作と一致していない | `docs/runbooks/pre-commit.md` 行45 | 修正推奨 |
| Medium | P8. コスト・保守負荷 | テスト結果が未記録のまま。`I055_auto_test.md` の末尾「backend: （実施後に記入）」「frontend: （実施後に記入）」が空白のまま。`I055_manual_test.md` の「結論: OK / NG」も未確定。CI pass の受け入れ条件（AC #9）の根拠がドキュメントに残っていない | `docs/tests/open/I055_auto_test.md` 末尾、`docs/tests/open/I055_manual_test.md` 末尾 | 修正推奨 |
| Medium | 要件適合性 | `frontend/src/pages/QuizManagement.tsx` が変更されている（`eslint-disable-next-line security/detect-object-injection` を 3箇所追加）が、計画書の影響範囲（§4）に `QuizManagement.tsx` の記載がない。`Login.tsx` のみ記載されている。計画書外の変更だが、ESLint `--max-warnings 0` 達成のために必要な変更であり、計画の意図には沿っている。計画書の影響範囲に追記が必要 | `docs/plans/open/plan_I055.md` §4 影響範囲 Frontend 行 | 修正推奨 |
| Low | ベストプラクティス | `backend/problems/utils.py` の文字列分割（行262-264）で、f-string とリテラル文字列が隣接連結されているが、第2行が f-string ではないため `directory_path` 変数が第2文字列には含まれない（これは意図通りだが、将来の変更時に誤って非 f-string に変数を埋め込もうとしてサイレントバグが生じるリスクがある）。コメントで明示するか、単一 f-string にまとめることを推奨 | `backend/problems/utils.py` 行261-264 | 改善提案 |
| Low | ベストプラクティス | `pre-commit.md` §100付近の「`.secrets.baseline` の更新」セクションにある `detect-secrets scan` コマンドが古い形式（`--force-use-all-plugins` なし）のまま。JWT 検出には `--force-use-all-plugins` が必要だが、ページ下部のロールバック手順などには古いコマンドが混在している | `docs/runbooks/pre-commit.md` 行104-106 | 改善提案 |

## 高リスク判定

判定: No
該当条件: なし

---

✅ コードレビュー完了。`/test I055` を実行してください。
