全ファイルを読み込みました。レビューを開始します。

---

```
# 計画書レビュー [I102] ── 2026-07-06 15:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | テスト妥当性 | TC-AUTO-03（admin 正常系）が list/retrieve/create/update/delete のみ検証しており、`generate_ai`・`generate_adaptive`・`upload_image`・`delete_image` の admin 側 200 検証が含まれていない。AC2「参照・更新系を従来どおり 200/201 で実行できる」には全アクションが含まれる。DRF の `permission_classes` は viewset 全体に一律適用されるため自明とも言えるが、CI 回帰として明示するとより確実。 | `I102_auto_test.md` TC-AUTO-03 | 対応は任意。実装時に TC-AUTO-03 にカスタムアクション（`generate_ai` 等）の admin 正常系ケースを追加することを推奨。 |
| Info | テスト妥当性 | TC-AUTO-03 の DELETE 期待値に「204（論理削除・DRF 既定）」とあるが、「論理削除」（soft delete）は通常 200 を返す実装が多く、「物理削除→204」の DRF 既定と概念が矛盾している。実装が論理削除（soft delete）なら 200 が正しい期待値になる可能性があり、テスト記述が誤検知を招く恐れがある。 | `I102_auto_test.md` TC-AUTO-03 / `plan_I102.md` ステップ3 | 実装時に既存の `ProblemViewSet.destroy()` の返却ステータスを確認し、期待値を実態に合わせて修正すること。 |
| Info | セキュリティ | カスタムアクション（`upload_image`/`delete_image`）が内部で `get_object()` を呼んでいるかどうかの確認が計画書本文には明示されていない（イシューの「参照・更新・削除・画像は `get_queryset` の org 絞り込み（views.py:161→get_object 404）」の記述で示唆はある）。`get_object()` を経由しないカスタム実装の場合、`get_queryset` の org 絞り込みが適用されず IDOR が残存する可能性がある。 | `plan_I102.md` ステップ1 / `I102_auto_test.md` TC-AUTO-02 | 実装時に `upload_image`・`delete_image` アクション内の `get_object()` 呼び出しを確認し、org 絞り込みが効いていることを保証すること。 |
| Info | テスト妥当性 | 未認証（匿名）ユーザーが `/api/problems/` を叩いた場合の 401 レスポンスを検証するテストケースが TC-AUTO に含まれていない。`IsAuthenticated` で担保されるため自明だが、明示テストがあると認可レイヤーの責務分離を文書化できる。 | `I102_auto_test.md` | 対応は任意。 |

## 高リスク判定
判定: Yes
該当条件:
- 認証・認可・ロール変更（`ProblemViewSet.permission_classes` 変更・`OrgAdminRoute` 新設）
- 外部公開 API の認可変更（`/api/problems/` 全エンドポイントに `IsOrgAdmin` 追加）
推奨: `/security-review [I102]` を実行してから `/implement [I102]` へ進んでください。

---

VERDICT: HIGHRISK
```
