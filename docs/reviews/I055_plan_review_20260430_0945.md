# 計画書レビュー [I055] ── 2026-04-30 10:55

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | ステップ8の CI YAML サンプルで `backend-lint` は `pre-commit/action@v3.0.1` を使うが、`SKIP=eslint` を環境変数として設定する手順が YAML サンプルに含まれていない。設計根拠の注記には「ESLint フックを `SKIP=eslint` で除外し」と記述されているが、実際の YAML サンプルに `env: SKIP: eslint` が示されておらず、実装者が見落とす可能性がある | plan_I055.md ステップ8 `backend-lint` YAML サンプル | 実装前に YAML サンプルに `env: SKIP: eslint` を追加することを推奨 |
| Warning | BP | ステップ4-3の `pip install -r requirements.txt` は仮想環境なしで CI の Python に直接インストールするため、ステップ8の `backend-lint` ジョブですでに `pip install -r backend/requirements-dev.txt` を実行した後に `pip install -r requirements.txt`（本番依存）を追加インストールする二重インストールになる。CI 設計との整合性を確認することを推奨 | plan_I055.md ステップ4-3・ステップ8 `backend-lint` YAML | 修正推奨 |
| Info | セキュリティ | detect-secrets ベースライン再生成時（`--use-all-plugins`）に既存の除外済み検出が再審査対象になる場合の対処方針が未記載。ステップ7で `detect-secrets audit .secrets.baseline` が指示されているが、誤検知の除外方法が具体化されていない | plan_I055.md ステップ7 | 改善提案 |
| Info | BP | ステップ6b の bandit Low 発見一覧で `watch_errors.py:69` の B110 処置が `except (ValueError, OSError):` への変更とあるが、`read_text()` が送出する可能性のある `FileNotFoundError`（`OSError` のサブクラス）も `OSError` でカバーされることが計画書の説明からは読み取りにくい。コードコメントで補足することを推奨（実装上は問題なし） | plan_I055.md ステップ6b 処置方針表 | 改善提案 |

## 高リスク判定

判定: No
該当条件: なし
推奨: `/implement I055` を実行してください。

---

**レビューサマリー**

- Blocker 件数: 0 件（差し戻しなし）
- Warning 件数: 2 件（次工程を止めないが、実装前の解消を推奨）
- Info 件数: 2 件（対応任意）

前回レビュー（20260430_0931）で指摘された Warning 3件はすべて対応済み：
- bandit 設定の「YAML」vs「INI」表記不整合 → 受け入れ条件5番が「INI 形式」に統一済み
- ESLint CI 実行経路の不明瞭さ → ステップ8に設計根拠（`SKIP=eslint` の説明）を追記済み
- Ruff `rev` のプレースホルダ → `v0.15.12` に具体化済み

新規 Warning はいずれも実装可能な範囲の指摘であり、Blocker は 0 件。`/implement I055` に進んでよい。
