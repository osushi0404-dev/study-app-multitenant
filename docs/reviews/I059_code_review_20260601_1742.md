すべての確認が完了しました。レビュー結果を出力します。

---

```
# コードレビュー [I059] ── [2026-06-01 17:45]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `issue_template.md` に `現状の問題（証拠・再現手順）`・`根本原因`・`解決方針` の各サブセクションが含まれる | ✅ 実装済み | - | L8-15 に3サブセクション確認済み |
| 2 | `issue_template.md` に `実装対象（既知のもの）` テーブルが含まれる（`スコープ` の直前） | ✅ 実装済み | - | L17（実装対象）< L22（スコープ）配置確認済み |
| 3 | `issue_template.md` に `制約・引き継ぎ情報` セクションが含まれる（`影響範囲` の直後） | ✅ 実装済み | - | L37（制約）が L31（影響範囲）直後・L40（Danger Ops）直前に挿入。既存セクション順序・名称維持・関連資料先頭維持を確認 |
| 4 | `.claude/review-agents/issue-reviewer.md` が新設され、読み取り専用・インジェクション対策ヘッダ・第三者レビュアー指示（自己完結度＋内容妥当性）を含む | ✅ 実装済み | - | `<instructions>` ブロックに stdin・ユーザーメッセージ経由テキストの明示（fix commit f67b3ec 対応済み）・`## 重要:` 二重防御・Bash/Edit/Write/MultiEdit 禁止・5観点のレビュー項目・助言旨の明記をすべて確認 |
| 5 | `scripts/claude/issue-review.sh` が新設され、issue-reviewer を `claude -p`（sonnet-4-6・Read/Grep/Glob）で起動し結果を `docs/reviews/` に保存。非ブロック・shellcheck PASS | ✅ 実装済み | - | `--model claude-sonnet-4-6 --tools "Read,Grep,Glob"` 確認。exit 0 が reviewer未検出/イシュー未検出/claude失敗/空応答の4パターン実装。AT1(shellcheck PASS)・AT2(bash -n PASS)・TC11(3パターン PASS)確認済み |
| 6 | `issue-bootstrap` スキルが GitHub 登録前（step 3.5）に `issue-review.sh` を起動し、結果をユーザーに提示する（ソフト・非ブロック） | ✅ 実装済み | - | SKILL.md L59-69 に step 3.5 追加。ソフト警告・非ブロック・プレースホルダ扱い方針を明記確認 |
| 7 | `issue-bootstrap` スキルの完了ガイドに「自己完結なら `/grill-me` スキップ可」が明記され、issue-review の判定で報告文が動的分岐する | ✅ 実装済み | - | step 6 に (A)要補足・(B)十分 の両分岐、(B)で `/grill-me` スキップ明記確認 |
| 8 | 改訂後のテンプレート＋issue-review フローで新規イシューを1件作成し、コンテキストクリア後に `/plan-issue` に入れることを確認する | ✅ 実装済み | - | TC9 でゼロ文脈サブエージェントが検証用 I061 を「十分」判定（AC の趣旨を直接再現。TC10 実セッション確認は任意とし代理検証充足と判断）。後始末（GitHub #124 クローズ・ローカル削除）確認 |
| 9 (C1) | `docs/runbooks/issue-flow.md` のフェーズ1 フロー図に issue-review（step 1.5・自己完結度レビュー・ソフト）が追記されている | ✅ 実装済み | - | L110 に step 1.5 挿入。十分/要補足分岐の両ケース記載確認 |
| 10 (C1) | `docs/runbooks/workflow.md` の「使うスキル」`/issue-bootstrap` 説明とフローに issue-review が反映されている | ✅ 実装済み | - | L4 使うスキル説明・L23-24 フロー手順に issue-review とスキップ可記載確認 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス（SKILL.md の明示性） | SKILL.md step 3.5 と step 6 の接続において、Claude が `bash scripts/claude/issue-review.sh` の stdout から `## 判定: 十分/要補足` を読み取って (A)/(B) 分岐を選択する仕組みが本文に明示されていない。Claude は自然言語処理でstdoutを解釈できるため実害はなく TC9 で動作確認済みだが、「step 3.5 実行後、Claude は出力の判定テキストを読み取り step 6 の分岐に使う」という一文を step 3.5 または step 6 の先頭に追記すると、将来の保守者（または LLM バージョン変更時）により堅牢になる | `.claude/skills/issue-bootstrap/SKILL.md` step 3.5 / step 6 | 任意改善。既存動作は確認済みのため対応必須ではない |
| Info | ベストプラクティス（既存パターン踏襲） | issue-review.sh が stdin 経由でイシュー本文を渡す（L33-36）一方、issue-reviewer.md が `docs/issues/open/I###.md` を Read で再取得する指示を持つ（L47-51）ため、イシュー内容が二重取得される動作になる。既存 `plan-issue-review.sh` + `plan-reviewer.md` も同パターンを採用しており、プロジェクト内で一貫した設計選択と確認できるため実害なし | `scripts/claude/issue-review.sh` L33-36 / `issue-reviewer.md` L47-51 | 既存パターン踏襲のため現状維持でよい |

## 高リスク判定
判定: No
該当条件: なし
- Backend/Frontend/DB 変更なし。アプリコード・認証認可・テナント境界に一切影響しない
- プロンプトインジェクション対策は `<instructions>` ブロック（stdin 言及含む・fix f67b3ec 適用済み）＋ `## 重要:` の二重防御で実装済み
- 非ブロック設計は exit 0 の4パターン全ケースで TC11 PASS 確認済み
- shellcheck AT1 PASS・bash -n AT2 PASS 確認済み
- grill-me の `## 設計確認メモ` 挿入位置（`## 関連資料` 直後）とテンプレート新セクション順序は非競合を TC3 で確認済み

---
✅ コードレビュー完了。`/test [I059]` を実行してください。
```
