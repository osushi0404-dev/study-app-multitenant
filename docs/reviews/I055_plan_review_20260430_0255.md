過去のレビューとの差分を把握しました。現在のドキュメントを精査してレビューを実施します。

計画書・イシュー・テスト文書の内容を網羅的に確認しました。以下がレビュー結果です。

---

# 計画書レビュー [I055] ── 2026-04-30 09:00

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `pip-audit==x.x.x` とバージョンが未確定のまま計画書に残っている。`ruff==0.15.12` / `bandit==1.7.9` は具体バージョンが記載されており整合性が取れていない。実装時に `pip install pip-audit` で取得した確定バージョンを記入すること | plan_I055.md ステップ2A-1 | 修正推奨 |
| Warning | BP | ステップ3では bandit の設定ファイルを `backend/.bandit`（INI 形式）として作成する設計だが、ステップ6のフック YAML では `args: ["-c", "backend/setup.cfg"]` のような CLI 引数指定の記述はなく「`.bandit` が自動検出されるため `--configfile` 不要」と記載されている。一方、受け入れ条件（計画書3節）では「`backend/.bandit`（YAML）に集約し、`--configfile .bandit` が明示されている」とあり、INI 自動検出と `--configfile` 明示で記述が矛盾している。実装前に一方に統一すること | plan_I055.md ステップ3・ステップ6・受け入れ条件 | 修正推奨 |
| Warning | テスト妥当性 | TC-05（bandit 設定の集約確認）の期待値が「`setup.cfg` に `[bandit]` セクションがある」とあるが、計画書ステップ3では bandit 設定は `backend/.bandit` に移行し `setup.cfg` から削除する設計になっている。テスト期待値と計画書の設計が不一致。実装確定後にどちらに集約するか一致させること | I055_auto_test.md TC-05 | 修正推奨 |
| Info | モダン | ステップ8 の CI YAML サンプルで `frontend-lint` ジョブに `pre-commit/action` の記述がなく、`npm audit` ステップのみが示されている。ESLint フックは pre-commit に追加する設計のため、`pre-commit run --all-files` 経由で実行されるはずだが、`frontend-lint` 側の YAML に `pre-commit/action` がないため CI 上での ESLint 実行経路が不明瞭。最終実装時に YAML と設計根拠を一致させることを推奨 | plan_I055.md ステップ8 | 改善提案 |
| Info | セキュリティ | detect-secrets の JWT 検出ギャップ対応（ステップ7）で `detect-secrets audit .secrets.baseline` による誤検知確認手順は記載されているが、ベースライン再生成時に既存の除外済み検出が再審査対象になる場合の対処方針（差分確認・スキップ等）が未記載。軽微な懸念のため記録のみ | plan_I055.md ステップ7 | 改善提案 |
| Info | BP | ステップ2A-1 の `requirements-dev.txt` 記載例で `pip-audit==x.x.x` のバージョンプレースホルダが残っている。実装時に `pip install pip-audit` で最新の確定バージョンを取得して記入すること（Warning と同根指摘） | plan_I055.md ステップ2A-1 | 改善提案 |

## 高リスク判定

判定: No
該当条件: なし
推奨: `/implement I055` を実行してください。

---

**レビューサマリー**

- Blocker 件数: 0 件（差し戻しなし）
- Warning 件数: 3 件（実装前に解消推奨だが次工程を止めない）
- 主要な Warning は bandit 設定ファイルのパス（`backend/.bandit` vs `setup.cfg`）と形式（INI 自動検出 vs `--configfile` 明示）の不一致、および自動テスト TC-05 の期待値との矛盾。実装時に設計を一方に統一してテストケースも更新すること。
