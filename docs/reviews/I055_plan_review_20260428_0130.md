これで全ファイルの確認が完了しました。レビューを実施します。

---

# 計画書レビュー [I055] ── [2026-04-28 00:00]

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | ベストプラクティス | ステップ3で `bandit -r . --configfile setup.cfg` または自動検出に変更するとしているが、bandit 1.7.x の `setup.cfg` 自動検出はプロジェクトルートに配置された場合に限られる。現構成では `setup.cfg` は `backend/` 配下にあり、`bandit -r .`（working-directory: backend）で実行した場合に自動検出されるか未検証のまま進む設計になっている。ステップ3の「設定読み込み検証（必須）」は明記されているため実施されることが前提だが、検証コマンドの出力が空（設定未適用）の場合のフォールバック（`--configfile setup.cfg` を明示）が計画書に記載されていない。 | plan_I055.md ステップ3 | 修正推奨 |
| Warning | ベストプラクティス | TC-03b（コミットブロック検証）でダミーファイルを `/tmp/test_lint_error.py` に作成しているが、pre-commit フックは `files: ^backend/` フィルタがある場合 `/tmp/` 配下のファイルをスキャン対象外とする。Ruff/flake8 フックに `files:` フィルタが設定されていない場合は問題ないが、設定内容次第でブロックされない（exit code 0 になる）可能性がある。ダミーファイルのパスは `backend/test_lint_dummy.py` 等リポジトリ内のパスにするか、実行前にフィルタ条件を確認する手順を追加することを推奨する。 | I055_auto_test.md TC-03b / I055_manual_test.md No.6 | 修正推奨 |
| Warning | ベストプラクティス | ステップ6の ESLint pre-commit フック設定で `--max-warnings 0` を args に指定しているが、計画書のリスク欄に「ESLint は `--max-warnings 0` で警告をエラー化しない設定で調整」と矛盾する記述がある。前者は「0警告でブロック」であり後者は「警告でブロックしない」という逆の意味になっている。実際の設計意図（ESLint warnings を事前に0件にしてから `--max-warnings 0` を適用する）は計画書本文中に記載されているが、リスク欄の記述が実装者に誤解を与える可能性がある。 | plan_I055.md ステップ6・Risk欄3行目 | 修正推奨 |
| Info | ベストプラクティス | ステップ4の pip-audit ベースライン確認で「既知の問題には `--ignore-vuln VULN-ID` を付与する方針を決定してから CI に追加する」とあるが、現時点でのベースライン実行結果（CVE 件数・ID）が計画書に記載されていない。実装時に想定外の CVE が多数検出された場合、実装者がどこまで除外してよいかの判断基準が計画書に存在しない。除外基準（例：「修正バージョンが存在しない場合のみ除外、理由を requirements-dev.txt にコメント」等）を計画書またはリスク欄に明示することを提案する。 | plan_I055.md ステップ4・Risk欄5行目 | 改善提案 |
| Info | Claude Code BP | `.claude/skills/implement/SKILL.md` の変更内容（削除対象の文言・置き換え後の文言）がステップ9①に具体的に記載されており、意図は明確。前回レビューで Info 指摘した箇所は修正済み。 | plan_I055.md ステップ9① | 問題なし（前回指摘解消済み） |

**前回 Blocker 3件の修正状況:**
- [B1] audit-level 乖離: イシュー I055.md の AC が `--audit-level=critical`（high は react-scripts 問題で修正不可のため）に更新されており、計画書と一致。解消済み。
- [B2] 手動確認区分の欠落: 計画書ステップ9③・受け入れ条件9項目に「手動レビュー対象」セクションの明示が追加されている。解消済み。
- [B3] コミットブロック検証の欠如: 自動テストに TC-03b（lint エラーファイルを使ったブロック検証・exit code 確認）が追加され、手動テスト No.6 も Claude 実施可能なコマンドベースに変更されている。解消済み。

## 高リスク判定

判定: No
該当条件: なし
推奨: `/implement I055` を実行してください。
