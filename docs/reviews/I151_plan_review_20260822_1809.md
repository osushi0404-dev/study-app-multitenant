4つのファイルをすべて確認しました。レビューを開始します。

---

```
# 計画書レビュー [I151] ── [2026-08-22 14:30]

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | P4 テスト妥当性 | **TC-AUTO-11 のステップ 1 整合性**: 計画 §6 ステップ 1 点 5 で TC-AUTO-11 を検証対象に列挙し、点 6 で「すべて合格したら実装コミット」としている。しかし TC-AUTO-11 の宣言側複合コマンドには `grep -qx 'pytest==9.0.3' backend/requirements-dev.txt` が含まれており、ステップ 1 時点（pytest=8.3.4）では必ず非ゼロ終了する。auto_test 文書の「補足」は「ステップ 1 時点では不合格が正しい」と断り書きするが、計画の「すべて合格」条件と矛盾する。文字通りに従う executor が ①コミット不能でブロックする、②TC-AUTO-11 を丸ごとスキップするという両極端に振れるリスクがある。**推奨**: 計画 §6 ステップ 1 の TC リストから TC-AUTO-11 を除き、ステップ 2 のみに移す（実装側の Django==5.2.17 インストール確認は TC-AUTO-11 の installation 側を単独で走らせるか、TC-AUTO-01/TC-AUTO-04 で代替できる）。あるいは点 6 に「ただし TC-AUTO-11 は pytest==9.0.3 判定を除いて合格であれば commit 可」と明記する。 | `docs/plans/open/plan_I151.md` §6 ステップ 1 点 5–6 / `docs/tests/open/I151_auto_test.md` TC-AUTO-11 補足 | 実装前に是正推奨 |
| Info | BP | **TC-AUTO-10 の二重ログ取得**: `docker compose logs celery --since 3m` を `&&` で 2 回実行している（成功ログの grep と ERROR/Traceback の grep）。2 回の実行の間に新たなログが書き込まれた場合、成功確認とエラー確認が異なるスナップショットを見る微細な競合がある。`docker compose logs celery --since 3m > /tmp/i151_celery.log 2>&1 && grep -qE 'succeeded' /tmp/i151_celery.log && ! grep -qE 'ERROR\|Traceback' /tmp/i151_celery.log` とテンポラリファイル経由にすると 1 スナップショットで完結する。 | `docs/tests/open/I151_auto_test.md` TC-AUTO-10 | 改善任意 |
| Info | P4 テスト妥当性 | **TC-AUTO-09 の再試行時リスク**: `CREATE DATABASE migrate_check` は既存 DB があると失敗し TC が中断する（前回 TC 失敗で後片付け `DROP DATABASE` が実行されなかった場合）。手順冒頭に `docker compose exec -T db psql -U postgres -c "DROP DATABASE IF EXISTS migrate_check;"` を入れると再試行時も安全。 | `docs/tests/open/I151_auto_test.md` TC-AUTO-09 | 改善任意 |
| Info | BP | **TC-AUTO-11 の `grep` フラグ不統一**: 宣言側複合コマンドで `djangorestframework` のみ `-qix`（大文字小文字無視）を使い、他は `-qx`（完全一致）を使っている。`pip freeze` の出力は PyPI の標準化名称で固定されるため動作上の問題はないが、意図が読みにくい。他と揃えて `-qx` で統一するか、コメントで意図を記載する。 | `docs/tests/open/I151_auto_test.md` TC-AUTO-11 実インストール側 | 改善任意 |

## 高リスク判定
判定: Yes
該当条件:
- 認証・認可・ロール変更 — django-guardian（認可ライブラリ）を `INSTALLED_APPS` および `requirements.txt` から除去する（auth backend 未登録・コード未使用だが、依存カテゴリとして認可ライブラリに分類される）
- DRF 3.15 → 3.18（マイナー 3 段）— REST API の権限クラス・シリアライザ・例外ハンドラを担うフレームワークの更新

推奨: 計画承認ポイント S-4 に記載のとおり、実装後に `/security-review [I151]` を実行してから `/close [I151]` へ進んでください。

---

VERDICT: HIGHRISK
```
