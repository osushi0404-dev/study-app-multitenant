`_ask()` と既存 hook の `tool_input` アクセスパターンを確認しました。レビューを確定します。

---

```
# 計画書レビュー [I146] ── [2026-08-04 14:30]

## 判定: 差し戻し（Blocker 1件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | BP（外部挙動依存の未実証） | `mcp__github__*` ツールが PreToolUse hook の stdin に渡す `tool_input` の JSON キー名が未実証のまま `pullNumber`（キャメルケース）と決め打ちされている。既存の hook 実装（`_check_edit_write`）は `tool_input.get("file_path")` とスネークケースで取得しており、MCP ツールも `pull_number`（スケマ定義名のまま）で渡る可能性が高い。`pullNumber` で取得すると常に `None` になり、識別子なしで `gh pr view` を呼ぶことになる（カレントブランチの PR を解決しようとするか、取得失敗→ ask 降格）。PR 番号が文言に出ず、想定外の PR を参照する可能性がある。スパイク S1〜S8 にこの確認が含まれていない。**条件(a)**: 未知前提・未実証・実証手段の明記なし。fix: スパイクに「サンプル MCP 呼び出しを hook に流し `tool_input` の JSON キー名（`pullNumber` か `pull_number` か）を実測する」を追加し、その結果に合わせてコードを修正すること。 | 計画書 §5-1 `_mcp_pr_target` の `tool_input.get("pullNumber")`・§2-4 スパイク表 | 実装前に実証し計画書を修正 |
| Warning | BP（設計メモ↔本文整合） | grill-me の設計確認メモに「ask の理由文言に fork 由来である旨と head リポジトリ名を含める」とあるが、計画書 §5-1 の `_guard_cross_repo` の ask 文言には head リポジトリ名が含まれていない。`_pr_is_cross_repo` は True/False/None のみ返すため、head リポジトリ名を含めるには別途 `gh pr view --json headRepositoryOwner` 等の追加照会が必要（照会増によるレイテンシとの兼ね合いで省略する場合はその判断を計画書に明記すること）。 | イシュー §設計確認メモ「hook の遮断強度」・計画書 §5-1 `_guard_cross_repo` の `_ask()` 文言 | 修正推奨（「省略する理由を明記」でも可） |
| Warning | BP（影響範囲の記載もれ） | イシュー文書の「実装対象」表に `.claude/settings.json` の MCP matcher 追加が記載されていない（根本原因 #5・#6 には問題として記載されているが、実装対象に昇格していない）。計画書では AC-10 として追加されており、実装者は計画書を見れば分かるが、イシュー文書との乖離があり後からトレースしにくい。 | イシュー §実装対象表・計画書 §3 AC-10・§5-2 | 修正推奨（次回イシュー起票時の教訓でも可） |
| Info | P4（テスト妥当性） | `test_pretooluse_gh_pr_guard.sh` のスニペットに `runj()` の実装詳細が `...` で省略されており、G13〜G19（MCP ツール）のテストを実行するヘルパー（`mjson()` に対応する `runmjson()` 相当）の呼び出しパターンが計画書から読み取れない。「push guard と同方式」の記述のみ。 | 計画書 §5-3 `test_pretooluse_gh_pr_guard.sh` の `runj()` 定義 | 改善提案（実装者が判断可能なレベルだが実装時に要注意） |

## 高リスク判定
判定: No
該当条件: なし
推奨: Blocker 修正後に再度 `/plan-issue-review I146` を実施し、VERDICT が HIGHRISK または OK になってから `/implement I146` を実行してください。

---
Blocker の修正内容:
1. **スパイクの追加（§2-4）**: `test_pretooluse_gh_pr_guard.sh` または別の確認手段で、`mcp__github__merge_pull_request` を hook に流したときの `tool_input` の JSON キー名を実測する（例: `{ "tool_name": "mcp__github__merge_pull_request", "tool_input": {"pull_number": 257, "owner": "...", "repo": "..."} }` を stdin に流して期待どおりに block されるかを確認する）。
2. **コードの修正（§5-1）**: 実測結果に応じて `tool_input.get("pullNumber")` を `tool_input.get("pull_number")` 等に修正する。
3. **TC-03 の補完（§5-3）**: G13〜G19 の MCP テストで投入する JSON の `tool_input` キー名を実測結果に合わせる（テストと実動作のキー名が一致するよう担保する）。

明確化後に再度 `/plan-issue-review I146` を実施してください。

VERDICT: BLOCKER
```
