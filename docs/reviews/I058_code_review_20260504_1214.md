# コードレビュー [I058] ── [2026-05-04 08:30]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `scripts/claude/plan-issue-review.sh I###` を実行すると、タイムスタンプ付きレビューファイルの保存・PR コメント投稿・計画書リンク追記・判定案内が一貫して行われる | ✅ 実装済み | - | ファイル保存（59行目）・gh pr review（64行目）・計画書追記（70-72行目）・判定案内（75-81行目）すべて実装済み |
| 2 | `scripts/claude/code-review.sh I###` を実行すると、同様の処理が一貫して行われる | ✅ 実装済み | - | 保存（88行目）・PR投稿（92行目）・判定案内（97-103行目）実装済み。計画書リンク追記は code-review では設計上不要（旧スキルも同様） |
| 3 | システムメタデータ（`agentId:`・`<usage>`）がレビューファイルに混入しない | ✅ 実装済み | - | 両スクリプトとも `claude -p` を使用（Agent ツール非使用）。TC-05 PASS 済み |
| 4 | 末尾スペース・末尾改行の正規化がスクリプト内で保証され、pre-commit の修正が発生しない | ✅ 実装済み | - | `sed 's/[[:space:]]*$//'` + `printf '%s\n'` による正規化。TC-03 PASS 済み |
| 5 | `/plan-issue-review`・`/code-review` スキルから従来通り呼び出せる（後方互換） | ✅ 実装済み | - | 両 SKILL.md が `disable-model-invocation: true` + `bash scripts/claude/...sh $ARGUMENTS` の thin wrapper に更新済み。TC-06 PASS 済み |
| 6 | 両スクリプトの `claude -p` に `--allowedTools "Read" "Grep" "Glob"`（スペース区切り個別引数）が設定されており、Edit・Write・Bash が使用不可 | ✅ 実装済み | - | plan-issue-review.sh 53行目・code-review.sh 82行目ともに `--allowedTools "Read" "Grep" "Glob"`（個別引数）確認済み。TC-10・TC-11 PASS 済み。なお提供された git diff 内の plan-issue-review.sh は旧版（`"Read,Grep,Glob"`）を示しているが、実ファイルは修正済み |
| 7 | `code-reviewer.md`・`plan-reviewer.md` にツールアクセス制限（Bash/Edit/Write 禁止）が明記されている | ✅ 実装済み | - | 両ファイルに「ツールアクセス制限」セクションが追記済み（diff 確認・本レビューで読み込み確認） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | 運用性 | 両スクリプトにファイル保存前の `mkdir -p "$(dirname "$REVIEW_FILE")"` がない。`docs/reviews/` が存在しない環境（新規クローン・CI 等）では `set -euo pipefail` によりスクリプトが即終了する。計画書レビュー（20260503_1438・20260503_1444版）で Warning 指摘されたが未対応 | `scripts/claude/plan-issue-review.sh` 59行目前・`scripts/claude/code-review.sh` 88行目前 | ファイル保存前に `mkdir -p "docs/reviews/"` を追加 |
| Medium | 運用性 | `claude -p` が空文字列を返した場合（API エラー・認証切れ・レート制限等）のガードがない。`REVIEW` が空のまま後続処理が続行し、空のレビューファイルが保存されて `grep` が「差し戻しなし」と誤判定する。計画書レビュー（20260503_1444版）で Warning 指摘されたが未対応 | `scripts/claude/plan-issue-review.sh` 53行目後・`scripts/claude/code-review.sh` 82行目後 | `REVIEW=$(claude -p ...)` 直後に `[ -z "$REVIEW" ] && { echo "⚠️ claude -p が空を返しました。終了します。"; exit 1; }` を追加 |
| Medium | テスト妥当性 | 手動テスト No.5 の検証コマンドが `--allowedTools "Read,Grep,Glob"` （カンマ区切り）を使用しており、スクリプト本体の `--allowedTools "Read" "Grep" "Glob"`（スペース区切り個別引数）と異なる形式でテストしている。カンマ区切りが無効ツール名として扱われた場合、Edit は「ツールが許可されていないため」に失敗し PASS になるが、スクリプトの本来の動作を検証したことにならない | `docs/tests/open/I058_manual_test.md` No.5 手順列 | テストコマンドを `claude -p --allowedTools "Read" "Grep" "Glob" "..."` に修正してから実施 |
| Low | 保守性 | `plan-issue-review.sh` の計画書リンク追記ロジックが実行ごとに `## レビュー結果` ヘッダーを生成するため、複数回実行後に計画書内で同名ヘッダーが重複する。`plan_I058.md` に現在 7 つの `## レビュー結果` セクションが存在。設計上の許容として明示するか、既存セクションへの追記方式に改善することを推奨 | `scripts/claude/plan-issue-review.sh` 69-72行目 | 設計意図として明示するか、I059 等の別イシューで改善 |
| Low | セキュリティ | 引数検証 `${1:?...}` は空文字チェックのみで、`../../etc/passwd` 形式の引数が `find_file()` に渡される。`find_file()` の固定プレフィックス（`docs/${dir}/open/`）と `-f` チェックにより実害リスクは低いが、`[[ "$ISSUE" =~ ^I[0-9]+$ ]]` による形式チェックの追加で確実に防止できる。内部ツールのため任意対応 | 両スクリプト 4行目 `${1:?...}` | `[[ "$ISSUE" =~ ^I[0-9]+$ ]] \|\| { echo "Invalid issue ID format. Expected: I###"; exit 1; }` を追加（対応任意） |

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test I058` を実行してください。
