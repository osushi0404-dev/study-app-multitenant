4ファイル読み取り完了。独立したレビューを実施します。

---

```
# 計画書レビュー [I151] ── 2026-08-23 15:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | ロールバック手順で「ファイルを戻す」の具体的操作方法が未明示。「コミット前なら `requirements-dev.txt` の pytest を 8.3.4 へ戻す」と書かれているが、`git restore` か Edit かが不明。この文脈では `git restore` を使っても実害は小さいが、BP 観点（未コミット変更への `git restore` 禁止）との整合が明示されていない。 | 計画書 §8「ステップ 2 で失敗」・「ステップ 1 で失敗」 | 修正推奨（「`requirements-dev.txt` を Edit で 8.3.4 に書き戻す」等と明示する） |
| Info | モダン | django-redis 5.4.0→7.0.0（メジャー2段更新）について、事前スパイク §2-5 に DRF は `exception_handler` 等の API 互換を個別確認しているが、django-redis は `manage.py check` 通過（設定整合）のみが確認されており、キャッシュシリアライザ設定・コネクションプール等の固有挙動確認は明示されていない。テスト実走（94件）で担保する方針は合理的で実装上の問題はないが、将来の参照用に確認項目として記録しておくと望ましい。 | 計画書 §2-5 事前スパイク・§5-1（django-redis 行） | 改善提案（対応任意） |

（他観点）
- P1 要件適合性: 受け入れ条件はイシューに忠実で過不足なし。マルチテナント境界は自前実装であり guardian に依存しないことを事前実測で確認済み。問題なし。
- セキュリティ: 認可判定経路の変更なし（guardian は AUTHENTICATION_BACKENDS 未登録）を §2-4 / §16 で確認。OWASP A06 を直接是正。pip-audit 本体・開発とも 0 件を計画時点で実測。DEFAULT_PERMISSION_CLASSES: 新規 View 追加なし。トランザクション原子性: DB スキーマ変更なし。対象外。
- BP（コマンド衛生）: TC 文書の全コマンドが `&&` / パイプで束ねられていない。`2>&1` リダイレクト・`$()` コマンド置換のみ使用で、複合化禁止原則に適合。`wc -l < file` がパイプでないことも明記済み。
- BP（外部挙動依存）: celery タスク成功まで確認する TC-AUTO-10 の実証手段が明記され、機構の事前検証（grep パターンの一致）も記録済み。未知リスクと認識する外部前提が未実証のまま残っているケースなし。
- P4 テスト妥当性: false-green 検証が TC-AUTO-04/05/06/07/11A/16 に対して実施済みで記録あり。否定系 TC の合否基準（`! grep -q...` → exit 0 が合格）と exit code の向きが一致。TC-AUTO-14 の `gh pr checks --json` 判定式の動作も PR #256 で事前確認済み。
- P3 データ整合性: `guardian_*` 2 テーブルの意図的残置が明記（§10）。マイグレーション新規生成なし（TC-AUTO-08/09 で担保）。ロールバック時の DB 復旧不要も明記。
- P5 運用性: celery の非同期タスク成功まで確認する方針（TC-AUTO-10）。ロールバック手順が各段階で定義済み（§8）。
- Claude Code BP: 変更対象に `.claude/skills/` 等の指示ファイルは含まれない。対象外。
- P8 コスト: 依存2件削除で保守対象が縮小。対象外。
- P9 プライバシー: 個人情報・テナントデータの取り扱い変更なし。対象外。

## 高リスク判定
判定: Yes
該当条件:
- 認可ライブラリ（django-guardian）の依存除去（guardian は `AUTHENTICATION_BACKENDS` 未登録で実害なしと実証済みだが、カテゴリとして認証・認可関連の変更に該当）
- 主要フレームワークのメジャーバージョン更新（Django 4.2→5.2 / DRF 3.15→3.18 / django-redis 5.4→7.0）

推奨: `/security-review [I151]` を実行してから `/implement [I151]` へ進んでください。
（計画書 §18 でも「実装後に `/security-review I151` を実施する」と明記されています）

---

VERDICT: HIGHRISK
```
