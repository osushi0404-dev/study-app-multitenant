全ファイルの読み込みが完了しました。前回レビューの Blocker（`pullNumber`→`pull_number`）が修正されているか確認しつつ、残存課題を精査します。

---

```
# 計画書レビュー [I146] ── [2026-08-04 23:30]

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | P4（テスト妥当性） | TC-04 FG-C の注入仕様が型不一致。`_pr_is_cross_repo` は `(cross, head)` の 2-タプルを返す設計だが、計画書 §5-3 と auto_test.md TC-04 の FG-C は `return False`（スカラー bool）を注入すると指定している。`_guard_cross_repo` 内の `cross, head = _pr_is_cross_repo(...)` がこれを受けると `TypeError: cannot unpack non-iterable bool object` でクラッシュ（exit 1）し、期待する "exit 0（素通し確認）" に到達しない。TC-04 は「FG-A/C/D が exit 0 を返す」とアサートするため、FG-C が exit 1 になると TC-04 全体が実装正しくても偽陰性（false failure）を報告する。正しい注入は `return False, None`（または `return (False, None)`）。plan/auto_test.md 両方で同様の記述のため実装者が踏む可能性が高い | 計画書 §5-3 false-green 注入表 FG-C 行、`I146_auto_test.md` TC-04 FG-C 行 | 実装時に `return False, None` へ修正推奨 |
| Info | BP（コマンド衛生・参考） | TC-02 の `sed` 注入式は「実装時に対象行へ合わせて確定する」と明記されており、計画書・テスト文書どちらにも未確定の旨が記載されている。実装者が確認すべき事項として残るが、差し戻し対象ではない | `I146_auto_test.md` TC-02 注 | 実装時に対象行へ合わせて確定（明記済み） |
| Info | Claude Code BP（分岐パリティ確認） | `.claude/skills/close/SKILL.md` step 0 へ追加するコマンドブロック（§5-6）は `--json isCrossRepository --jq '.isCrossRepository'` を使う形式で書かれているが、`pr-base-sync.sh`（§5-4）は `-q .isCrossRepository` を使っている。どちらの `gh` フラグ形式を使うかはホスト環境で動作するため大差ないが、SKILL.md には `--jq` を使い pr-base-sync.sh には `-q` を使うという表記揺れがある。機能上の問題はないが視覚的な一貫性のため揃えることを推奨 | 計画書 §5-4 vs §5-6 | 改善提案（整合させるなら `--json isCrossRepository -q .isCrossRepository` に統一） |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I146` を実行してください。

---

前回 Blocker の解消確認:

1. **キー名実測（S9 追加）**: `pull_number`（スネークケース）を JSON Schema の実取得で確定。計画書・テスト・実装コードがすべて `pull_number` / `pull_number` フォールバック `pullNumber` の形で一致している ✅
2. **head リポジトリ名の文言含有（Warning 修正）**: `_pr_is_cross_repo` が `(cross, head)` のタプルを返す形に変更され、`_guard_cross_repo` が `origin = f"（head: {head}）" if head else ""` で文言に含めている。S10 により同一クエリで取得可（照会 1 回のまま）✅
3. **`.claude/settings.json` の MCP matcher 追加がイシュー実装対象表に記載済み（Warning 修正）**: イシュー §実装対象表に MCP matcher 行が追加されている ✅
4. **MCP テストヘルパー（`mjson` / `runm`）が §5-3 に明示（Info 修正）**: 両ヘルパーが定義・使用例とともに記載されている ✅

VERDICT: OK
```
