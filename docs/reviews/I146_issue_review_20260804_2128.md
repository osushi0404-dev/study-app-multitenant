# イシュー自己完結度レビュー [I146]

## 判定: 十分

重要セクションに未記入・テンプレート説明文の残留はなし。根本原因・解決方針・実装対象・スコープ・受け入れ条件のいずれも実質的に記入されており、文脈ゼロの担当者が `/plan-issue` に着手できる水準に達している。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 自己完結度 | runbook の配置先（`workflow.md` か `issue-flow.md`）を「計画時に確定」と明示している点は適切だが、計画フェーズで選定根拠が問われる。どちらに置くかの判断軸（例：操作手順系 → `workflow.md` / イシューライフサイクル系 → `issue-flow.md`）をイシューに添えておくと計画者の調査コストが下がる。 | 実装対象テーブル 4 行目 |
| Info | 解決方針の整合 | 問題 3（Actions 承認してしまった）への直接的な gate が存在しない点は解決方針内で触れられていない。do 層（runbook への記載）で対応する意図は読み取れるが、「gate が /close step 0 のみに置かれており、承認ミスは do 層のみで防ぐ設計判断」と一文添えるとレビュアーの疑問が解消される。 | 解決方針 |
| Info | 実装対象の妥当性 | 受け入れ条件に「sync / final 両モード」とあるが、実装対象テーブルの `pr-base-sync.sh` 行にはモードへの言及がない。計画時に「冒頭チェックがどちらのモードでも共通に走ること」を確認するリマインダとして、テーブルに一言添えておくと計画者の見落とし防止になる。 | 実装対象テーブル 2 行目 / 受け入れ条件 2 項目 |

---

## 次のアクションの推奨

重大な欠落・論理矛盾はなし。上記 Info はすべて計画フェーズで自然に解消される粒度のため、**/grill-me をスキップして `/plan-issue` に直接進められる**。
