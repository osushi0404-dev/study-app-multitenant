全ファイルと参照ソースコードを読み終えました。レビューを実施します。

---

```
# 計画書レビュー [I142] ── 2026-07-21 14:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | テスト妥当性 | テスト文書（I142_auto_test.md）の共通フィクスチャに `raise_request_exception=False` の設定が記載されていない。計画書 5-7 には明記されているが文書間の不整合がある。なお、設計上は `custom_exception_handler` が RuntimeError を Response に変換して返すため Django テストクライアントまで例外が伝播せず、本設定は実際には不要（調査2 のスパイクと一貫）。ただし「計画書に記載・テスト文書に非記載」のまま実装へ進むと混乱を招く可能性がある | `docs/tests/open/I142_auto_test.md` 共通フィクスチャ節 | テスト文書に「TC-AUTO-01/04 では `APIClient(raise_request_exception=False)` を使うが設計上は不要（custom_exception_handler が Response を返すため伝播しない）」の補足コメントを追記するか、計画書 5-7 の記述と整合させることを推奨 |
| Warning | ベストプラクティス | `EnvironmentMisconfiguredError` 発生時に `_get_organization` 内の `logger.error()` と `custom_exception_handler` 内の `logger.exception()` が**二重に** `django.request` / `django` の各ロガーへ記録される。計画書・テスト文書に言及がなく、error_file のノイズになる。`_get_organization` の `logger.error` で意味あるメッセージを記録し、`custom_exception_handler` の `logger.exception`（5-1 の `if response.status_code >= 500:` 分岐）はスタックトレース記録が主目的だが、`EnvironmentMisconfiguredError` は明示的 raise であり exc_info はスタック深度が浅い。二重記録の意図をコメントで明記することを推奨 | `plan_I142.md` 5-1 コードスニペット / `_get_organization` | `custom_exception_handler` の 5xx 分岐コメントに「APIException 由来（EnvironmentMisconfiguredError 等）は raise 元で記録済みのため、ここでのスタックトレースは補足情報」と注記すると保守時の混乱を防げる |
| Info | 要件適合性 | `create` メソッドの slug 指定組織不在時の 400 応答（5-3 コードスニペット）に `details` キーが欠落（`{'error': {'main_message': '...', 'sub_message': '...'}}` のみ）。統一 JSON の定義（`{details: {}}`）と微妙に不整合だが、既存コード（views.py:79-84）と同パターンであり FE 消費箇所への影響もないため動作上の問題はない | `plan_I142.md` 5-3 | 対応は任意。既存コードのパターン踏襲として許容可 |
| Info | ベストプラクティス | `create` メソッドと `OrganizationSlugValidationView.get` の `import logging` がメソッド内にある（既存コードと同パターン）。Python の慣習ではファイル先頭にまとめるべきだが、本イシューの変更対象外であり既存パターンを踏襲する記載 | `plan_I142.md` 5-3 / 5-5 コードスニペット | 対応は任意。別イシューで一括整理する方が適切 |

## 高リスク判定
判定: Yes
該当条件:
- 外部公開 API の挙動変更（logout: 有効トークンで 400→200 へ変化。想定外例外: 汎用 4xx→統一 JSON 5xx へ変化）

推奨: （Yes の場合）`/security-review [I142]` を実行してから `/implement [I142]` へ進んでください。

---

補足事項（参考情報・差し戻し対象外）:
- TC-AUTO-11/12 の期待値（500 body 完全一致）は `EnvironmentMisconfiguredError` → `exception_handler` → `custom_exception_handler` の既存 `elif status_code == 500:` 分岐 → `details: {}` 含む統一 JSON の変換経路を実コードで追跡しても一致することを確認済み（core/exceptions.py:64-67 の変換ロジックと plan_I142 5-1 の期待値が整合）
- I140 既存テスト TC-AUTO-03 は変更後も PASS する見込み（`_get_organization` の `logger.error` 呼び出しが `raise` より前にあるため `caplog` で捕捉可能）
- `set_rollback()` の追加は `ATOMIC_REQUESTS = False`（未設定デフォルト）では実質 no-op。明示的 `transaction.atomic()` ブロックの挙動は変更されない

VERDICT: HIGHRISK
```
