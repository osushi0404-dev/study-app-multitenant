---

# テストレビュー [I074]

## 指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 回帰防止 | fix テスト結果（item 4）は formal gate の正例（exit=0）のみ。TC-01 ゲートの失敗注入（テンプレートから `**背景**:` を除いたとき exit=1 になるか）は「計画時 G1」として事前実施済みであり、run_declared_gates の fail-closed コード（GATE_VERDICT=BLOCKER）と合わせて論理的には保証される。ただし fix 後のゲートセクション追加後に改めて失敗注入を実行した記録はない。実害は小さいが、「正例のみ確認」という点は記録として残す。 | `I134_fix_test_result_20260719_2239.md` item 4、`false-green 自己検証` G1（計画時前倒し） |
| Info | テスト十分性 | fix テスト結果の `verify_fix.sh`（`REVIEW_LIB_SOURCE_ONLY=1` で source 実行）はスクラッチパッドファイルで再現不可。ただし `REVIEW_LIB_SOURCE_ONLY=1` は `scripts/claude/tests/test_review_gates.sh` 等の既存テストが使用している正規パターン（l.20, l.166-172 で確認済み）であり、結果はコードロジックと整合する。再実行不可能な記録という点は Info レベル。 | `I134_fix_test_result_20260719_2239.md`、`test_review_gates.sh` |

## false-green 評価

**TC-01 formal gate（grep 2本）**
- `false-green 自己検証` G1（l.39）で「未実装テンプレートに対し grep 2 本 → 両方 exit 1」を事前実証済み。`run_declared_gates` は ALLOW コマンドが非ゼロ終了時に `GATE_VERDICT=BLOCKER` を設定する（`code-review.sh` l.195）。パターン自体は行頭アンカー `^\*\*背景\*\*:` で部分一致の偽陽性もない。false-green でないと判断できる。

**TC-02 / TC-02-inject（チェッカー検知能力）**
- 実施記録（l.52）に「ラベル無しダミー md → exit 1（`MISSING` 列挙）→ ダミー削除」と明記。失敗注入で NG を返すことを確認済み。false-green でない。

**omission_lint 回帰（fix テスト item 5）**
- 「宣言セクション外の単一コマンド bash fence → HIGH」を fix 後に明示的に確認。修正が omission_lint の検知能力を無力化していないことを決定論的に保証。false-green でない。

**TC-04/05/06（挿入のみ保証）**
- false-green 自己検証 G4/G5 で「削除入力 → exit 1」「行削除入り diff → 不合格」を事前実証。exit code の向き（削除=非ゼロ終了）と合否基準（合格=exit 0）が一致している。false-green でない。

VERDICT: OK
