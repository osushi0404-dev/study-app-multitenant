全4ファイルの読み込みが完了しました。前回レビュー（20260716_0142・Blocker 1件差し戻し）からの修正状況を含め、詳細にレビューします。

---

```
# 計画書レビュー [I113] ── 2026-07-16 10:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| — | — | 指摘なし | — | — |

**前回 Blocker の解消確認（差し戻し起因: resolve_status のサブシェル fail-open）**

前回指摘の修正内容を計画書 §4-1 で追跡確認した（差し戻し解消済み）:

1. `get_status()`: エラーメッセージを `>&2` にリダイレクト済み。`exit 1` → `return 1` に変更済み
2. `resolve_status()`: ループ内 `s=$(get_status) || return 1` でエラーを伝播
3. 両呼び出し箇所: `S=$(resolve_status) || { echo "⛔ ..."; exit 1; }` でガード済み
4. T15/T16 がこのシナリオを専用 TC として捕捉（fail-open 回帰防止テスト追加済み）

**前回 Warning の解消確認（TC-02 差し替え機構の未定義）**

§4-4 に `TARGET_SCRIPT="${TARGET_SCRIPT:-scripts/claude/pr-base-sync.sh}"` を明記済み ✅

**前回 Info の解消確認（PR番号表記の非対称）**

§4-3 に「PR番号は省略可（step 0 と同じ・カレントブランチの PR を自動特定。明示指定も可）」を明記済み ✅

---

**各観点ごとの確認結果（主要項目）**

**P1 要件適合性**: AC 7件全て計画内容に 1:1 対応。スコープ拡大なし。8値分岐・リトライ値・CI 待機値がイシュー確定内容と一致。問題なし。

**BP（Claude Code）**: SKILL.md への追加に分岐パリティを確認。step 0 sync 追加・step 5.5 final 追加の双方に `exit 0/1/2` 各ケースの進行指示が具体的コマンドレベルで対称記載されている。`TARGET_SCRIPT` 注入機構が明文化され TC-02 が実施可能。外部挙動依存（draft PR での BEHIND 取得・一覧照会の UNKNOWN）は計画確定前にスパイク実証済み（稼働中セッション実測）。`gh pr checks` 出力形式は code-review.sh 稼働実績で担保。決定論ゲートコマンドが単体呼び出しで複合化なし（コマンド衛生 OK）。設計メモ登場ファイルが実装対象に全件反映済み（P3 backstop OK）。

**P4 テスト妥当性**: T1〜T16 全分岐網羅。T15/T16 が前回 Blocker シナリオ（取得失敗 fail-open）の回帰防止として追加済み。T10 が I107 実証事象（BEHIND→追従→CI 全緑）を捕捉。TC-02 false-green 注入が DIRTY 分岐改変で T3 を NG にすることを設計。TC-03 の維持系 grep（`gh pr ready`・staged ガード文言）が無改変検証を担保。期待値が exit code＋副作用（git ログ・push 有無）の両方で具体的に定義されている。TDD Red（ベースライン）計画時実走済み記録あり。問題なし。

**P3/P5/P8/P9**: アプリコード・DB・インフラ変更なし。対象外。

**セキュリティ**: 認可変更なし・個人情報なし・新規依存パッケージなし。入力検証はモード（sync/final 2択）を明示 case で検証済み。PR番号は gh コマンドの位置引数として渡されるため注入リスク低。対象外。

---

**軽微な観察事項（Info・対応任意）**

- `ci_wait` の `grep -q "fail"/"pending"` は文字列部分一致。リポジトリにチェック名に "fail" を含むジョブが追加された場合に誤検知しうる（Risk 1 に文書化済み・code-review.sh と同一パターン）。
- final モードで BEHIND 追従後に ci_wait が成功→ループ先頭に戻って BLOCKED を得た場合、ci_wait が 2 回呼ばれる（2 回目は即時 return 0）。冗長だが無害。T10 の期待値は exit 0・push あり で合格する。
- CI チェックが存在しないリポジトリでは ci_wait が即時 return 0 になる（本リポジトリは CI 必須なので実害なし）。

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement [I113]` を実行してください。

VERDICT: OK
```
