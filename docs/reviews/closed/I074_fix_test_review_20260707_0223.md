確認完了。核心的な事実が判明しました。

---

# テストレビュー [I074]

## 指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| HIGH | テスト十分性 / false-green | **`fix-test-reviewer.md` の観点#5（NG 性質分類）が TC-28 でテストされていない**。IMPL_AGENT には `TC-28-impl-class` で `実行の不備\|方針の誤り` の存在を検証しているが、TEST_AGENT に対応する `TC-28-test-class` が存在しない。`fix-test-reviewer.md` には観点#5（`実行の不備`・`方針の誤り` の明示）が正しく実装されているにもかかわらず、この行を削除しても全テストが PASS するため、**壊れても通る false-green** になっている。fix-loop の (a)/(b) 分類駆動ルーティングはテストレビューの出力にも依存するため、この抜けは重要な回帰保護の欠落である。 | `scripts/claude/tests/test_fix_review.sh` L148–153 ／ `fix-test-reviewer.md` L30（観点#5） |
| Info | false-green | TC-29-decoy は SKILL.md の `前回NG` キーワード除去で TC-26-prior が NG になることを確認しているが、`fix-test-reviewer.md` の分類キーワード除去に対応する decoy がない（TC-28-test-class が追加されれば自然に対となる反証が必要になる）。TC-28-test-class 追加後に対応する decoy（TEST_AGENT から `実行の不備` を除去した複製で NG を確認）も付随して追加すること。 | `scripts/claude/tests/test_fix_review.sh` TC-29 周辺 |

## false-green 評価

**TC-28 の TEST_AGENT 側（`TC-28-test-class`）は false-green**。

具体的に言うと：
- `fix-test-reviewer.md` 観点#5 の `実行の不備`・`方針の誤り` キーワードを削除する → テストスイートは `pass=81 fail=0` のまま通過する
- テストレビュアーがこの分類を出力しなくなった場合、fix-loop の分類駆動ルーティング（(a)=手順5 vs (b)=手順2 環流）が テストゲート REMAND 時に判断根拠を失う
- IMPL_AGENT（3 チェック）と TEST_AGENT（2 チェック）の非対称構造が false-green を生んでいる

TC-07b/12/13/17/24/29 の各 decoy は正しく構造化されており、それぞれの false-green 反証として機能している（壊れた複製が実際に NG になることを確認）。
TC-27 のスクリプトへの `PRIOR` 引数追加検証はキーワード存在チェックだが、内容注入ロジックはシンプルな bash (`[ -f "$PRIOR" ]` + `$(cat "$PRIOR")`) で構造的に自明のため許容範囲内。

---

**NG の性質分類**: 本指摘は **(a) 実行の不備**（実装としての `fix-test-reviewer.md` 観点#5 は正しく存在する。テストの書き方・不足のみが問題）。承認済み方針（分類駆動ルーティング・TEST_AGENT への分類要求追加）に誤りはなく、`test_fix_review.sh` に `ck_grep TC-28-test-class "$TEST_AGENT" '実行の不備|方針の誤り'`（および対応 decoy）を追加するだけで解消する。

VERDICT: HIGH
