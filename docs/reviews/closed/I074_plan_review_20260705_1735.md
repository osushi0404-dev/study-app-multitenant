全ファイル読み取り完了。レビューを実施します。

---

```
# 計画書レビュー [I074] ── 2026-07-05 14:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP・コマンド衛生 | §4-4 手順5.5 の fail-safe 再判定説明に「機械補助＝`git diff --name-only \| wc -l`」とパイプ複合コマンドが例示されている。これが SKILL.md に反映されると Claude が Bash ツールでパイプ複合コマンドを実行→allowlist 不一致でプロンプト誘発（MEMORY 「Bashは複合化せず単体で実行」）。SKILL.md 実装時に「`git diff --name-only` で変更ファイル一覧を取得し行数は別途確認する」等の単体形に分解する旨を計画書に注記すべき。 | plan §4-4 手順5.5「機械補助＝`git diff --name-only \| wc -l`」 | SKILL.md 実装時はパイプを使わず単体コマンドに分解すること、を §4-4 の実装注意として明記する（修正推奨） |
| Warning | Claude Code BP（分岐パリティ） | 手順5.5 fail-safe 再判定の「合致→簡略化」の具体的動作が未定義。対をなす「外れ→`bash scripts/claude/fix-implementation-review.sh I###` でフルレビュー」は具体コマンドまで明記されているが、「合致」した場合に「スクリプト呼び出し省略のみで手順6へ直行」なのか「何らかの軽量チェックを実施」なのかが推測依存。文脈から「スクリプトを呼ばず手順6へ直行（SKIP 扱い）」と読めなくはないが、SKILL.md 実装者が迷わないよう「合致した場合：`fix-implementation-review.sh` の呼び出しを省略し手順6へ進む」と明示すると完全。 | plan §4-4 手順5.5「合致→簡略化、外れ→`bash scripts/claude/fix-implementation-review.sh I###`」 | 修正推奨（「合致した場合はスクリプト呼び出し省略のみで手順6へ直行」と一文追加） |
| Info | P4 テスト妥当性 | TC-13「ゲートが常時 0 の空振りでない」は独立した検証方法が記されていないが、TC-08/09/11（REMAND=exit1・PASS=exit0）と重複しており実質カバー済み。反証として整理するなら TC-11 と統合して明示すると readable。 | I074_auto_test.md TC-13 | 対応任意 |

### 前回 Blocker の解消確認（20260705_1724）
- **[解消済み] `<test_result_path>` 生成方法未定義**: §4-2 に「手順6.5 で Claude が手順6 のテスト/lint/scan 出力を `docs/reviews/I###_fix_test_result_<ts>.md` に保存したファイル」と明記。§4-4 手順6.5 に (a) サブステップ「Claude が手順6 で観測した出力を `docs/reviews/I###_fix_test_result_<ts>.md` に保存する（手順6 は再実行しない・非改変）」が追加。§3 影響範囲にも記載。✅

### 前回 Warning の対応確認
- **[対応済み] source ガード位置「lib 先頭」記述**: §4-1 に「全ヘルパー関数定義の後・本体実行コードの前」と正確に明記。✅
- **[対応済み] 連続 NG カウンタのリセット条件**: §4-4「カウンタは各ゲート × 各 fix-loop 実行で独立にカウントし、当該ゲートが PASS したら 0 にリセット。2 到達でエスカレーション＝ユーザー介入を挟むため次の fix-loop 実行では新規に 0 から数える」と明記。✅
- **[対応済み] TC-17 反証手順**: auto_test.md TC-17 に「書込ツール注入文字列に対し同 grep が一致（検出成功）することを確認」追記。✅
- **[対応済み] ハングタイムアウト R7**: plan §8 に R7「`claude -p` がハング → `timeout ${FIX_REVIEW_TIMEOUT:-300}` でラップし超過（exit 124）を SKIP に落として fix-loop を止めない」追加。§4-1 手順3・§11 にも詳述。✅
- **[対応済み] VERDICT 行不在→OK 問題（前回 Info）**: §4-1 `detect_fix_verdict` が「不在→空文字（判定不能）」設計に変更。TC-05 も空文字を期待値として明記。✅

### 観点別サマリー
- **P1 要件適合性**: 問題なし（AC 全項目が計画書にマッピング済み。「テスト結果の生成方法」も前回指摘解消で完備。仕様追加・逸脱なし）
- **ベストプラクティス (BP)**: Warning 2件（上記）。責務分離（lib ＋ 薄いエントリポイント）・source ガード位置・REVIEW_LIB_SOURCE_ONLY 規約・prompt-injection ガード・最小権限の設計は問題なし
- **セキュリティ**: 問題なし（`--tools "Read,Grep,Glob"` 最小権限・Write/Edit/Bash 除外 AC/TC-17 で回帰保証・prompt-injection ガード設計済み・新規 Web エンドポイントなし・OWASP 該当なし・DEFAULT_PERMISSION_CLASSES N/A）
- **モダンウェブ開発**: 対象外（UI/API なし）
- **Claude Code BP（指示ファイル）**: 問題なし（`allowed-tools` 最小権限・`disable-model-invocation: true` 既存・`argument-hint` 既存・分岐パリティは Warning で実装可能範囲・SKILL.md 500行以内・停止条件明記・設計メモ↔本文整合: fix-review-lib.sh/fix-{diagnosis,implementation,test}-review.sh/test_fix_review.sh/fix_test_result ファイルが全て §3 影響範囲に列挙済み）
- **P4 テスト妥当性**: 問題なし（false-green 反証 TC-12/13/17/24 を含む・decoy 注入 TC-06/07・回帰 TC-23/24・否定/不在系 TC-05/17/25・実装未着手のため plan フェーズでの false-green 反証は実装後検証が明記されている）
- **P3 データ整合性**: 対象外（DB・マイグレーション・API 変更なし）
- **P5 運用性**: 問題なし（タイムアウト R7 明記・監査証跡として SKIP も記録・PR コメントは任意・ループ暴走ガード=2回）
- **P8 コスト・保守負荷**: 問題なし（lib+薄いラッパーで DRY・連続 NG 上限=2 でトークン浪費防止・ロールバック容易）
- **P9 プライバシー**: 対象外（個人情報・テナントデータ非関与）
- **外部挙動依存の未実証**: 問題なし（`claude -p` パターンは既存 3 スクリプトで実証済みと §1 調査結果に明記・exit code ゲートの挙動も R6 で既存パターン実証済みを確認）
- **破壊的取り消し**: 問題なし（`git checkout/restore` の記述なし）

## 高リスク判定
判定: No
該当条件: なし（認証・認可変更なし・DB 変更なし・外部公開 API なし・指示ファイルのみ変更）
推奨: `/implement [I074]` を実行してください。

VERDICT: OK
```
