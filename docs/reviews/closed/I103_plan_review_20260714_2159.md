すべてのファイルを読み取りました。レビューを実施します。

---

```
# 計画書レビュー [I103] ── 2026-07-14 14:35

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `perform_create` コード例で `serializer.validated_data.get('subject')` + `if subject and ...` を使用しているが、subject の必須/オプション属性が計画書に明示されていない。subject が必須フィールドなら `.get()` は不要で `validated_data['subject']` による直接アクセスの方が明確・安全（subject=None 経路がデッドコードになることを計画書が保証していないため、実装者が "None ならスキップ可" と誤解するリスクがある）。なお subject=None のままでは ProblemService の `organization = subject.organization` でエラーになるため越境注入にはならないが、意図の明示性に欠く | `plan_I103.md` §4 実装コード例（perform_create） | 計画書に「subject は必須フィールドのため validated_data['subject'] で直接取得し、if 節は None ガードを省略する」旨を明記（または `.get()` + `None` スキップが安全な根拠を記載）。いずれにしても実装時に確認を推奨 |
| Warning | P4 テスト妥当性 | TC-AUTO-02a の「Problem 未作成」確認のアサーション具体性が不足している。TC-AUTO-01 は `Problem.objects.filter(question="新規問題").exists() is False` と具体的なのに対し、TC-AUTO-02a は「org B に AI 問題が増えない」という記述のみで、どのフィールド・フィルタ条件で確認するかが未記載 | `I103_auto_test.md` TC-AUTO-02a | 実装時に `Problem.objects.filter(subject=subject_b).count()` 前後比較など具体的なアサーションを追加 |
| Info | BP | `perform_create` コード例で `from rest_framework.exceptions import PermissionDenied` をメソッド内でローカル import している。Python はキャッシュするため実害はないが、Django/DRF 慣用ではファイル先頭での import が標準 | `plan_I103.md` §4 実装コード例（perform_create） | 実装時にファイル先頭 import へ移動することを推奨 |
| Info | BP | `auto_test.md` の fixture コード例で `return {...}` が省略されており、実装者が参照する際に具体的なキー名が不明。「`test_I102_problem_authz.py` に倣う」という説明はあるが、新規テストファイルの可読性のためにキー例の記載があると良い | `I103_auto_test.md` fixture 方針 | 対応任意。実装時に test_I102 を参照すれば進められる |

## 高リスク判定
判定: Yes
該当条件:
- 認証・認可・ロール変更（テナント境界の認可ロジック追加）
- マルチテナント境界変更（越境書き込み拒否の構造的追加）
- 画面制御していてもAPI直叩きで事故りうる変更（API直叩きでの越境を塞ぐ変更そのもの）
推奨: `/security-review I103` を実行してから `/implement I103` へ進んでください。

VERDICT: HIGHRISK
```
