# イシュー自己完結度レビュー [I081]

## 判定: 十分

重要セクションに未記入・テンプレート残存はなく、内容の論理的整合も確認できた。以下は助言レベルの指摘のみ。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 実装対象 | `danger-ops.md`（または common-commands.md）と「または」で配置先が未決定。どちらでも起票意図は通じるが、`plan-issue` フェーズで選択が必要になる。「git checkout/restore は破壊的操作 → danger-ops.md 優先」のような選好を一言添えると計画者の判断コストを下げられる | 実装対象テーブル 2 行目 |
| Info | スコープ・受け入れ条件 | AC4「I079 固有の技術名に依存しない一般形」は意図は明確だが、判定が評価者の主観に依存しやすい。「追記文言中に "I079"・"I080" 等のイシュー番号が含まれていないこと」のように**機械的に確認可能な言い換え**を添えると gate として扱いやすくなる | 受け入れ条件 AC4 |

---

## 次のアクションの推奨

指摘はすべて Info レベルのため、**このまま `/plan-issue` に進められる**。
上記2点は `plan-issue` 内で実装者が補足判断すれば十分（`/grill-me` はスキップ可）。
