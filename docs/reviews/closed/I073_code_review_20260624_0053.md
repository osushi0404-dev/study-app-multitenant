# コードレビュー [I073] ── 2026-06-24 10:30

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 既存問題に画像を追加でき、保存後の再取得で反映される | ✅ 実装済み | - | `_reconcile_images` step4/5 + TC-AUTO-01 |
| 2 | 既存画像を削除でき、共有されていない場合はストレージ実体も削除される（共有時は紐づけのみ解除） | ✅ 実装済み | - | step6の共有チェック + TC-AUTO-03/05/11 |
| 3 | 既存画像を差し替えでき（削除＋追加）、結果が正しく反映される | ✅ 実装済み | - | TC-AUTO-11 |
| 4 | 画像の並び順を変更でき、`position` が再採番されて表示順に反映される | ✅ 実装済み | - | delete→recreate 方式 + TC-AUTO-04 |
| 5 | 問題画像・解説画像それぞれで上記が機能する | ⚠️ 一部不足 | Low | TC-AUTO-10 が説明画像の「削除」「並び替え」を独立TCとしてカバーしていない（追加のみ） |
| 6 | 枚数上限（各5枚）を編集時も超過できない（超過時はバリデーションエラー） | ✅ 実装済み | - | `MAX_IMAGES_PER_KIND` 定数 + TC-AUTO-08 |
| 7 | 編集処理はトランザクションで、途中失敗時に部分更新が残らない | ✅ 実装済み | - | `@transaction.atomic` + 物理削除遅延 + TC-AUTO-07 |
| 8 | 他組織ユーザーは対象問題を編集できない（queryset スコープにより 404） | ✅ 実装済み | - | TC-AUTO-06 |
| 9 | 上記を検証する決定論テストが追加され全て PASS する | ⚠️ 一部不足 | Low | TC-AUTO-05（AC#5）の説明画像削除・並び替えTCが未作成（Low評価の理由は後述） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | モダンウェブアプリ / メモリリーク | `URL.createObjectURL(item.file)` が `getPreview()` 内（レンダー関数）で毎レンダー呼ばれ、`URL.revokeObjectURL()` が一切呼ばれない。編集ダイアログを複数回開閉したり画像を追加/削除する操作で Object URL がリークし続ける。修正案: `useEffect(() => { const urls = items.filter(i => i.kind==='new').map(i => URL.createObjectURL(i.file)); return () => urls.forEach(URL.revokeObjectURL); }, [items])` で URL を state に保持するか、`useMemo` でキャッシュする。 | `frontend/src/components/ImageUploadArea.tsx` L197-206 `getPreview()` | 修正推奨 |
| Medium | データ整合性 / エラーハンドリング | `order` 配列に同一の `existing` UUID が重複して含まれると、step5 の link recreate 時に `unique_together = ['problem','asset','usage_kind']` 違反で `IntegrityError` が発生し、ビューで捕捉されず HTTP 500 が返る。認証済みユーザが意図的に送ることでサービスの 500 ログを汚染できる。修正案: step3 の検証ループ内で `keep_asset_ids` への add の前に「同一 UUID が既に登場しているか」をチェックし、重複時は `ValidationError` (400) を raise する。 | `backend/problems/services/problem_service.py` `_reconcile_images()` step3（keep_asset_ids 収集ループ） | 修正推奨 |
| Low | ベストプラクティス | `order` エントリに `existing` と `new` が両方含まれる場合、step4 で `_save_image_as_asset` が呼ばれて孤児 MediaAsset と物理ファイルが生成されるが、step5 では `'existing' in entry` 側のブランチが使われるため孤児が生まれる。計画書の R2「残留オーソン」として許容されているが、入力バリデーション（step3）で「entry に both keys は不正」として 400 にできるとより明確。 | `problem_service.py` `_reconcile_images()` step3/4 | 改善提案（対応任意） |
| Low | テスト妥当性 | TC-AUTO-10 の実装は explanation 種別への「追加」と「problem 種別の不変確認」のみを検証しており、テスト文書の「TC-01/03/04 相当」（追加・削除・並び替え）の全範囲をカバーしていない。`_reconcile_images` は `usage_kind` に無関係な共通ロジックで、explanation のみが別コードパスを通る可能性は低いため Low とするが、差分テストの見落とし防止のため削除（空配列送信）と並び替えを explanation 種別でも追加テストすることを推奨。 | `backend/problems/tests/test_I073_image_update.py` `test_explanation_images()` | 改善提案（対応任意） |
| Low | ベストプラクティス | step6 の共有チェック `.exclude(problem=problem)` はコメントで「多重防御」と記載されているが、step5 で既に当該問題の link 行を全件 `link.delete()` で物理削除済みのため、exclude 条件がマッチするレコードは存在しない（完全に冗長）。将来の読者の混乱を防ぐためコメントを更新するか、サビアント削除の意図を明記する。 | `problem_service.py` `_reconcile_images()` step6 `shared` クエリ | 改善提案（対応任意） |
| Info | 運用性 | subject 変更時のキャッシュ無効化は「新 subject のキャッシュのみ」を対象としており、旧 subject のキャッシュが残る（旧 subject の問題一覧にレコードが残る可能性）。ただし、本 PR 以前の `perform_update`（`serializer.save()` 後に同じ `instance.subject.id`）も同一の挙動であり、**本 PR による回帰ではなく既存の系統的問題**。本 PR のレビュースコープを超えるため Low/Info として記録し、フォローアップイシューで対処を推奨。 | `backend/problems/views.py` L337 `cache_service.invalidate_problems_cache(instance.subject.id)` | 情報提供（既存の挙動） |

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test [I073]` を実行してください。

VERDICT: OK
