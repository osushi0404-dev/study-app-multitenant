# イシュー自己完結度レビュー [I084]

## 判定: 十分

重要セクション（問題・根本原因・解決方針・実装対象・スコープ・受け入れ条件）にいずれも空欄・テンプレート残存はない。設計上の未確定点（抽出方式(a)/(b)）は「grill-me で確定」と意図的に宣言されており、欠落ではなくコントロールされた未決定として適切に扱われている。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | スコープ | 「抽出方式の確定（grill-me）」がスコープの「含む」に並列列挙されているため、「grill-me は plan-issue **前**に完了している前提」なのか「plan-issue/implement 工程内で確定する」のかが担当者に若干曖昧になり得る。AC6 が「grill-me で確定した方式が実装と一致」という事後確認になっているため、前者（事前完了前提）と読むのが自然だが明示されていない | スコープ「含む」3項目目・AC6 |
| Info | 実装対象 | `REVIEW_LIB_SOURCE_ONLY=1` という環境変数名が実装対象表に突如登場するが、その設計根拠（source-only テストパターンの前例・由来）はイシュー内に説明がない。plan-issue 段階で設計すれば足りるレベルではあるが、文脈ゼロの担当者には初見でやや不明瞭 | 実装対象 `scripts/claude/tests/` 行 |
| Info | 受け入れ条件 | 「破壊的コマンドは実走対象外」という重要な設計制約が「制約・引き継ぎ情報」にのみ記載されており、AC には検証可能な条件として含まれていない。実装の安全境界に関わるため「実走対象は副作用なし系（grep/json.tool/tests/*.sh）に限り、破壊系は実走しないことを単体テストで確認」等をACに追記するとより堅牢 | 制約・引き継ぎ情報 3項目目 / 受け入れ条件 |

---

## 次のアクションの推奨

判定は「十分」。ただし AC6 と実装方針が grill-me の結果に依存しているため、**grill-me を先に完了させてから `/plan-issue` に進む**ことを推奨。grill-me 完了後であればそのまま着手可能。

上記 Info 指摘（特に「破壊的コマンドは実走対象外」のAC化）は起票をブロックしないが、plan-issue 工程で AC に取り込む or 設計上明示することを推奨。
