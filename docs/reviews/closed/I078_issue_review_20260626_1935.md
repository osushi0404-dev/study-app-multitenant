# イシュー自己完結度レビュー [I078]

## 判定: 十分

重要セクション（現状の問題・根本原因・解決方針・実装対象・スコープ・受け入れ条件）がすべて記入されており、内容に重大な論理矛盾はない。文脈ゼロの担当者が `/plan-issue` に着手できる水準に達している。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | スコープ / 自己完結度 | 「管理経路で `is_correct` を返す」分離の安全性は、管理用 ViewSet に管理者専用の認証・認可が既存で掛かっていることが前提。イシュー本文に「既存の認可機構はそのまま維持する（変更なし）」と一言添えると計画者の確認漏れ防止になる | 解決方針・制約引き継ぎ情報 |
| Info | 実装対象の妥当性 | `ProblemDisplaySerializer` を「等」で省略しているが、出題経路に紐付く ViewSet / シリアライザが複数ある可能性がある。計画時に実在確認と紐付けの特定が必要なことを `備考` に書いておくと計画書作成がスムーズ（計画書の仕事ではあるが先手として） | 実装対象テーブル |
| Info | 実装対象の妥当性 | `frontend/src/services/types.ts` の変更を「必要に応じ」と条件付きにしているが、フロントが `is_correct` を参照プレビュー・編集ダイアログで使う以上、型定義の追加は実質的に必須。「必要に応じ」という表記が計画者に判断コストを生む | 実装対象テーブル |

---

## 次のアクションの推奨

**`/grill-me` をスキップして `/plan-issue` に進める。**

指摘はいずれも `Info` 級の助言であり、計画書作成段階でソースを確認すれば解消できる。認可機構の現状確認と `ProblemDisplaySerializer` の実在確認を `/plan-issue` の調査ステップに含めるとよい。
