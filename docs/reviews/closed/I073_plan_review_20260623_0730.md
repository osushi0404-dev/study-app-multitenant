全4ファイルを読み取り完了。前回レビュー（Blocker 1件差し戻し）の是正状況と新規問題を中心にレビューを実施する。

---

```
# 計画書レビュー [I073] ── 2026-06-23 15:45

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| 問題なし | P1 要件適合性 | 受け入れ条件との完全一致を確認。multipart 契約・上下ボタン・IsAuthenticated+組織スコープはすべてイシュー確定値に準拠。`order is None`=無変更/`[]`=全削除の二値仕様は「仮定（要確認）」として承認ポイントに明示されている。 | 計画書 §2/§9 | — |
| 問題なし | セキュリティ（前回Blocker是正確認） | **前回Blocker（物理削除先行問題）が解消されている**。`_reconcile_images` の実行順序が「④新規ファイルを先に保存→⑤link 再構築→⑥最後に物理削除」に変更されており、不正ファイルのバリデーション失敗はステップ4で例外を上げてステップ6（物理削除）に到達せず終了する。TC-AUTO-07の期待結果「物理ファイル2個が全て元のまま」は新しい実行順序で実現可能。前回レビューの修正指示内容と完全に一致。 | 計画書 §5 `_reconcile_images` ステップ4→5→6、§10、`I073_auto_test.md` TC-AUTO-07 | 是正確認済み |
| Warning | P4 テスト妥当性 | **TC-AUTO-10 の期待結果が参照渡し（「TC-01/03/04 相当」）で具体値の記載がない**。"explanation の link が期待通り" は "正常に動作する" と同義であり、実装者がどの assert を書くか迷う可能性がある。TC-01=2件・pos1=既存A/pos2=新B、TC-03=link 0件/is_deleted=True×2/物理削除済み、TC-04=C=1/A=2/B=3 のように explanation 向けの具体値を添えることを推奨。 | `docs/tests/open/I073_auto_test.md` TC-AUTO-10「各 200。explanation の link が期待通り」 | 修正推奨 |
| Warning | P4 テスト妥当性 | **計画書 §5 ステップ3 の TC 範囲記述が古い**。"TC-AUTO-01〜09 を pytest スタイルで実装" となっているが、テスト文書・承認ポイントでは TC-AUTO-00〜11（12件）が正とされている。実装時に TC-AUTO-00（回帰）・TC-AUTO-10（解説画像）・TC-AUTO-11（差し替え）が見落とされるリスクがある。 | 計画書 §5 ステップ3 "TC-AUTO-01〜09" | 修正推奨 |
| Warning | P4 テスト妥当性 | **TC-AUTO-11（差し替え）が false-green 自己検証テーブルに含まれていない**。差し替えは "B が is_deleted=True かつ物理削除" という負の不在を検証する。故障注入（共有チェックを無効化、または物理削除ステップを削除）での FAIL 確認を false-green テーブルに追記することを推奨。TC-AUTO-03（全削除）の不在も同様だが、こちらは正の assertion（削除が起きる）のため優先度は低い。 | `docs/tests/open/I073_auto_test.md` 否定・回帰系 false-green テーブル | 修正推奨 |
| Info | P5 運用性 | 物理削除失敗ログに `user_id`・`request_id` が含まれていない。前回 Warning より改善（`problem_id`/`organization_id`/`asset_id`/`storage_key`/`error` が追加済み）。`organization_id` が事実上 tenant_id を兼ねるため追跡可能ではあるが、`request_id` によるリクエスト単位の追跡がないと並行リクエスト時の障害切り分けが難しい。Django ミドルウェアでリクエストIDを付与できる場合は extra に追加を推奨。 | 計画書 §5 `_reconcile_images` ステップ6 logger.warning、R7 | 改善提案（対応は任意） |
| Info | BP（定数） | 枚数上限「5」が `_reconcile_images` 内のコードで直接 `> 5` として使われる想定。計画書本文には「既存定数方針に合わせる」とあるが、参照する定数名（例: `MAX_IMAGES_PER_KIND`）が明記されていない。実装時に新規マジックナンバーを作らないよう注意。 | 計画書 §5 `_reconcile_images` ステップ1、§設計品質「ハードコード回避」 | 改善提案（対応は任意） |

## 高リスク判定
判定: Yes
該当条件:
- ファイルアップロード/ダウンロード（画像ファイルの保存・物理削除を伴う）
- 外部公開API追加・変更（既存 PUT `/api/problems/{id}/` を multipart 対応に変更）
推奨: `/security-review I073` を実行してから `/implement I073` へ進んでください。

VERDICT: HIGHRISK
```
