# 計画書レビュー [I055] ── 2026-04-30 11:00

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `pip-audit==2.10.0` は計画書ステップ2A-1で具体バージョンが記載されているが、受け入れ条件（3節）の「`backend/.bandit`（YAML）に集約」という記述と、ステップ3の「INI 形式」「自動検出」という記述が依然矛盾している。前回レビューで Warning として指摘済みだが、計画書本文がまだ修正されていない | plan_I055.md ステップ3・3節受け入れ条件 | 修正推奨（実装前に統一） |
| Warning | テスト妥当性 | `I055_auto_test.md` TC-05 の期待値「`backend/.bandit` に `[bandit]` セクションがあり `skips = B101` が含まれる」と手動テスト No.3「`grep -A 5 "[bandit]" backend/.bandit`」は計画書のINI形式と一致しており、3節受け入れ条件の「YAML」表記のみが不整合。前回 Warning として指摘済みだが計画書が未修正 | plan_I055.md 3節受け入れ条件 | 修正推奨 |
| Warning | BP | ステップ8の CI YAML サンプルで `frontend-lint` ジョブに `pre-commit/action` の記述がなく、ESLint の CI 実行経路が不明瞭。`backend-lint` は `pre-commit/action` 経由で実行されるが、`frontend-lint` は `npm audit` ステップのみ記載されており、ESLint が CI でどのジョブ・どのステップで実行されるかが計画書から読み取れない。前回 Info として記録されていたが、実装時の設計根拠として具体化が必要 | plan_I055.md ステップ8 | 修正推奨 |
| Info | セキュリティ | detect-secrets ベースライン再生成時に既存除外済み検出が再審査対象になる場合の対処方針が未記載（前回 Info として継続指摘） | plan_I055.md ステップ7 | 改善提案 |
| Info | モダン | ステップ6の Ruff pre-commit フック設定で `rev: v0.x.x` というプレースホルダが残っている。ステップ2A-1 で `ruff==0.15.12` とバージョンが具体化されているため、フック設定の `rev` も `v0.15.12` に揃えることを推奨 | plan_I055.md ステップ6 | 改善提案 |

## 高リスク判定

判定: No
該当条件: なし
推奨: `/implement I055` を実行してください。

---

**レビューサマリー**

- Blocker 件数: 0 件（差し戻しなし）
- Warning 件数: 3 件（次工程を止めないが、実装前に解消推奨）
- 主な Warning は前回レビューから継続する指摘（bandit 設定の「YAML」vs「INI」表記不一致・ESLint CI 実行経路の不明瞭さ）。Blocker は 0 件のため `/implement I055` に進んでよい。実装時に設計の一方に統一し、テストケースと計画書の整合を取ること。
