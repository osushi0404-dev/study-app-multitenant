# イシュー自己完結度レビュー [I113]

## 判定: 十分

重要セクションに未記入の空欄はなく、内容に重大な論理矛盾もない。実証事象・根本原因・解決方針・実装対象・受け入れ条件がすべて記入されており、文脈ゼロの担当者が `/plan-issue` に着手できる水準に達している。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 解決方針 | `BLOCKED` を「承認待ちのみ → 続行」と定義しているが、GitHub API の `mergeStateStatus=BLOCKED` はレビュー未承認以外（必須 CI 未通過・ブランチ保護の他条件）でも立つ場合がある。「承認待ちのみ」という読み方が plan で引き継がれると分岐漏れになる可能性がある。「全値の分岐は plan で確定する」と明示済みのため即時問題ではないが、plan 作成者への補足として認識しておくとよい | 解決方針 `mergeStateStatus の他の値の扱い` |
| Info | 自己完結度 | `gh pr checks` の pending 待機ループの具体的な待機間隔・タイムアウト上限はイシュー内に記載がない（参考実装 `code-review.sh` を参照する旨の誘導はある）。plan フェーズで同スクリプトを参照すれば補完できるため、イシュー単体では軽微な不足にとどまる | 解決方針・参考資料 |

---

## 次のアクションの推奨

判定は **十分**。`/grill-me` をスキップして `/plan-issue` に直接進められる。

plan 着手時の留意点として、`BLOCKED` の定義確認（GitHub docs の mergeStateStatus 値一覧との照合）と `gh pr checks` 待機パラメータ（間隔・タイムアウト）の `code-review.sh` からの流用を早めに確定することを推奨する。
