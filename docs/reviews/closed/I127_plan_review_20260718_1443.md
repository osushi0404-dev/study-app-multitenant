4 ファイル全部読了。レビューを開始します。

---

```
# 計画書レビュー [I127] ── 2026-07-18 今日

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | テスト妥当性 | FE テスト（TC-FE-01/02）がインターセプタの内部ハンドラ（`interceptors.response.handlers[0].rejected`）を直接呼び出す実装詳細依存方式。api.ts のリファクタリング時にインターフェースが変わると TC が追従必要。計画書の承認ポイントに「axios モックライブラリ未導入のため」と合理的理由が明記されており受け入れ可能だが、将来の保守負荷として記録。 | `docs/tests/open/I127_auto_test.md` TC-FE-01/02・plan §6 | 修正推奨（ただし現状の制約下での合理的選択であり Blocker ではない） |
| Warning | 運用性 | 手動テスト No.1 の「61 回実行（ループ）」の具体的なシェルコマンド（`for i in $(seq 61); do ... done` 等）が手順に明記されていない。Claude 実施なので実行時決定可能だが、文書上の曖昧さが残る。 | `docs/tests/open/I127_manual_test.md` No.1 | 修正推奨（`for i in $(seq 60); do curl ...; done` 形式で明記） |
| Info | 要件適合性 | BE 429 統一 JSON の `sub_message`（「しばらく時間をおいて再度お試しください」）と FE トースト文言（「しばらく待ってから再試行してください」）が微妙に異なる。機能的問題なし（ユーザーが見るのは FE トーストのみ・BE JSON は API クライアント向け）。意図的な差異であれば問題なし。 | plan §4 変更点2・変更点5 | 改善提案（対応任意） |
| Info | セキュリティ | 未認証 anon スロットルは IP 単位のため、同一 NAT 環境の複数ユーザーが 60/min バケットを共有する known limitation が計画書・イシューで言及なし。本イシューの目的（大量収集の減速・Low リスク）対比では許容範囲だが、将来のレート値調整時の判断材料として記録。 | plan §8 Risk リスト | 改善提案（対応任意・I129 での議論候補） |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I127` を実行してください。

---

VERDICT: OK
```
