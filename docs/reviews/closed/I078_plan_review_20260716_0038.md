すべてのファイルを読み終わりました。レビューを開始します。

---

```
# 計画書レビュー [I078] ── 2026-07-16 16:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | false-green 注入の復元方法が「Edit で戻す」と明示されていない。auto_test の false-green 自己検証では「注入・復元は作業ツリー上の一時変更で行い」とのみ記載されており、`git restore <file>` で戻す可能性が排除されていない。実装差分が未コミットの状態で `git restore serializers.py` 等を実行すると注入と実装差分が共に失われるリスクがある。「復元は Edit ツールで元に戻すこと」を明記することを推奨。 | `docs/tests/open/I078_auto_test.md`「false-green 自己検証」セクション | 修正推奨 |
| Warning | P4 | TC-AUTO-05 / TC-AUTO-06 に必要な `QuizSession`（normal・`is_active=True`）と `QuizAnswer`（`selected_choices` あり）の ORM 作成コードが fixture コードスニペットに含まれていない。fixture 方針テキストには記載があるが、実装コード例が不完全なため、実装時に漏れるリスクがある（特に `QuizSession` の `subject` 紐づけ・`is_active` の設定が暗黙になっている）。コードスニペットへの追記、または TC 内でインライン生成する旨を明示することを推奨。 | `docs/tests/open/I078_auto_test.md` fixture コードスニペット・TC-AUTO-05/06 | 修正推奨 |
| Info | モダン | 手動テスト No.8「`i102_user@example.com` でログイン→クイズ出題画面で**同科目**の問題を開く」の「同科目」が未特定。cute_school 組織内で admin が管理する科目名・`/quiz/` 経由の具体的 URL ないし操作ステップを明示すると再現性が高まる。 | `docs/tests/open/I078_manual_test.md` No.8 | 改善提案 |
| Info | BP | `ChoiceAdminSerializer` が `ChoiceSerializer` を継承せず `serializers.ModelSerializer` を直接継承している（意図的・「既存クラス不変更」方針に基づく）。将来 `ChoiceSerializer` に新規フィールドが追加された場合、`ChoiceAdminSerializer` への反映が漏れる二重保守リスクがある。doc コメントに「`ChoiceSerializer` の field 変更時は本クラスも合わせて更新すること」の旨を明記しておくと将来の漏れを防げる。 | `docs/plans/open/plan_I078.md` セクション4・コード例 `ChoiceAdminSerializer` | 改善提案 |

## 高リスク判定
判定: Yes
該当条件:
- 「画面制御していても API 直叩きで事故りうる変更」（シリアライザ配線を誤ると学生に正解が漏れる情報露出軸）
- 計画書自身も「高リスク判定: Yes」と明記・C2（プライバシー/情報露出）に該当

推奨: `/security-review I078` を実行してから `/implement I078` へ進んでください。

---

VERDICT: HIGHRISK
```
