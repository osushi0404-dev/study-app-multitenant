4ファイルをすべて読み込みました。レビューを実施します。

---

```
# 計画書レビュー [I083] ── 2026-07-01 14:10

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `rung` ヘルパー（false-green 注入で壊した版フックを実行するヘルパー）が TC-FG-G/H/I/J で参照されているが、§4-2 で追加すべきヘルパーとして明記されていない。`cka` は明示追加されているが `rung` は言及なし。既存テストスクリプトに存在しない場合、実装者が TC-FG を実装できない。 | `I083_auto_test.md` §I / `plan_I083.md` §4-2 | 修正推奨: §4-2 に「`rung`（壊したフックのコピーを実行するヘルパー）を追加する」旨を明記するか、TC-FG の注記内に実装方針を追記 |
| Warning | P4 | `plan_I083.md` §5 ステップ4 および §8 Risk 表が「TC-DOC1」を参照しているが、`I083_auto_test.md` に TC-DOC1 が定義されていない（手動テスト No.4 が対応するが、自動テスト文書には存在しない）。実装者が TC-DOC1 を探して見つけられないクロスリファレンス不整合がある。 | `plan_I083.md` §5/§8 vs `I083_auto_test.md` vs `I083_manual_test.md` No.4 | 修正推奨: `I083_auto_test.md` の「完了条件」に TC-DOC1 = 手動テスト No.4 で代替する旨を明記するか、計画書の「TC-DOC1」参照を「手動テスト No.4」に統一 |
| Info | P4 | false-green 注入 TC-FG-J は "構造的ラッパー分岐を削除" のテスト入力として `eval "git push origin develop"` を使用しており、`eval` サブ分岐を裏取りする。しかし `git -c alias.p=...` サブ分岐には専用の注入テストがなく、alias 検出の false-green は TC-D4（正例テスト）のみで担保される。alias 分岐のバグは TC-D4 が NG になることで検出されるが、「TC-D4 自体が false-green でないこと」の注入裏取りは存在しない。 | `I083_auto_test.md` §I TC-FG-J / §A TC-D4 | 改善提案: TC-FG-J に加えて「alias サブ分岐削除」の注入 TC を追加すると alias 検出が load-bearing であることの裏取りが完全になる（任意） |
| Info | BP | `_is_force_flag` の単一ダッシュ短縮クラスタ判定 `"f" in tok[1:]` は、git push の文脈で実在しないフラグ（例: `-rf`）も force と判定して exit 2 block する。`-rf` を含むコマンドはそもそも有効な `git push` ではないため実害は薄いが、クラスタ内の `f` を広く拾う点は設計の既知の保守上の注意点として文書化しておくと後続メンテが容易。 | `plan_I083.md` §4-1 `_is_force_flag` | 改善提案: コード内コメントに「単一ダッシュで `f` を含む全クラスタを force として扱う（例: `-rf` も対象）」と明記（任意） |

## 高リスク判定
判定: No
該当条件: なし（本件はハーネス安全機構の堅牢化。Django/React のアプリコード・DB・認証・個人情報・マルチテナント境界変更なし）
推奨: `/implement I083` を実行してください。

VERDICT: OK
```
