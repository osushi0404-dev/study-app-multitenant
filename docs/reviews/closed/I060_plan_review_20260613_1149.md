実体確認が完了しました。レビュー結果を出力します。

---

```
# 計画書レビュー [I060] ── 2026-06-13 （D16 追加改訂版）

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | テスト妥当性 | TC-06 は retro 行から「任意/optional」の**消去**を検証するが、「必須」という語の**存在**を正に確認しない。「推奨」「原則実施」等の表現で修正されても pass してしまう。補足欄で workflow.md との目視比較を推奨しているが、自動 pass 条件が狭い。 | I060_auto_test.md TC-06 | 修正推奨: `grep -c "retro.*必須" docs/runbooks/issue-flow.md` を OR で追加するか、補足に「pass 後は必ず目視確認すること」を明記 |
| Warning | テスト妥当性 | TC-13 が `review-rules.md` を意図的に除外している理由がテストファイル内に記載されていない。テストを単独で読むと除外の根拠が不明。 | I060_auto_test.md TC-13 補足 | 修正推奨: 補足欄に「review-rules.md は旧体系再編の別イシュー対象のため除外（plan_I060 §3 参照）」を1行追記 |
| Info | テスト妥当性 | TC-13 の除外フィルタ `grep -v '"status"'` は `"status"` を含む行を広く除外する。将来 `"status"` を含む行に不在ディレクトリ参照が混入しても検知されない。現状は問題ないが grep パターンを `grep -v '"status".*in_progress'` に限定するとより堅牢。 | I060_auto_test.md TC-13 | 改善提案（対応は任意） |
| Info | 実体整合 | 実体確認の結果、CLAUDE.md（D1・D2）/ pre-commit.md（D5）/ issue-flow.md（D6/D7/D8）/ common-commands.md（D10）/ onboarding.md（D13/D14）はすでに実装済み。残作業は主に D16（workflow.md:17,20 / issue-flow.md:18,39,187,451 / onboarding.md:105）と D9/D3/D4/D11/D12/D15 の未完部分のみ。計画書に「既実装項目」の明示がないため実装者が再実行しないよう注意が必要。 | plan_I060 §6・§7 | 改善提案: 実装完了済みの D 番号を §6 or §7 で「✅ 実装済み」マーク付きにすると混乱を防げる（対応は任意） |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I060` を実行してください。
```

---

**補足メモ（レビュアー所見）**

D16 追加の妥当性について実体を確認した結果:
- `workflow.md:17,20`・`issue-flow.md:18,39,187,451`・`onboarding.md:105` には今なお `in_progress` ディレクトリ参照が残存しており、D16 の修正対象として正確
- `workflow.md:140,151` の `"status": "in_progress"`（TodoWrite 値）は存在し、計画書どおり「触らない」ことが確認できた
- `review-rules.md:282,285` の `in_progress` は旧体系（`docs/reviews/in_progress/`）であり、別イシュー切り出しの判断は妥当

計画書全体の品質（乖離一覧 D1〜D16 の具体性・行番号明示・Risk & 回避策・設計判断の明示）は高く、Blocker 相当の記載不備はなし。
