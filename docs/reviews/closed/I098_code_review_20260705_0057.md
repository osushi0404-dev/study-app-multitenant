## 決定論ゲート実行結果（code-review.sh 自動注入）

- `bash scripts/claude/tests/test_next_issue_num.sh` → exit=0 ✅

- omission-lint: HIGH
- FINAL VERDICT（gate/omission/LLM 合成）: HIGH

---

# コードレビュー [I098] ── [2026-07-05 19:30]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 複数 worktree に未コミットのイシューが存在しても採番が全 worktree を通じて一意になる | ✅ 実装済み | - | `git worktree list` 横断スキャンで untracked 含む全 FS 最大を取得。TC-N1 で検証。`bash scripts/claude/tests/test_next_issue_num.sh` → exit=0 ✅ |
| 2 | primary の untracked I085〜I091 が wt-harness から不可視ケースが回避される | ✅ 実装済み | - | TC-N1 が同等ケース（primary 側 I050 untracked ＋ linked 側 I030）で再現・検証。gate exit=0 ✅ |
| 3 | `scripts/claude/next-issue-num.sh` が新規作成され全 worktree 横断＋`git log --all` で max+1 を算出 | ✅ 実装済み | - | `worktrees()` 関数で `git worktree list --porcelain` を列挙し絶対パスで各 worktree の docs/issues を走査、`git log -- ':/docs/issues'` で CWD 非依存の履歴最大を算出。gate exit=0 ✅ |
| 4 | 成功時は 3 桁ゼロ詰めの番号のみを stdout に出力（`ISSUE_NUM=$(...)` 契約が成立） | ✅ 実装済み | - | `printf '%03d\n'` で出力。TC-N5 で `^[0-9]{3}$` 1行のみ・余分行なし・rc=0 を検証。gate exit=0 ✅ |
| 5 | CWD 非依存：どのサブディレクトリ/worktree から実行しても同一結果 | ✅ 実装済み | - | git 履歴 pathspec を `:/docs/issues`（マジック接頭辞）に固定、FS 走査は worktree 絶対パス使用。TC-N6 で回帰検出・反証済み。gate exit=0 ✅ |
| 6 | `issue-flow.md` 3 箇所と `SKILL.md` 1 箇所の計 4 箇所の採番 bash が `next-issue-num.sh` 呼び出しに置換 | ✅ 実装済み | - | diff および実ファイル Grep で確認済み。TC-DOC1・DOC2 で `FS_MAX=`/`GIT_MAX=` 不在を grep 検証。gate exit=0 ✅ |
| 7 | `worktree.md` §8 が「任意 worktree でスクリプト実行＝権威単一化」に更新され旧人手規律が撤廃 | ✅ 実装済み | - | §8 の書き換えを diff・実ファイルで確認。TC-DOC3 で `next-issue-num.sh`・「権威」出現と旧「すみやかに develop へ取り込」不在を検証。gate exit=0 ✅ |
| 8 | 他 worktree の読み取りが I095 の読み取り許可方針と衝突しない | ✅ 実装済み | - | `find`・`git log` の read-only のみ。書込命令なし。I095（書込のみブロック）に抵触しない |
| 9 | `test_next_issue_num.sh` が再現ケース＋I094 decoy 反証＋CWD 非依存回帰を検証し全 PASS | ✅ 実装済み | - | TC-N1〜N7・TC-DOC1〜4 の 21 アサーション実装済み。`bash scripts/claude/tests/test_next_issue_num.sh` → exit=0 ✅ |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| **High** | ベストプラクティス（コマンド衛生・omission-lint） | `docs/tests/open/I098_auto_test.md` の先頭 "実行コマンド:" セクション（l.6-8）が fenced `bash` ブロックで `bash scripts/claude/tests/test_next_issue_num.sh` を記述しており、`## 決定論ゲート（自動実走）` セクションの外側に位置する。`code-review.sh` の `omission_lint()` 関数はゲートセクション外の fenced bash ブロックに allowlist パターンを発見すると HIGH を返す設計であり（実装確認済み）、pre-injected で `omission-lint: HIGH` が実証されている。同一コマンドがゲートセクション（l.37-39）にも存在し実走されているため機能的欠陥ではないが、以降の `/code-review` 実行で常に `omission-lint: HIGH` が注入される永続的ノイズになる | `docs/tests/open/I098_auto_test.md` l.5-8 のフェンスブロック | 先頭 fenced bash ブロックを `` `bash scripts/claude/tests/test_next_issue_num.sh` `` インラインまたはプレーンテキストに書き直してフェンスブロックを除去する |
| Low | ベストプラクティス | `next-issue-num.sh` l.7 のコメントが `docs/plans/closed/plan_I098.md` を参照しているが、計画書は現在 `docs/plans/open/plan_I098.md` に存在する。PR マージ後のクローズ手順（closed 移動）で整合するが、PR 時点では stale なパス参照 | `scripts/claude/next-issue-num.sh` l.7 | クローズ手順実施時に自然整合。急ぎであれば `open/` に修正 |
| Low | ベストプラクティス（規約・消費箇所網羅性） | `docs/plans/open/plan_I031.md`（open 状態・別イシューのアクティブ計画書）に旧採番 bash（`FS_MAX=$(find docs/issues...`）が残存している。TC-DOC4 の grep 検証対象は ISSUE_FLOW・SKILL・WORKTREE_MD の 3 ファイルに限定されており、plan_I031.md は対象外。実行パスではなく実際の採番では更新後の SKILL.md が参照されるため機能的影響はないが、I031 作業者が旧手順に倣うリスクが微存在する | `docs/plans/open/plan_I031.md` l.86 | 本 PR スコープ外として許容（別イシューで対応）。plan_I031 の次回参照時にコメント追記等で注意喚起 |

## 高リスク判定
判定: No
該当条件: なし

---
High のみ: 「`/fix-loop [I098]` を実行してください。fix-loop 完了後は `/code-review [I098]` に戻ってください。」

VERDICT: HIGH
