差分を詳細に確認しました。レビューを実施します。

---

# コードレビュー [I067] ── [2026-06-18 19:30]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `plan-issue/SKILL.md` に「イシューが既にベースへコミット済みの場合」の分岐が明記され、その手順どおりに進めれば `gh pr create` がエラーにならない（invariant も併記） | ✅ 実装済み | - | `ISSUE_ALREADY_ON_BASE` 自動判定・invariant 追記・`git push -u origin feature/...` も明示済み（前回 Medium 指摘対応済み） |
| 2 | `test/SKILL.md` に「計画書（auto_test.md）が指定する自動テストを正とし、指定が無い場合のみ既定 pytest/Jest/E2E にフォールバックする」分岐が明記され、`description` も更新される | ✅ 実装済み | - | 計画駆動節・description 更新（plan-specified）・フォールバック見出し・停止条件への計画書指定テスト失敗トリガー追加・重複注記すべて対応済み |
| 3 | `retro/SKILL.md` に「バックログ issue ファイルはローカル未コミットのまま残す（ベースへコミットしない）」ハンドオフ invariant が明記される | ✅ 実装済み | - | step 4 のコードブロック直後に invariant を追記済み |
| 4 | `close/SKILL.md` step 3 の staging が close 対象ファイルに限定され、未コミットのバックログ issue ファイルを巻き込まないことが明記される（`git add -A`/`.`/`docs` 禁止） | ✅ 実装済み | - | `git add -u`＋決定論ゲート（番号アンカー `I${ISSUE_NUM}([^0-9]\|$)` で `I0670` 誤マッチ防止）＋broad add 禁止注記すべて追記 |
| 5 | I062/I067 と同等の状況（イシュー既コミット・非app変更）を分岐文言に当てはめて規定どおり完走できることを文書レビュー（手動テスト No.1）で確認 | ⚠️ 一部不足 | Low | `/code-review` 時点では manual_test.md No.1〜4 の実結果欄が空白。フロー順序 `/implement → /code-review → /test` の正常状態。TC-01〜TC-08 は PASS 記録済み（auto_test.md） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス | `test/SKILL.md` において、計画書指定テストを実行した後の「記録」（docs/reviews・docs/tests への記録）と「手動テスト実施」（4)〜5)）が計画駆動経路に明示されていない。既定テストのフォールバック節に 4)〜5) として書かれているが、計画駆動テストを正として実行した場合も同ステップを踏むことが暗黙的。運用者が文脈補完に頼る必要がある | `.claude/skills/test/SKILL.md` 「自動テストの選択（計画駆動）」節 | 計画駆動節末尾に「実行後は手順 4) 以降（記録・手動テスト）を続ける」旨を一文追記することで明確化可能。現状は Low で実害リスクは低い |
| Low | ベストプラクティス | `plan-issue/SKILL.md` の git log 判定コマンドに `docs/issues/open/${ISSUE_NUM}.md`（`I` プレフィックスなし）のパスが列挙されている。現行命名規則では `I${ISSUE_NUM}.md` が正でありプレフィックスなしパスは実在しない冗長エントリ | `.claude/skills/plan-issue/SKILL.md` step 2 の git log コマンド | 前回レビューで「保持」（close/issue-bootstrap の旧 `###.md` 形式サポートと対称・許容）として記録済み。新たな問題はなし |

（Blocker・High なし）

## 高リスク判定
判定: No
該当条件: なし

---

✅ コードレビュー完了。`/test [I067]` を実行してください。

VERDICT: OK
