変更ファイルの内容を確認できました。レビューを実施します。

---

# コードレビュー [I067] ── [2026-06-18 20:30]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `plan-issue/SKILL.md` に「イシューが既にベースへコミット済みの場合」の分岐が明記され、その手順どおりに進めれば `gh pr create` がエラーにならない（invariant も併記） | ✅ 実装済み | - | git log 自動判定・`ISSUE_ALREADY_ON_BASE` マーカー・invariant すべて追記済み。本イシュー自身が既コミット経路で完走している（commit `b030664`）ことで実証済み |
| 2 | `test/SKILL.md` に「計画書（auto_test.md）が指定する自動テストを正とし、指定が無い場合のみ既定 pytest/Jest/E2E にフォールバックする」分岐が明記され、`description` も更新される | ✅ 実装済み | - | 計画駆動節・description 更新・フォールバック見出しすべて追加済み |
| 3 | `retro/SKILL.md` に「バックログ issue ファイルはローカル未コミットのまま残す（ベースへコミットしない）」ハンドオフ invariant が明記される | ✅ 実装済み | - | step 4 のコードブロック直後に invariant を追記済み |
| 4 | `close/SKILL.md` step 3 の staging が close 対象ファイルに限定され、未コミットのバックログ issue ファイルを巻き込まないことが明記される（`git add -A`/`.`/`docs` 禁止） | ✅ 実装済み | - | `git add -u` + 決定論ゲート（番号アンカー付き）+ broad add 禁止注記すべて追記。`I0670` 誤マッチ防止も対応済み |
| 5 | I062/I067 と同等の状況（イシュー既コミット・非app変更）を分岐文言に当てはめて規定どおり完走できることを文書レビュー（手動テスト No.1）で確認 | ⚠️ 一部不足 | Low | `/code-review` 時点では manual_test.md No.1 の実結果欄が空白（フロー順序 `/implement → /code-review → /test` の正常状態）。TC-01〜TC-08 はすべて PASS 記録済み（auto_test.md） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | ベストプラクティス | `plan-issue/SKILL.md` の既コミット経路に初回 push の具体コマンドが記載されていない。「push してから step 3 の draft PR を作成する」とのみ書かれ、feature ブランチ初回 push に必要な `git push -u origin feature/I${ISSUE_NUM}-[概要]` が省略されている。未コミット（通常）経路には `git push -u origin feature/...` が明示されており非対称。本イシューの目的（分岐の明確化）と整合しない | `.claude/skills/plan-issue/SKILL.md` 既コミット経路の説明文末尾 | 既コミット経路の手順に `git push -u origin feature/I${ISSUE_NUM}-[概要]` を追記推奨。ただし本イシュー自身が既コミット経路で完走しており（`b030664`）、Claude が文脈補完可能なため実害リスクは低い |
| Low | ベストプラクティス | `test/SKILL.md` の停止条件 2 条件（「pytest/Jest/E2E が失敗」と「計画書指定専用テストが失敗」）の関係が不明確。計画書が pytest 等の既定テストを正と指定した場合、両条件に重複して該当するが、どちら扱いになるか記載なし | `.claude/skills/test/SKILL.md` `## 停止条件` | 実害なし（どちらも STOP）。必要に応じて「計画書が既定テストを正として指定している場合も条件2として扱う」の注記を検討 |
| Low | ベストプラクティス | `plan-issue/SKILL.md` の git log 判定コマンドに `docs/issues/open/${ISSUE_NUM}.md`（`I` プレフィックスなし）のパスが列挙されている。現行命名規則では `I${ISSUE_NUM}.md` が正であり、プレフィックスなしパスは実在しない冗長エントリ | `.claude/skills/plan-issue/SKILL.md` step 2 の git log コマンド | 害なし（過剰マッチも誤マッチも起きない）。将来の命名変更時の防御的な列挙として許容範囲 |

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test [I067]` を実行してください。

VERDICT: OK
