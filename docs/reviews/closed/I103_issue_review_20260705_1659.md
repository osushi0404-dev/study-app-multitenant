# イシュー自己完結度レビュー [I103]

## 判定: 十分

重要セクション（問題・根本原因・解決方針・実装対象・スコープ・受け入れ条件）すべてに具体的な記述がある。プレースホルダや空欄なし。内容に重大な論理矛盾もなく、文脈ゼロの担当者が `/plan-issue` に着手できると判断する。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 実装対象 | `ProblemSerializer.subject` の queryset 絞り込み（二重防御）は解決方針に「任意・堅牢化・検討」と書かれているが、実装対象テーブルには `serializers.py` が存在しない。「やる/やらない」の判断が起票側では「やらない」に傾いていると読めるが、実装者が迷わないよう「本イシューでは実装しない」と明言すると親切。| 解決方針（任意・堅牢化の箇所）vs 実装対象テーブル |
| Info | 自己完結度 | 参照している `perform_update` の SEC-1（:294-297）は既存コードの行番号であり、ファイルの変更や将来の行ずれで参照先がズレる可能性がある。計画書で実際のコードスニペットを引用しておくと精度が上がる（計画フェーズへの申し送り）。| 制約・引き継ぎ情報 |

---

## 次のアクションの推奨

指摘はいずれも Info 止まりで起票・着手に問題なし。**`/grill-me` をスキップして `/plan-issue` に進める**。

計画書作成時に以下を決め切っておくと実装がスムーズになる：
1. `serializers.py` の queryset 絞り込み（二重防御）を「本イシューのスコープに含めるかどうか」を明示する。
2. `perform_update` SEC-1 の実際のコードスニペットを計画書に引用し、create 側の差分が一目で分かるようにする。
