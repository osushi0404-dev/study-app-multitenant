# 計画書レビュー [I058] ── [2026-05-03 01:45]

## 判定: 差し戻し（Blocker 2件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | P4/BP | `plan-issue-review.sh` の判定検知パターンが計画書仕様と乖離。計画書: `grep -qE "^## 判定:.*差し戻し"` → 実装: `grep -q "差し戻し"`。行頭アンカーがないため、✅完了レビューでも本文中に「差し戻し」が含まれれば誤検知し「⛔差し戻し」と誤表示される。`docs/reviews/I058_plan_review_20260503_0123.md`（✅完了）を対象に実行すると誤検知が発生することを確認済み（同ファイルに「差し戻し」が3箇所以上出現）。TC-07 は FAIL する。 | `scripts/claude/plan-issue-review.sh` 75行目 `grep -q "差し戻し"` | 計画書記載のパターン `grep -qE "^## 判定:.*差し戻し"` に修正してから TC-07 を検証する |
| Blocker | P4/BP | `code-review.sh` の `gh pr checks` 行に `|| true` が欠落。計画書: `CI_OUTPUT=$(gh pr checks "$PR_NUM" 2>&1 \|\| true)` → 実装: `CI_OUTPUT=$(gh pr checks "$PR_NUM" 2>&1)`。`set -euo pipefail` 下で `gh pr checks` が非ゼロ終了（CI 未設定・認証エラー・チェックなし等）した場合、スクリプトが即終了する。TC-09 は FAIL する。 | `scripts/claude/code-review.sh` 20行目 | `\|\| true` を追記し TC-09 を検証する |
| Warning | BP | `code-review.sh` の Blocker 判定パターンが計画書と乖離。計画書: `grep -qE "^\| Blocker \|"` → 実装: `grep -qF "\| Blocker \|"`（行頭アンカーなし）。コードレビュー出力フォーマット上はテーブル以外に `| Blocker |` が出現しにくいため実害は低いが、TC-08 は FAIL する。計画書パターンに揃えることで TC-08 も PASS になる。 | `scripts/claude/code-review.sh` 88・90行目 | 計画書記載のパターン（`-qE "^\| Blocker \|"` / `"^\| High \|"`）に修正推奨 |
| Warning | BP | `docs/reviews/` ディレクトリが存在しない場合のフォールバック（`mkdir -p docs/reviews/`）がスクリプトに含まれない。当該リポジトリでは既に `docs/reviews/` が存在するため現状は実害なし。 | `scripts/claude/plan-issue-review.sh`・`code-review.sh` 冒頭 | `REVIEW_FILE` 設定直後に `mkdir -p "$(dirname "$REVIEW_FILE")"` の追加を推奨 |
| Warning | BP | 計画書 Section 9「`${1:?...}` でパストラバーサル防止」は不正確な記述。実際には空文字・未定義変数のみチェックするシェル展開であり、パストラバーサル防止の効果はない（`I=../../etc/foo` 等は通過する）。開発者ツールのため実害は低いが、ドキュメントとして誤解を招く。 | `docs/plans/open/plan_I058.md` Section 9 | 「引数の非空チェック（パストラバーサル防止は `find_file()` の固定プレフィックスで担保）」等に表現を修正推奨 |
| Info | BP | `plan-issue-review.sh` の `## レビュー結果` 追記ロジックは実行ごとに新しいセクションヘッダーを生成するため、複数回実行後に計画書内で同名ヘッダーが重複する。現在の `plan_I058.md` は手動整理済み。設計意図として許容するなら計画書への明記を推奨。 | `scripts/claude/plan-issue-review.sh` 141〜144行目 | 改善提案（対応は任意） |

## 高リスク判定
判定: No
該当条件: なし
推奨: Blocker 2件を修正後に `/plan-issue-review I058` を再実行してください。

---
明確化後に再度 `/plan-issue-review I058` を実施してください。
