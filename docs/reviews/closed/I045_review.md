# I045 レビュー文書

## 基本情報
- **イシュー**: I045 レビュースキルにデータ整合性・運用性・コスト保守観点を追加する
- **レビュー対象**: `.claude/skills/plan-issue-review/SKILL.md`・`.claude/skills/code-review/SKILL.md`
- **作成日**: 2026-04-15

---

## レビュー観点

### Claude Code ベストプラクティス（`.claude/skills/` 変更のため必須）

| 項目 | 確認内容 | 計画時点での状況 |
|------|---------|----------------|
| allowed-tools 最小権限 | 既存設定を変更しないため問題なし | OK |
| disable-model-invocation | 副作用のある操作なし（Read/Edit のみ）。既存設定維持 | OK |
| argument-hint | 既存設定を変更しないため問題なし | OK |
| description | 既存設定を変更しないため問題なし | OK |
| 停止条件・完了条件 | 既存の判定ロジック（Blocker/High判定）を変更しない | OK |
| 変数の一貫性 | `$ARGUMENTS` の使用は既存部分のみ。追加セクションに変数は不要 | OK |
| 500 行以内 | 現行: plan-issue-review 103行・code-review 80行。追加で +60行程度 → 両ファイルとも 200行以内の見込み | OK |

### セキュリティ
スキルファイルのみの変更。バックエンド・フロントエンドのコード変更なし。セキュリティ影響なし。

### ベストプラクティス
- 既存の3分類（ベストプラクティス/セキュリティ/モダン開発）に統合せず、独立セクションとして追加することで観点の境界を明確に保つ
- proposalsの「含めないもの」定義（Section 4）との整合を実装後に確認する

### モダン開発
変更対象なし。

---

## 実装後レビュー結果

（/code-review 実行時に記録）
