## 決定論ゲート実行結果（code-review.sh 自動注入）

(決定論ゲート宣言なし)
- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

```
# コードレビュー [I104] ── 2026-07-15

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 非admin が PUT/PATCH/DELETE /api/subjects/{id}/ → 403 | ✅ 実装済み | - | `get_permissions()` L87: `if self.action in ('create','update','partial_update','destroy')` で `[IsAuthenticated, IsOrgAdmin]` を返す。TC-AUTO-01 で 403 + DB 未改変の両面確認。TC-INJECT-01（false-green 注入）も OK 記録済み |
| 2 | 非admin が GET /api/subjects/ ・ /api/subjects/{id}/ → 200 | ✅ 実装済み | - | else 分岐で `[IsAuthenticated()]` のみ。TC-AUTO-02 で list/retrieve 両方 200 確認 |
| 3 | admin は create(201) / update(200) / destroy(204) を実行できる | ✅ 実装済み | - | TC-AUTO-03 で create→update→destroy フローをステータスコード + DB 状態で確認 |
| 4 | 非admin が /subject-management・/subject-management/:id 直接遷移 → OrgAdminRoute 権限エラー | ✅ 実装済み | - | App.tsx L84-85 で 2 ルートとも `<OrgAdminRoute>` でラップ。TC-STATIC-01/02 で静的確認済み。OrgAdminRoute は `user.role !== 'admin'` で MUI Alert 表示・未ログインは `/login` redirect |
| 5 | 認可回帰テスト追加・全 PASS | ✅ 実装済み | - | `test_I104_subject_authz.py` 5TC 追加。TC-INJECT-01（false-green 注入）で Red→Green を確認・manual_test に記録済み。テスト実行結果欄空白は `/code-review` 時点で正常（`/test` スキル前） |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | セキュリティ / `@action` と `get_permissions()` の相互作用 | `public` アクション（`@action(permission_classes=[AllowAny])`）が `get_permissions()` の else 分岐に落ち `[IsAuthenticated()]` を返すため、**未認証で `/api/subjects/public/` が 401 になる pre-existing bug** が TC-SMOKE-01 で確認されている。登録画面（未認証ユーザーの科目選択）が設計通りに動作しない状態。ただし I104 の変更（else 分岐不変更）による退行ではなく変更前から存在する挙動。TC-SMOKE-01 に記録・MEMORY.md にも起票候補として記録済み | `backend/problems/views.py` L84-89（`get_permissions()` else 分岐）/ L48（`@action(permission_classes=[AllowAny])`） | 本 PR スコープ外・差し戻しなし。別イシューで `elif self.action == 'public': return [permissions.AllowAny()]` を追加して根治することを推奨。MEMORY.md 記録で対応済み |
| Low | P4 テスト妥当性 | `test_admin_can_crud` の PUT で `{"name": "更新後"}` のみ送信しているが、SubjectSerializer に必須フィールドが他にある場合は 400 になり TC が偽陽性になりうる。ただし `assert put.status_code == 200, put.content` と content を出力するようになっているため実行時に即判明する設計で許容範囲 | `test_I104_subject_authz.py` L77 | 実行時に自明なため対応不要 |

## 高リスク判定
判定: No
該当条件: なし（認可変更はあるが「許可範囲を狭める」方向のみ。認証方式・セッション・Cookie・CORS の変更なし。DB 変更なし。`get_queryset()` の自組織フィルタリングは不変でクロステナント越境なし。`IsOrgAdmin` と `OrgAdminRoute` は I102 で実証済みの既存部品を再利用）

---
✅ コードレビュー完了。`/test [I104]` を実行してください。

VERDICT: OK
```
