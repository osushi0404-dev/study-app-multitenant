## 決定論ゲート実行結果（code-review.sh 自動注入）

- `bash scripts/claude/tests/test_pretooluse_worktree_guard.sh` → exit=0 ✅
- `bash scripts/claude/tests/test_pretooluse_checkout_guard.sh` → exit=0 ✅
- `bash scripts/claude/tests/test_pretooluse_push_guard.sh` → exit=0 ✅
- `python3 -m py_compile scripts/claude/hooks/pretooluse_guard.py` → exit=0 ✅
- `python3 -m json.tool .claude/settings.json` → exit=0 ✅
- `grep -q "pretooluse_guard.py" docs/runbooks/worktree.md` → exit=0 ✅
- `grep -q "MultiEdit" .claude/settings.json` → exit=0 ✅
- `grep -q "NotebookEdit" .claude/settings.json` → exit=0 ✅

- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

全ファイル読み取り完了。レビューを実施します。

---

```
# コードレビュー [I095] ── 2026-07-02 16:35

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | Edit/Write → 別 worktree 配下で exit 2 ブロック（逆方向も） | ✅ 実装済み | - | `main()` L413-421。`_cross_worktree` True/None → `_block_cross_worktree` → sys.exit(2)。gate=exit 0 |
| 2 | Bash の echo/>>/tee/cp/mv/sed -i が別 wt 宛でブロック | ✅ 実装済み | - | `_bash_write_targets()` + `_cross_worktree()` → `_block_cross_worktree()`。gate=exit 0。TC-A11〜A15, A11b |
| 3 | 現 worktree 配下の Edit/Write/Bash 書込は通る（誤ブロック無し） | ✅ 実装済み | - | `_cross_worktree()` が cur と一致するルートをスキップ。realpath+os.sep 境界一致。gate=exit 0。TC-A2, A9, A20a |
| 4 | /tmp 等リポジトリ外への書込は通る | ✅ 実装済み | - | いずれの worktree ルートにも一致しなければ False → allow。gate=exit 0。TC-A3 |
| 5 | 別 worktree 配下の Read/cat/grep の読み取りは通る | ✅ 実装済み | - | cp 宛先のみ抽出（src 除外）、cat/grep は書込語なし。gate=exit 0。TC-A16, A17 |
| 6 | worktree 境界確定不能時に fail-safe ブロック＋git 状態修復メッセージ | ✅ 実装済み | - | `_cross_worktree()` が None → `_block_cross_worktree()` fail-safe メッセージ＋exit 2。gate=exit 0。TC-A18 |
| 7 | DANGER_OK=1 を前置しても横断書込は解除されない | ✅ 実装済み | - | worktree 判定 L442-452 は `if not danger_ok:` (L454) の外。gate=exit 0。TC-A19 |
| 8 | decoy 反証含む自動テスト PASS | ✅ 実装済み | - | TC-A4（兄弟名 decoy）、TC-A10（symlink decoy）、TC-A21（falsegreen 注入 複製=0/実体=2）。gate=exit 0（26/26 PASS） |
| 9 | Bash 未カバー経路が stderr に明示（silent に取りこぼさない） | ✅ 実装済み | - | `_bash_uncovered_risk()` + L450-452 stderr 出力。gate=exit 0。TC-A22（cd経由 exit 0 かつ警告出力を grep 確認） |
| 10 | worktree.md §9 に技術強制済みを追記 | ✅ 実装済み | - | L134 に pretooluse_guard.py 言及・fail-safe/エスケープ無し/双方向記述あり。gate=exit 0 |
| 11 | MultiEdit/NotebookEdit でも別 worktree 配下がブロック（マッチャ明示化） | ✅ 実装済み | - | settings.json L143 に `Edit\|Write\|MultiEdit\|NotebookEdit`。main() L413 完全一致分岐。gate(MultiEdit)=exit 0 / gate(NotebookEdit)=exit 0。TC-A7, A8 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス | 前回 Medium 指摘（`_has_write_intent()` による cp read への誤警告）は `_bash_uncovered_risk()` の独立関数化と TC-A23 追加により解消確認済み。`_bash_uncovered_risk()` は `_has_write_intent()` 先行チェックで read-only コマンドを除外し、cd/eval/変数越しの場合のみ警告する設計。TC-A23 が `cp <別wt>/src ./dest` で警告不発を決定論検証 ✅。なお `sed -i 's/$VAR/x/' ./local.txt` のように sed スクリプト文字列に `$` を含む**ローカルへの書込**で誤警告（exit 0 + 警告のみ）が残存するが、ブロックではなく誤警告・前回 Low 継続として記録する。 | `pretooluse_guard.py` `_bash_uncovered_risk()` L366 | 改善提案（対応任意）。`$` 判定をリダイレクト演算子直後トークン・tee/cp/mv/sed の宛先候補トークンに限定すれば根治可能だが、スコープ外での対応でよい |
| Low | ベストプラクティス | 前回 Low 指摘（`_block_cross_worktree()` 内で `_worktree_roots()` / `_current_worktree_root()` を再呼び出し=git 二重実行）は未修正。競合状態での `別 worktree（None）` 表示は `where = f"別 worktree（{x_root}）" if x_root else "別 worktree"` で防止済み。通常運用上は実害なし。 | `pretooluse_guard.py` `_block_cross_worktree()` L370-385 | 対応任意（前回 Low 継続）。`_cross_worktree()` が `(result, roots, cur)` タプルを返す設計で根治可能 |
| Low | ベストプラクティス | 前回 Low 指摘（リダイレクトループで宛先トークン `/other/f` が次反復でも `any(op in t ...)` の判定対象になる）は未修正だが実害なし（絶対パスに `>` を含む実使用は皆無・後段の絶対パスフィルタが安全弁）。 | `pretooluse_guard.py` `_bash_write_targets()` L305-315 | 実害なし・対応不要（前回 Low 継続） |

（Blocker・High 指摘なし）

## 高リスク判定
判定: No
該当条件: なし

---

決定論ゲート全 8 件 exit=0、omission-lint: OK。前回 BLOCKER の原因（grep パターンに `|` を含む単一コマンドがチェーン系判定で fail-closed）は `grep -q "MultiEdit"` / `grep -q "NotebookEdit"` への分割（最終コミット `cf4fea8`）で解消。前回 HIGH の omission-lint HIGH も今回 OK に解消。LLM 観点では Blocker・High 指摘なし。前回 Medium（cp read 誤警告）は `_bash_uncovered_risk()` 導入+TC-A23 で解消確認済み。残存 Low 3 件はいずれも前回から継続する cosmetic・既知の限界。

✅ コードレビュー完了。`/test [I095]` を実行してください。

VERDICT: OK
```
