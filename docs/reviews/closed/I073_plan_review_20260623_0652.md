全4ファイルの読み取り完了。実在コード（`models.py` / `problem_service.py`）も照合してレビューを実施する。

---

```
# 計画書レビュー [I073] ── 2026-06-23 14:30

## 判定: 差し戻し（Blocker 1件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | P3/P4 データ整合性・テスト妥当性 | **TC-AUTO-07 の期待結果と `_reconcile_images` ロジックの矛盾（物理削除先行問題）**。計画書 `_reconcile_images` のステップ4（削除対象の物理ファイル削除 + `asset.is_deleted=True`）がステップ5の新規ファイル `_save_image_as_asset`（内容バリデーション含む）より先に実行されるため、TC-AUTO-07 のシナリオ「既存1枚削除＋不正ファイル追加」では: ①ステップ4で削除対象Bの物理ファイルが削除される → ②ステップ5で不正ファイルのバリデーション失敗 → ③トランザクションロールバック で DB は元に戻るが、**Bの物理ファイルは削除済みで戻らない**。TC-AUTO-07 期待結果「物理ファイルが全て元のまま（削除されていない）」は実現できない。また、セクション10「新規保存失敗が先に起きるケースでは削除前に例外で抜ける順序を保つ」という設計意図が `_reconcile_images` のコードロジックに反映されていない。修正方法: ステップ3と4の間に「新規ファイルの内容バリデーション先行実行（保存はしない）」ステップを挿入し、バリデーション失敗時は削除前に例外で抜ける順序に変更すること。あわせて TC-AUTO-07 の期待結果を修正すること。 | 計画書 §5 `_reconcile_images` ステップ3〜5、§10 トランザクション境界「物理削除の安全性」、`docs/tests/open/I073_auto_test.md` TC-AUTO-07 期待結果 | 実装前に修正必須 |
| Warning | P4 テスト妥当性 | AC「既存画像を差し替えでき（削除＋追加）、結果が正しく反映される」に対応する**正常系の自動テスト TC が不在**。TC-AUTO-01（追加）・TC-AUTO-03（削除）は独立ケースであり、1リクエスト内で「既存アセットを除外しつつ新規ファイルを同時追加する」差し替えシナリオを明示的にテストするケースがない。TC-AUTO-10（explanation 種別）は「TC-01/03/04 相当」とあるが差し替えが含まれるかも不明。差し替えは `order` に `existing` と `new` を混在させる最も一般的な編集操作であり、単独 TC（例: TC-AUTO-11）の追加を推奨。 | `docs/tests/open/I073_auto_test.md` TC一覧、計画書 §6 テスト計画 | 修正推奨（差し戻しはしないが TC 追加を強く推奨） |
| Warning | P5 運用性 | 物理ファイル削除失敗時の `logger.warning(...)` に記録される情報（request_id / user_id / tenant_id 等）が計画書・コードロジックに明示されていない。`delete_problem` の既存実装（`f"Failed to delete file: {asset.storage_key}, error: {e}"`）は storage_key と error のみで、障害追跡に必要な文脈情報が不足している。本イシューの新規ログも同様になる可能性がある。構造化ログ方針（`problem_id`・`user_id`・`organization_id` を含める）を計画書に明記することを推奨。 | 計画書 §5 `_reconcile_images` ステップ4 ログ、R7 | 修正推奨 |
| Info | BP | `_reconcile_images` のステップ4共有チェックで、同時並行リクエストが同じ非共有アセットを削除しようとした場合、両方が `shared=False` と判定して二重物理削除を試みる競合条件が発生し得る。計画書には「同一問題の同時編集は想定外・スコープ外」と明記されており許容設計だが、`SELECT_FOR_UPDATE` 等の排他制御の可否について将来イシューでの検討を推奨。 | 計画書 §10 同時更新 | 改善提案（対応は任意） |

## 高リスク判定
判定: Yes
該当条件:
- ファイルアップロード/ダウンロード（画像ファイルの保存・物理削除を伴う）
- 外部公開API変更（既存 PUT `/api/problems/{id}/` を multipart 対応に変更）
推奨: Blocker 解消後に再度 `/plan-issue-review I073` を実施し、クリアになれば `/security-review I073` を実行してから `/implement I073` へ進んでください。

---
## 差し戻し内容の要約

### Blocker（修正必須）
**`_reconcile_images` ロジックの修正（バリデーション先行化）**:

現在のステップ順序:
```
3. keep_asset_ids 決定（ファイルキー存在確認のみ）
4. 削除対象の物理ファイル削除 + is_deleted=True  ← 先に削除
5. current.delete() → _save_image_as_asset でバリデーション  ← バリデーションは後
```

修正後（セクション10の意図を実現する順序）:
```
3.  keep_asset_ids 決定（ファイルキー存在確認）
3'. 新規ファイルの内容バリデーション先行実行（validate_image_file のみ・保存しない）
    → バリデーション失敗 → ValidationError → 削除前に例外で抜ける
4.  削除対象の物理ファイル削除 + is_deleted=True  ← バリデーション確認後に削除
5.  current.delete() → _save_image_as_asset で保存（バリデーション済み）
```

また、TC-AUTO-07 の期待結果の「物理ファイルが全て元のまま」はロールバック保証範囲（DB のみ）を超えているため、修正後の意図に合わせて「DB 上は全て元のまま（link 2件・MediaAsset.is_deleted=False × 2）」と修正すること（修正後ロジックでは物理ファイルも削除前に抜けるため、この期待結果が正しくなる）。

明確化後に再度 `/plan-issue-review I073` を実施してください。

VERDICT: BLOCKER
```
