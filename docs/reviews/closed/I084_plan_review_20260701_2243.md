全ファイルを読み込みました。第三者レビューアとして、問題の発見を優先して各観点を検討します。

---

```
# 計画書レビュー [I084] ── 2026-07-01 11:45

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | **サブシェル scope 不整合**: §4-2 の `GATE_EVIDENCE=$(run_declared_gates "$AUTO_TEST_FILE")` は `$()` 内（サブシェル）で動くため、関数内の `GATE_VERDICT=BLOCKER` は呼び出し元に伝播しない。コメント "# GATE_VERDICT を設定" は bash の動作として誤り。実装は temp ファイル・fd・または2コール分離等で解決が必要。TC-RUN4/5 が実装時に必ず捕捉するが、計画書レベルで解決策を記載すべき。 | plan §4-2 コードスニペット・関数定義テーブル | 修正推奨（実装者は実装時に気付くが、計画書の code snippet が misleading） |
| Warning | テスト妥当性 | **AC3「timeout 超過→BLOCKER」の自動 TC 不在**: AC3 は「timeout 超過」を fail-closed の対象として明示しているが、auto_test.md Section C に long-running コマンドを ALLOW 分類してタイムアウトさせ BLOCKER になることを確認する TC がない。`sleep 200` 相当の fixture で検証できる。 | auto_test.md §C（TC-RUN 群）/ plan AC3 | 修正推奨（AC3 の検証漏れ） |
| Warning | テスト妥当性 | **`python3 -m py_compile` の classify_gate TC 不在**: allowlist に `python3 -m py_compile` が含まれる（plan §4-1・I084 grill-me 追補）が、TC-CL1〜14 に対応するケースがなく、allowlist 前方一致判定の正しさが単体テストで固定されていない。 | auto_test.md §B / plan §4-1 allowlist 定義 | 修正推奨 |
| Warning | テスト妥当性 | **omission-lint の対象行スコープ未定義（テーブルセル自傷リスク）**: plan §4-1 は "allowlist ゲートパターン行" を検出とするが、行先頭アンカー（`^`）の有無を未定義。`I084_auto_test.md` の §B/C/E/G テーブルには `` `bash scripts/claude/tests/test_x.sh` `` 等が多数含まれており、非アンカー grep 実装だと code-review 実行時にドッグフーディング文書自身で自傷 HIGH が発生する。TC-OM6 は template の heavy ブロックを対象とするが、auto_test.md の TC テーブルセル含有ケースを網羅していない。 | plan §4-1 omission_lint 定義・auto_test.md §B/C/E/G テーブル・TC-OM6 | 修正推奨（実装仕様に「行先頭アンカー or テーブル行除外」を明記、TC-OM にカバーケース追加） |
| Warning | Claude Code BP（実行順序） | **TC-WIRE1 が実行順序を検証しない**: TC-WIRE1 は `run_declared_gates` 等の関数呼び出しを grep で存在確認するのみで、「`claude -p` より前に `run_declared_gates` が呼ばれること」「`inject_gate_result` が `claude -p` 保存後に呼ばれること」の順序を保証しない。順序が逆転すると証跡注入が無効になるが、grep 存在確認では検出できない。 | auto_test.md §F TC-WIRE1 | 修正推奨（行番号比較 grep や integration fixture 等でフロー順序を固定） |
| Info | テスト妥当性 | TC-RUN8 は sentinel 生存確認（破壊コマンドが実行されないこと）のみ確認し GATE_VERDICT の値を検証していない。UNSAFE→BLOCKER は TC-RUN6 が担保しているため実質的な穴はないが、RUN8 で GATE_VERDICT も assert すれば安全境界の完全性が高まる。 | auto_test.md §C TC-RUN8 | 改善提案 |
| Info | Claude Code BP | `code-reviewer.md` の更新対象は「テスト妥当性」節・「出力フォーマット」節と計画に記載があるが、同ファイル内の VERDICT 説明箇所や「判定の根拠」節（既存内容次第）にも「決定論ゲートが注入済みの場合は LLM 独自判定を記録の根拠にしない」旨の整合確認が明記されていない。現ファイル内容との差分が実装時に確認されれば問題ないが計画書への記載はない。 | plan §4-3 | 改善提案 |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I084` を実行してください。

---

VERDICT: OK
```
