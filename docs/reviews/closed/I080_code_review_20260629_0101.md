十分な情報を収集できました。レビューを作成します。

```
# コードレビュー [I080] ── 2026-06-29 10:45

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `allow` に `Bash(git push)`・`Bash(git push *)` が含まれ、`ask` から `Bash(git push *)` が除かれている | ✅ 実装済み | - | settings.json L40-41 に追加。ask セクションから削除確認。冗長な `git push -u origin *` も削除（理想形集約） |
| 2 | `deny` の保護ブランチ/force エントリが維持されている | ✅ 実装済み | - | L104-109: develop/main/HEAD/`--force*` 全エントリ維持 |
| 3 | settings.json が有効な JSON | ✅ 実装済み | - | TC-S1 PASS |
| 4 | フックが protected 宛先 push の**静的形**を exit 2 ブロック（明示名・引数なし保護ブランチ上・refspec・+develop・refs/heads/develop・HEAD:refs/heads/main。動的形は I083 対象） | ✅ 実装済み | - | TC-P1〜17, P27, P33 全 PASS。`_norm_push_dest` の処理順（`:` 分割→`lstrip("+")`）が正しく TC-P33（dst側`+`）も捕捉 |
| 5 | protected 宛先 push が `DANGER_OK=1` 前置で exit 0（release/hotfix 解除経路） | ✅ 実装済み | - | TC-P6,7,23,24,26,31 PASS。M3-B で使い捨て bare repo への実行成立を確認 |
| 6 | 宛先正規化が完全一致で develop-fix/main-backup が exit 0（FP なし） | ✅ 実装済み | - | `dest in PROTECTED_BRANCHES`（タプル完全一致）。TC-P18/P19 PASS |
| 7 | `--all`/`--mirror` が exit 2・DANGER_OK で exit 0 | ✅ 実装済み | - | L234 の独立 regex チェック。TC-P21/P22/P24 PASS |
| 8 | `--force`（`git push --force origin feature` 形）が DANGER_OK 解除可 | ⚠️ 一部不足 | Medium | `git push --force origin feature` 形（TC-P25/P26）は満たす。ただし `-f` 短縮形・複合コマンドは `re.match` + deny glob 双方が空振りし DANGER_OK 不要で通る（L2/F1）。既知の限界として I083#166 へ繰り越し済み |
| 9 | feature 上の安全 push（素 push・`-u origin <feature>`・`<src>:<feature>`）が exit 0 | ✅ 実装済み | - | TC-P5/P8/P9/P10/P20 PASS |
| 10 | false-green テストが protected/`--all`/`--mirror`/`--repo` 判定行ごとに独立（A〜F） | ✅ 実装済み | - | FG-A〜F の3系統、`sed` による各判定行の1回限りの置換を確認 |
| 11 | `docs/claude-code-structure.md` が新仕様に同期 | ✅ 実装済み | - | allow 列に `git push`/`git push *` 追加、ask 列から削除、danger-op 説明追加、既知の限界注記追加。TC-DOC1〜3 PASS |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | セキュリティ（L2/F1・既知繰り越し） | `allow: git push *` 化により、旧 `ask: git push *` が提供していた `-f` 短縮形の確認機会が消失。`git push -f origin <feature>` は `re.match(r"git\s+push\b.*--force")` でマッチせず（先頭アンカー＋`--force` 限定）、deny `Bash(git push * --force*)` も空振り（`-f` は `--force*` と不一致）し、**非保護ブランチへの `-f` force push が DANGER_OK 不要で無確認実行される**。複合コマンド（`cd foo && git push --force origin x`）も同様に先頭アンカーで取りこぼし。I080 実装前は `ask` が第二の安全網だったが今は完全に無防備。保護ブランチ宛ては `_push_protected_target`（`_segments` 分割）が捕捉するため漏れは非保護宛に限定。イシュー L2/計画書 R6/review.md F1 に明記済み・I083#166 で根治予定 | `pretooluse_guard.py:243`（`re.match` の力 push 判定）、`.claude/settings.json`（deny glob `git push * --force*`） | I083#166 で `re.search` 化＋`-f`/`--force-with-lease` 対応。本 PR はスコープ外として現状維持 |
| Medium | テスト妥当性 | `TC-FALSEGREEN` が protected 判定・`--all`/`--mirror`・`--repo` の3判定行を裏取りしているが、**force push 判定行**（`re.match(r"git\s+push\b.*--force", ...)`）の false-green 注入 TC が存在しない。この行が削除・無効化されても `test_pretooluse_push_guard.sh` は検知できない（TC-Pregr1 が正常系として機能するだけ） | `scripts/claude/tests/test_pretooluse_push_guard.sh`（TC-FALSEGREEN 群） | I083 で force 判定行を `re.search` 化修正する際に TC-FALSEGREEN-G/H を追加予定（plan-review 20260629_0047 Info 指摘と一致） |
| Low | ベストプラクティス（実装一貫性） | force push 判定だけが `re.match`（先頭アンカー）を使用。同じ `if not danger_ok:` ブロック内の他の danger-op（`reset --hard`・`docker compose down -v`・`rm -rf`・`--all`/`--mirror`・`--repo`）は全て `re.search` を使用しており、force 判定のみ実装方針が非対称 | `pretooluse_guard.py:243` vs :234/:239/:247/:251/:255 | I083 での修正（`re.search` 化）で解消予定 |

## 高リスク判定
判定: Yes
該当条件:
- ハーネスのツール実行権限変更（`git push *` を ask→allow 化）：Claude Code の認可権限の変更に相当
- セキュリティガード（`pretooluse_guard.py`）の既存 protected-push 判定ロジックを全置換するリファクタリング：実装ミスが即 protected push の素通りにつながる
- `allow: git push *` により L2/F1 の gap（`-f` 短縮形）が ask 確認なしになる回帰

---

（Blocker / High なしの場合）
✅ コードレビュー完了。`/test [I080]` を実行してください。

VERDICT: OK
```
