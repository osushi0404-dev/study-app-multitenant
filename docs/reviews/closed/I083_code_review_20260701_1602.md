実装コード・テストスクリプト・ドキュメント変更を読み込みました。第三者レビューを実施します。

---

```
# コードレビュー [I083] ── 2026-07-01 03:30

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 6 回避形（`$(...)`/`$(rev-parse)`/`$VAR`/`-c alias.`/`eval`/`sh -c`）が `not danger_ok` で全て ask（exit0＋ask JSON） | ✅ 実装済み | - | `_push_is_dynamic` (i) push 以降引数の `$\`{(` 検出、(ii) 構造的ラッパー（eval/shell-c/git -c alias.）検出の2経路で網羅。TC-D1〜D6/D8/D10 で固定 |
| 2 | 誤検知=hard block(exit2)。動的 feature push が exit2 されない（ask は許容） | ✅ 実装済み | - | `_push_is_dynamic` は `_ask()`→`sys.exit(0)` で hard block しない設計。TC-D7 で固定 |
| 3 | 静的安全 push（素 push・`-u origin feature`）が無確認通過（ask も block もなし） | ✅ 実装済み | - | `after` に動的メタ文字なし・ラッパー先頭トークンなし → False。TC-S1〜S3 で `runj=0`（ask JSON 無し）を確認 |
| 4 | ラッパー誤 ask 回帰（`bash-feature`/`sh-fix`/`eval-remote`/非 alias `-c`）が無確認通過 | ✅ 実装済み | - | 構造的検出（先頭トークン判定）で branch/remote 名の偶然一致を排除。TC-W1〜W4 で固定。`git -c user.name=x push` は alias. で始まらないため正しくスルー |
| 5 | 静的 protected 形（`origin develop` 等）が従来どおり exit2 block で無回帰 | ✅ 実装済み | - | 既存 P1〜P33/Pregr1 が I083 変更後も変更なしで稼働。`_push_protected_target` の前置確認済み |
| 6 | 【F1】force 取りこぼし形（`-f`/`-uf`/`--force-with-lease`/`--force-if-includes`/複合）が exit2 block | ✅ 実装済み | - | `_push_has_force` が `_segments` ベースでトークン走査。TC-F1〜F6 で全形を固定 |
| 7 | 【F1 誤 block 回帰】`; echo "--force"`/`&& echo "--force"`/`--push-option=force`/`rm -f && push` が無確認 pass | ✅ 実装済み | - | `_segments` トークン単位判定により後続 echo の `--force` は別セグメントとなり push セグメントの after に含まれない。TC-FP1〜FP4 で `runj=0` 固定 |
| 8 | 【ラッパー隠蔽 force】`eval "git push --force origin x"` が block でなく ask に degrade | ✅ 実装済み | - | `eval "..."` はシングルトークン化で `_push_has_force` の `"git" in toks and "push" in toks` に非マッチ → force block せず、`_push_is_dynamic` の eval 分岐で ask。TC-F7/FG-L で固定 |
| 9 | `DANGER_OK=1` 前置時の escape（動的形・force・ラッパー隠蔽 force）が維持される | ✅ 実装済み | - | `danger_ok = raw.lstrip().startswith("DANGER_OK=1 ")` で `if not danger_ok:` 全体をスキップ。TC-DOK1〜3 で確認 |
| 10 | false-green 注入で各判定行が load-bearing であることを対で裏取り | ✅ 実装済み | - | TC-FG-G〜L の 6 分岐（`_push_has_force`/短縮クラスタ/`_push_is_dynamic`/eval 分岐/sh-c 分岐/alias 分岐）を sed 無効化＋対検証。ask 系（FG-I/J/K/L）は `rungj`/`runj` で stdout JSON 判別 |
| 11 | `.claude/settings.json` 不変。`docs/claude-code-structure.md` 限界注記を「対応済み」＋残余既知限界明記に更新 | ✅ 実装済み | - | diff に settings.json なし。claude-code-structure.md の旧注記を「I083 で対応済み」へ更新し `eval "$VAR"` 完全隠蔽を残余限界として明記。TC-DOC1a/b/c で grep 決定論固定 |
| 12 | 既存 `test_pretooluse_push_guard.sh`（40件）・`test_pretooluse_checkout_guard.sh`（14件）無回帰 | ✅ 実装済み | - | I083 TC は追記形式。既存 TC（P1〜P33/Pregr1/FG-A〜F）は変更なし。checkout guard ファイルも diff に含まれず不変 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | ベストプラクティス（兄弟ルール一貫性） | `--all/--mirror` 判定（`:291`）が `re.search(r"\bgit\s+push\b.*\s(--all|--mirror)\b", cmd, re.I)` のまま残っており、force の `re.match` を `_segments` 化した今回の修正と非対称。`git push origin feature; echo "--all done"` のような複合コマンドで `.*` が `;` を跨いで `--all` に貪欲一致し、安全な feature push が誤 block される。I083 の force false-positive 修正と同一原因・同一修正方針（`_segments` ベース）が適用可能だが、本 PR の修正対象から漏れている。I080 由来の既存欠陥であり今回の変更が新規に持ち込んだものではないが、force の類縁バグを直しつつ類縁の `--all/--mirror` は残ったことは指摘しておく。 | `pretooluse_guard.py:291` | 次イシューで対応推奨（今回スコープ外のため差し戻しはしない） |
| Low | ベストプラクティス | `_is_force_flag` の単一ダッシュ短縮クラスタ判定 `"f" in tok[1:]` は `-rf`・`-xf` 等の非実在フラグを push セグメント内で force と判定し exit 2 block する。コード内コメントに「push セグメント内で `-rf` は無効構文＝実害なし」と明記済みのため運用上の影響はない。 | `pretooluse_guard.py:150` | 文書化済み・許容 |
| Low | テスト妥当性 | `runj` の `out=$(json "$1" \| python3 "$GUARD" 2>/dev/null); code=$?` で `set -uo pipefail` が有効な場合、`json()` が（万一）非ゼロ終了すると `code` が json の exit を取る可能性がある。実用上 `json()` は Python の `print()` のみで失敗しないが、注記があると後続メンテが容易。 | `test_pretooluse_push_guard.sh:33` | 情報共有のみ |

## 高リスク判定
判定: No
該当条件: なし（本件はハーネス安全機構の堅牢化。Django/React アプリコード・DB・認証・個人情報・マルチテナント境界変更なし。変更方向は「取りこぼし削減＋誤検知根治」であり権限を緩める変更を含まない。`allow: Bash(git push *)` 維持）

---
✅ コードレビュー完了。`/test [I083]` を実行してください。

VERDICT: OK
```
