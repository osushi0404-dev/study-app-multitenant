ソースコードをすべて確認しました。レビュー開始します。

---

```
# 計画書レビュー [I131] ── 2026-07-19 15:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Info | P1 | `_get_organization` のpersonal自動作成パス（views.py:154-162）が `category` なし で `Organization.objects.create()` を呼ぶため、personal組織が存在しない環境ではDB制約エラーになる（`category` は NOT NULL FK）。テストはfixture自作のため影響なし・既存バグかつスコープ外だが、計画書「スコープ外の既知事項」欄への補足記載があると将来の混乱を防げる | `accounts/views.py:154-162` | 任意（スコープ外） |

（その他の観点は以下のとおり問題なし）

- **P1 要件適合性**: バグ箇所3点をソースコードで実確認（`views.py:88`・`:230`・`serializers.py:91` すべて `organization_id` 旧参照が残存）。`Organization.id = models.AutoField(primary_key=True)` への対応が3箇所とも正確。レスポンスキー名 `organization_id`・serializer 入力フィールド名 `organization_id` の維持は API 契約上正当。`validate()` 内の `Subject.objects.filter(organization_id=organization_id, ...)` は Subject の FK attname であり改名不要の正当利用として正しく分類されている。スコープ拡張の承認ポイントが明示されており、3箇所同時修正でなければ登録導線の復旧が達成されないことを調査で実証済み。
- **ベストプラクティス**: TDD RED確認→修正→GREEN確認→false-green自己検証（注入1/2）の手順が具体的に明記。注入2で `organization.id + 1` → validate_organization_id が FieldError ではなく ValidationError（404 ではなく 400）になることがコードから確認でき、`status == 201` assertが確実にFailになる設計。`git restore` 禁止・Edit ツールで復元する旨の注記あり。
- **セキュリティ**: 両エンドポイントとも既存 `AllowAny` 維持。新規開放ではなく壊れた公開設計の修正のみ。3行改名追随で新規 SQL / XSS / CSRF / SSRF リスクなし。`views.py:88` 修正後にようやく到達する `serializers.py:91` の FieldError も同時修正で解消されることを計画書が正確に説明している。
- **P4 テスト妥当性**: TC-AUTO-01/05/06 は TDD フローで修正前 FAIL を記録してから採用（false-green でない保証）。TC-AUTO-03/04 は body 完全一致でルート不在等の別要因 404 と区別。TC-AUTO-07 は `main_message` 文言で AttributeError 経由の「エラーが発生しました」と区別（コード実確認: `views.py:79-84` の分岐文言と一致）。認可変更なしのため未認証クライアントで全 TC を実行すること自体が認可回帰固定になる。TC-AUTO-06 で personal org を fixture 自作する理由（`--no-migrations`）が明記されている。
- **P3 データ整合性**: 対象外（DB・マイグレーション変更なし）。
- **P5 運用性**: 対象外（外部API・非同期処理変更なし）。
- **P8 コスト**: 問題なし（新規インフラなし）。
- **P9 プライバシー**: 対象外（個人情報の取り扱いコード変更なし）。
- **Claude Code BP**: 対象外（指示ファイル変更なし）。

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I131` を実行してください。

VERDICT: OK
```
