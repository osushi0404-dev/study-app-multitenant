実装コードとテストスクリプトを読み込みました。詳細なコードレビューを実施します。

---

```
# コードレビュー [I083] ── 2026-07-01 XX:XX

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 6 回避形（`$(...)`/`$(rev-parse)`/`$VAR`/`-c alias.`/`eval`/`sh -c`）が `not danger_ok` で全て ask（exit0＋ask JSON） | ✅ 実装済み | - | `_push_is_dynamic` が (i) push 以降引数の動的メタ文字、(ii) 構造的ラッパー（eval/shell-c/git-c alias.）を判定。TC-D1〜D6/D8/D10 で固定 |
| 2 | 誤検知=hard block(exit2)。動的 feature push が exit2 されない（ask は許容） | ✅ 実装済み | - | `_push_is_dynamic` は ask(exit0) であり hard block しない。TC-D7 で固定 |
| 3 | 静的安全 push（素 push・`-u origin <feature>`）が無確認通過 | ✅ 実装済み | - | TC-S1〜S3 で `runj=0`（ask JSON 無し）を固定 |
| 4 | ラッパー誤 ask 回帰（`bash-feature`/`sh-fix`/`eval-remote`/非 alias `-c`）が無確認通過 | ✅ 実装済み | - | 構造的検出（先頭トークン判定）で branch/remote 名の偶然一致を排除。TC-W1〜W4 で固定 |
| 5 | 静的 protected 形（`origin develop` 等）が従来どおり exit2 block で無回帰 | ✅ 実装済み | - | 既存 P1〜P33 + TC-P1/P2 で無回帰確認済み |
| 6 | force 取りこぼし形（`-f`/`-uf`/`--force-with-lease`/`--force-if-includes`/複合）が exit2 block | ✅ 実装済み | - | `_push_has_force`（pure `_segments` ベース）が全形を捕捉。TC-F1〜F6 で固定 |
| 7 | F1 誤 block 回帰（`; echo "--force"`/`&& echo "--force"`/`--push-option=force`/`rm -f &&`）が無確認 pass | ✅ 実装済み | - | `_segments` トークン単位判定により後続 echo の `--force` を誤貪欲一致しない。TC-FP1〜FP4 で `runj=0` 固定 |
| 8 | ラッパー隠蔽 force（`eval "git push --force origin x"`）が block でなく ask に degrade | ✅ 実装済み | - | eval セグメントの 'git'/'push' トークン分離で `_push_has_force` が捕捉できず ask degrade。TC-F7/FG-L で固定 |
| 9 | `DANGER_OK=1` 前置時の escape（動的形・force・ラッパー隠蔽 force）が維持される | ✅ 実装済み | - | `danger_ok` フラグで `if not danger_ok:` ブロック全体をスキップ。TC-DOK1〜3 で確認 |
| 10 | false-green 注入で各判定行が load-bearing であることを対で裏取り | ✅ 実装済み | - | TC-FG-G〜L で全 6 判定分岐（`_push_has_force`/短縮クラスタ/`_push_is_dynamic`/eval/sh-c/alias）を `sed` 無効化＋対検証。ask 系は `rungj`/`runj` で stdout JSON 判別 |
| 11 | `.claude/settings.json` 不変。`docs/claude-code-structure.md` 限界注記を「対応済み」＋残余既知限界明記に更新 | ✅ 実装済み | - | diff に settings.json なし。claude-code-structure.md の旧注記を「I083 で対応済み」へ更新し、`eval "$VAR"` 完全隠蔽を残余限界として明記。TC-DOC1a/b/c で grep 決定論固定 |
| 12 | 既存 `test_pretooluse_push_guard.sh`（40件）・`test_pretooluse_checkout_guard.sh`（14件）無回帰 | ✅ 実装済み | - | I083 TC を追記形式で既存 TC（P1〜P33/Pregr1/FG-A〜F）は変更なし。checkout ガードも変更なし |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス | `_push_is_dynamic` の `if not toks: continue` ガードが (i) 直接 push チェックの後に来る。`_segments` が空セグメントを返さないため実害はないが、コードの読み手には (i) が空 toks で失敗するかのように見える。空チェックを (i) の前に移動すると意図が明確になる（機能上の問題なし）。 | `pretooluse_guard.py:181` | 任意改善（次機会） |
| Low | ベストプラクティス | `_is_force_flag` の単一ダッシュ短縮クラスタ判定 `"f" in tok[1:]` は `-rf`・`-xf` 等の非実在フラグも push セグメント内で force と判定する。コメントに「push セグメント内で -rf は無効構文＝実害なし」と記載済みだが、`-rf` 等の奇妙な入力が false-positive block になる（安全側の挙動）。文書化済みのため運用上問題なし。 | `pretooluse_guard.py:150` | 文書化済み・許容 |
| Low | テスト妥当性 | TC-D3（`D=develop; git push origin $D`）は `_push_is_dynamic` が `$D` の `$` をメタ文字として検出する経路で pass する。しかし shlex が `;` を分割しない fallback（`cmd.split()`）の場合、`_push_has_force` が後続トークンに `$D` を含むセグメントで `-f` 等を探してもマッチしない（問題なし）一方、`_push_is_dynamic` が `$` を含む after を検出するため結果は同じ。fallback 経路の明示的な TC はないが、設計上どちらの経路でも正しく ASK になるため Low 扱い。 | `test_pretooluse_push_guard.sh:108` | 情報共有のみ |

## 高リスク判定
判定: No
該当条件: なし（本件は Claude ハーネス安全機構の堅牢化。Django/React アプリコード・DB・認証・個人情報・マルチテナント境界の変更なし。変更方向はガードの「取りこぼし削減＋誤検知根治」であり権限を緩める変更を含まない。`allow: Bash(git push *)` は維持）

---
✅ コードレビュー完了。`/test [I083]` を実行してください。

VERDICT: OK
```
