> **【supersession 注記・2026-06-29】** 本記録の「全形を捕捉 ✅ / VERDICT OK」は**静的形に限る**。後続の敵対的レビュー・再レビューで、(R5) 動的宛先（`$(...)`/`$VAR`/`-c alias.`/`eval`・`sh -c`）と (R6/F1・High) force push の `-f`/複合コマンド取りこぼしが**フック・deny の双方を擦り抜ける**ことが判明し、当初の self-review「合格」は**撤回**された。最新の正本判定は `docs/reviews/open/I080_review.md`（条件付き合格＝静的形スコープ・残余は I083(#166) で根治）を参照。本ファイルの AC#4「全形」は静的形と読むこと。

```
# コードレビュー [I080] ── 2026-06-28 19:00

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | allow に `Bash(git push)`・`Bash(git push *)` が含まれ、ask から `Bash(git push *)` が除かれている | ✅ 実装済み | - | settings.json L40-41 に追加、ask セクションから削除確認済み |
| 2 | deny の保護ブランチ/force エントリが維持されている | ✅ 実装済み | - | L104-109: develop/main/HEAD/--force* 全エントリ維持 |
| 3 | settings.json が有効な JSON | ✅ 実装済み | - | 構造確認済み（TC-S1 で機械検証可） |
| 4 | フックが protected 宛先 push の全形を既定 exit 2 ブロック（明示名・引数なし保護ブランチ上・refspec・+develop・refs/heads/develop・HEAD:refs/heads/main） | ✅ 実装済み | - | `_push_protected_target` + `_norm_push_dest` でコードトレース確認。全形が正規化→完全一致で捕捉 |
| 5 | protected 宛先 push が `DANGER_OK=1` 前置で exit 0 | ✅ 実装済み | - | 全判定が `if not danger_ok:` 内に配置。TC-P6/P7/P23/P24/P26/P31 で検証 |
| 6 | 宛先正規化が完全一致で develop-fix/main-backup が exit 0（FP なし） | ✅ 実装済み | - | `dest in PROTECTED_BRANCHES` の完全一致。TC-P18/P19 で固定 |
| 7 | `git push --all`/`--mirror` が既定 exit 2・DANGER_OK で exit 0 | ✅ 実装済み | - | L234-235 の独立 regex チェック。TC-P21/P22/P24 + TC-FALSEGREEN-C/D |
| 8 | `--repo[=]origin ...`（evasion 形）が既定 exit 2・DANGER_OK で exit 0 | ✅ 実装済み | - | L239-240。TC-P28〜P32 + TC-FALSEGREEN-E/F |
| 9 | `--force`（非 protected）が従来どおり DANGER_OK 解除可 | ✅ 実装済み | - | TC-P25/P26 で確認 |
| 10 | feature 上の安全 push（素 push・`-u origin <feature>`・`<src>:<feature>`）が exit 0 | ✅ 実装済み | - | TC-P8〜P10/P18〜P20 で固定 |
| 11 | フック異常系テストが false-green でない（protected/--all/--mirror/--repo の各判定行無効化複製との差分で注入確認） | ✅ 実装済み | - | TC-FALSEGREEN A/B（protected）・C/D（--all/--mirror）・E/F（--repo）の3系統独立注入を実装 |
| 12 | `docs/claude-code-structure.md` が新仕様に同期（git push * を allow と記載・protected push の danger-op 化が反映） | ✅ 実装済み | - | L124 allow 列に追加・L133 ask 列から削除・L162 danger-op 説明段落追加 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス（セキュリティ） | `src:+dst` 形（dst 側 `+`）の refspec（例: `git push origin feat:+refs/heads/main`）は `_norm_push_dest` で検出されない。`token.startswith("+")` は token 先頭の `+` のみ除去し、コロン後の dst 側 `+` は除去しない。ただし本形式は計画書・AC に明示的に含まれておらず、イシューの「先頭 `+` 除去」定義に従った設計範囲内の仕様。実害は限定的（deny の `* --force*` との組み合わせで一部カバー、実運用では非常に稀な形）。 | `pretooluse_guard.py` `_norm_push_dest` | 次イシューで `_norm_push_dest` の dst 側 `+` 除去を追加検討（`":" in t` 分岐後に `t = t.lstrip("+")` を挿入するのみ） |
| Low | ベストプラクティス | `re.match(r"git\s+push\b.*--force", cmd, re.I)` は行頭マッチのため、`foo && git push --force origin feature` 形の複合コマンドでは force 判定が素通りする。一方で `_push_protected_target` は `_segments` を使い複合コマンドを正しく分割するため、protected 宛先への force push は保護される。非 protected 宛先への force push のみが影響を受ける（既存挙動・I080 スコープ外）。 | `pretooluse_guard.py` L243 | 現 I080 スコープ外。他の危険 push 判定（`_push_protected_target`・`--all`/`--mirror`・`--repo`）は `re.search` を使用しており非対称。別イシューで `re.search` への統一を検討 |

## 高リスク判定
判定: Yes
該当条件:
- ハーネスのツール実行権限変更（`git push *` を ask→allow 化）：Claude Code の認可権限の変更に相当
- セキュリティガード（`pretooluse_guard.py`）の既存 protected-push 判定ロジックを全置換するリファクタリング：実装ミスが即 protected push の素通りに直結

---
✅ コードレビュー完了。`/test [I080]` を実行してください。

VERDICT: OK
```
