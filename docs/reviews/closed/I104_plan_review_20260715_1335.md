コードの確認が完了しました。全ドキュメントをレビューします。

---

```
# 計画書レビュー [I104] ── 2026-07-15 14:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | P4 テスト妥当性 | false-green 注入検証の**記録先が不整合**。auto_test 文書に「結果を manual_test の該当行に記録する」と指定があるが、`I104_manual_test.md` に false-green 結果を記録する行・列が存在しない。実装時に証跡が残らないリスクがある | `docs/tests/open/I104_auto_test.md`「否定・回帰テストの false-green 自己検証」節 → `docs/tests/open/I104_manual_test.md`（対応行なし） | 修正推奨: manual_test に TC-INJECT-01（false-green 注入確認）行を追加するか、auto_test 内に記録セクションを設ける（実装前に対応） |
| Info | セキュリティ / テナント境界 | **クロステナント IDOR の明示的 TC が欠如**。既存の `get_queryset()` が `subject_service.get_user_subjects(request.user)` で組織フィルタリングを行っており、他組織 subject_id を指定すると 404 が返る防御は既存。ただし認可変更 PR として「admin が他組織 subject を PUT/DELETE → 404」を TC で固定する回帰確認があるとより堅牢。既存 ViewSet 修正のため Blocker とはしない | `docs/tests/open/I104_auto_test.md`（TC-AUTO-01〜05 に他テナント境界確認 TC なし） | 改善提案: `TC-AUTO-06`（他テナントの admin が subject_id 直打ち → 404）を任意追加 |
| Info | ベストプラクティス | **`@action(permission_classes=[AllowAny])` と overrideされた `get_permissions()` の相互作用が未検証**。DRF では `get_permissions()` が `self.action` ベースの明示的リストを返す場合、`@action` デコレータの `permission_classes` は自動適用されない（デコレータ属性は `self.permission_classes` を経由しないため）。実際、現行 `get_permissions()` の else 分岐では `public` action も `[IsAuthenticated()]` が返る。本 PR では経路不変のため I104 の正確性に影響しないが、既存の登録画面（未認証ユーザーの科目取得 `/api/subjects/public/`）が実際に動作しているかの確認を `/test` 時に追加することを推奨する | `backend/problems/views.py` L48（`@action`）/ L84-88（`get_permissions()` else 分岐） | 改善提案: `/test` 時に未認証で `GET /api/subjects/public/?slug=<slug>` を叩き 200 が返ることをスモークテストで確認（もし 401 なら pre-existing bug として別イシュー化） |
| Info | P4 テスト妥当性 | Risk 表の**「登録画面の科目取得を回帰確認可」が TC 未化**。Risk §9 に「手動でも登録画面の科目取得を回帰確認可」と記載があるが、`I104_manual_test.md` に該当 TC が存在しない。`public` の経路は不変のため実リスクは低い | `docs/plans/open/plan_I104.md` §9 Risk 表（回避策）→ `docs/tests/open/I104_manual_test.md`（TC 未化） | 改善提案: 必要なら `TC-MANUAL-06` を追加（未認証で登録画面に遷移し科目リストが正常表示される/または空リストで落ちない）。任意対応 |

## 高リスク判定
判定: Yes
該当条件: 認証・認可・ロール変更（`SubjectViewSet` の認可条件を変更する変更）
推奨: `/security-review [I104]` を実行してから `/implement [I104]` へ進んでください。

---

VERDICT: HIGHRISK
```

---

**補足メモ（レビュアーとして）**

- **コアロジックは正確**: 5-1 の `get_permissions()` 修正コード・5-2 の `<OrgAdminRoute>` ラップは受け入れ条件と1対1で対応しており、実装内容に曖昧さはない。
- **最も対処しやすい指摘は Warning**: `manual_test.md` に false-green 記録行（例: `TC-INJECT-01`）を1行追加するだけで解消できる。実装着手前に修正を推奨。
- **Info 3件はいずれも pre-existing または optional**: I104 の正確性・安全性を左右しないため、実装を阻まない。
- **高リスク判定について**: 「認可変更を厳格化する方向」であっても認可変更は高リスク区分。 `/security-review` で Security 観点の追加確認を推奨（I102 と同型のため短時間で完了するはず）。
