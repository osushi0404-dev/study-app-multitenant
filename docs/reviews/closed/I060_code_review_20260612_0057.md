以上で必要な確認が完了しました。

---

```
# コードレビュー [I060] ── [2026-06-12]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `CLAUDE.md` の参照先一覧が `docs/runbooks/` 配下の実ファイルと一致している（漏れ・誤参照ゼロ） | ✅ 実装済み | - | D1 適用済み。14本すべて列挙・実在確認（Glob による突き合わせ）。TC-01 コマンドも有効 |
| 2 | CLAUDE.md が参照する外部実体（hooks等）が現存し記述と整合している | ✅ 実装済み | - | D2 適用済み。pretooluse_guard.py（PreToolUse）・posttooluse_check.py（PostToolUse）を各1行で併記 |
| 3 | `docs/runbooks/*.md` 各ファイルが現行の `.claude/skills/` / `.claude/settings.json` / `docs/issues/templates/` 等の実体と整合している | ⚠️ 一部不足 | Medium | D5〜D14 の修正はすべて適用済み。D15「セマンティクス不変」方針で意図的に除外された review-rules.md が旧命名規則（`reviewXXX_IYYY.md`）を全面的に記述したまま（後述） |
| 4 | runbook 間の重複・矛盾箇所が解消され、片寄せ先が明示されている | ⚠️ 一部不足 | Medium | D8 で issue-flow.md 内を `IXXX_review.md` に統一済み。一方 review-rules.md は `reviewXXX_IYYY.md` 命名規則・存在しない `in_progress/` ディレクトリを記述しており inter-document 矛盾が残存 |
| 5 | 乖離一覧（修正前後の差分まとめ）が計画書または PR 本文に記録されている | ✅ 実装済み | - | plan_I060.md §3 に D1〜D15 の乖離一覧を記録 |
| 6 | 新規 runbook 追加時の CLAUDE.md 同期手順が `template-sync.md` に追記されている | ✅ 実装済み | - | D3 適用済み。ステップ2.5（bash コマンド付き）とチェックリスト項目を追記。D4 の tasks/templates → tests/templates 修正も確認 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | 要件適合性・相互整合性 | `review-rules.md` が旧命名規則（`reviewXXX_IYYY.md` / `reviewXXX_IYYY_post.md`）を全面的に記述し続けており、D8 で整備した `IXXX_review.md` 命名と矛盾する。加えて、同ファイルに `mv docs/reviews/open/... docs/reviews/in_progress/...` の移動手順があるが `docs/reviews/in_progress/` ディレクトリは実在しない（Glob で確認）。plan §5「セマンティクス不変」制約により D15 除外は理解できるが、AC4「runbook 間矛盾解消」が充足されていない。次イシューで `review-rules.md` の命名規則・ディレクトリ参照を実体（`IXXX_review.md`・`open/closed/` のみ）に合わせることを推奨 | docs/runbooks/review-rules.md（全体）| 次イシューで対応推奨 |
| Medium | リンク健全性（D15 対象） | `workflow.md`「ディレクトリ規約」（lines 17,20）が存在しない `docs/issues/in_progress` と `docs/reviews/in_progress` を列挙。workflow.md は本 PR で D9 変更を受けており D15「リンク健全性」の対象だったが、このデッドパス参照が見落とされた。実際に `in_progress/` ディレクトリは issues・reviews ともに存在しない（Glob 確認済み） | docs/runbooks/workflow.md:17,20 | 次イシューまたは次回 workflow.md 変更時に合わせて修正 |
| Low | ベストプラクティス | `plan-writing-rules.md`「計画書ヘッダー」テンプレート例（line 180）が `reviewXXX_IXXX` 旧形式のまま。読者が混乱する可能性あり。D15「セマンティクス不変」で除外は妥当だが、next issue でのフォローを記録しておく | docs/runbooks/plan-writing-rules.md:180 | 低優先度で対応 |
| Low | ベストプラクティス | D15 の "全文リライト" 対象 7 ファイル（danger-ops / plan-writing-rules / review-rules / ux-rules / mcp-github-setup / mcp-usage / branch-protection-setup）がいずれも diff にない。danger-ops・ux-rules・mcp-github-setup・mcp-usage・branch-protection-setup は内容を読み取り確認した結果、スタイル的な問題なしで変更不要と判断できる。plan-writing-rules も大部分は問題ないが上記 Low 指摘あり。review-rules は上記 Medium 指摘のとおり | D15 対象 7 ファイル | 問題なし（review-rules・plan-writing-rules は別途対応） |

## 高リスク判定
判定: No
該当条件: なし（Backend/Frontend/DB/設定ファイルへの変更なし。ドキュメント記述の正確化のみ）

---
✅ コードレビュー完了。`/test [I060]` を実行してください。
```
