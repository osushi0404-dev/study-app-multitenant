# イシュー自己完結度レビュー [I062]

## 判定: 十分

重要セクションにすべて具体的な記述があり、内容に重大な論理矛盾はない。問題の証拠・再現手順・根本原因・解決方針・実装対象・受け入れ条件がそろっており、文脈ゼロの担当者が `/plan-issue` に着手できる水準に達している。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 解決方針 | 「(a)/(b) いずれか／併用」と複数案が並存しており、`/plan-issue` 着手前に採用方針を確定させる判断が発生する。「(b) を主として (a) を保険に」と推奨方向は示されているため着手自体は可能だが、計画書で明示的に確定させること | 「解決方針 1」 |
| Info | 実装対象 | `find_file` の「連番最大を選ぶ」実装について、`plan_I062_9.md` と `plan_I062_10.md` が共存する場合の辞書順 vs 数値順が未指定。/plan-issue 段階で設計すれば良いが、受け入れ条件に「最新の計画書を読む」とあるため、「最新」の定義（= 連番最大 or mtime）を計画書で明記すること | 「実装対象」`find_file` 行 / 「受け入れ条件」3項 |
| Info | 解決方針 | plan 命名規約の「一本化（規約変更）」オプションと「スクリプト側修正」オプションの選択基準が解決方針 2 に明記されていない。制約セクションに「スクリプトを規約に合わせるのが基本」と示唆はあるが、明示的に方針 2 の採用方向を計画書で確定させること | 「解決方針 2」 |

---

## 次のアクションの推奨

判定は **十分** であり、`/grill-me` をスキップして `/plan-issue` に進める。
計画書作成時に以下の2点を明示的に決定・記述すること：
1. 方針 1 の採用形式（(a)+(b) 併用を確定させる）
2. `find_file` における「最新」の定義（連番数値順 or mtime、及び桁数対応の有無）
