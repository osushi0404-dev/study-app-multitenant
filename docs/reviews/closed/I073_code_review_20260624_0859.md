全ファイルの確認が完了しました。最終レビューを出力します。

---

```
# コードレビュー [I073] ── 2026-06-24 17:00

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 既存問題に画像を追加でき、保存後の再取得で反映される | ✅ 実装済み | - | `_reconcile_images` step4/5 + TC-AUTO-01 |
| 2 | 既存画像を削除でき、共有されていない場合はストレージ実体も削除される（共有時は紐づけのみ解除） | ✅ 実装済み | - | step6 共有チェック（`.exclude` バグ修正済み）+ TC-AUTO-03/05/11/15 |
| 3 | 既存画像を差し替えでき（削除＋追加）、結果が正しく反映される | ✅ 実装済み | - | TC-AUTO-11 |
| 4 | 画像の並び順を変更でき、`position` が再採番されて表示順に反映される | ✅ 実装済み | - | delete→recreate 方式 + TC-AUTO-04/10b |
| 5 | 問題画像・解説画像それぞれで上記が機能する | ✅ 実装済み | - | TC-AUTO-10（追加）+ TC-AUTO-10b（削除・並び替え）|
| 6 | 枚数上限（各5枚）を編集時も超過できない（超過時はバリデーションエラー） | ✅ 実装済み | - | `MAX_IMAGES_PER_KIND = 5` 定数 + TC-AUTO-08 |
| 7 | 編集処理はトランザクションで、途中失敗時に部分更新が残らない | ✅ 実装済み | - | `@transaction.atomic`（L228）+ 物理削除遅延（step4→5→6 順序）+ TC-AUTO-07 |
| 8 | 他組織ユーザーは対象問題を編集できない（queryset スコープにより 404） | ✅ 実装済み | - | TC-AUTO-06 |
| 9 | 上記を検証する決定論テストが追加され全て PASS する | ✅ 実装済み（実行未確認） | - | TC-AUTO-00〜15（10b 含む）。テスト結果欄は `/test` 前の正常状態として Low 以下扱い |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス / パフォーマンス | `handleUpdateProblem` が `appendImageOrder` を question・explanation の両方で**常に**呼ぶため、テキストのみ編集でも `*_images_order` が必ず送信される。バックエンドは `order is None=無変更` 最適化が使われず、変更のない usage_kind でも link delete→recreate が走る（max 5件×2）。機能的には正しいが不要な DB 操作が発生する。修正案: items が初期ロード時の `existing` のみで変更がない場合は `*_images_order` を送信しないガードを追加。**前回ラウンド2 Low の未修正継続**。 | `frontend/src/pages/QuizManagement.tsx` `handleUpdateProblem()` L360-361 | 改善提案（対応任意） |
| Low | ベストプラクティス | `parse_order` の `isinstance(raw, (list, dict))` が `dict` を通過させる。`_reconcile_images` の `isinstance(order, list)` チェックで後続 400 はされるが、エラーメッセージが `{field: ['不正なJSON形式です']}` でなく `"{usage_kind}画像の順序指定（order）が不正です"` になる。また辞書が JSON 文字列として届いた場合も `json.loads` が dict を返すと同様。型検証を `isinstance(raw, list)` に絞ることで前段でより明確なエラーを返せる。 | `backend/problems/views.py` `parse_order()` L316-321 | 改善提案（対応任意） |
| Low | ベストプラクティス | `perform_update` 内で `import json` をメソッドボディ内でインポートしている（stdlib モジュール）。既存の `perform_destroy` が `from problems.services import ProblemService` を内部インポートするパターンに倣った形だが、`json` は標準ライブラリなのでファイル先頭の import 群に移すのが通例。機能影響なし。 | `backend/problems/views.py` L287 `import json` | 改善提案（対応任意） |
| Info | 運用性 | subject が同組織内で変更された場合、キャッシュ無効化が新 subject（更新後）のみを対象としており、旧 subject のキャッシュが残る可能性がある。前ラウンドから継続して記録済みの既存系統問題。本 PR による回帰ではなくフォローアップ候補。 | `backend/problems/views.py` L337 `cache_service.invalidate_problems_cache(instance.subject.id)` | 情報提供（既存挙動） |

## 前回ラウンド2指摘の対応確認
| 前回指摘 | 重大度 | 対応状況 |
|---------|--------|---------|
| `_reconcile_images` step6 `.exclude(problem=problem)` が別usage_kind共有を誤除外するバグ | Medium | ✅ `.exclude` 除去済み（`filter(asset=asset, is_deleted=False).exists()` のみに修正）+ TC-AUTO-15 追加・auto_test.md にも記載 |
| `handleUpdateProblem` の catch が汎用トーストのみで DRF エラー詳細未表示 | Medium | ✅ `extractApiErrorMessage` ヘルパ追加・`toast.error(extractApiErrorMessage(error, ...))` で詳細表示 |
| `appendImageOrder` の無条件呼び出しによる不要な order 送信 | Low | ⚠️ 未修正（今回も Low として継続記録） |
| 保存ボタン `disabled={isSubmitting}` 未設定による重複送信リスク | Low | ✅ `disabled={isSubmitting}` 実装済み（L893） |

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test [I073]` を実行してください。

VERDICT: OK
```
