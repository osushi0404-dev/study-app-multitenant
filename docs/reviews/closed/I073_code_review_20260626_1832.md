必要な情報がすべて揃いました。フォーマット通りに出力します。

---

```
# コードレビュー [I073] ── 2026-06-26 00:00

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 既存問題に画像を追加でき、保存後の再取得で反映される | ✅ 実装済み | - | `_reconcile_images` step4/5 + TC-AUTO-01 / TC-AUTO-17（キャッシュ経路越し） |
| 2 | 既存画像を削除でき、共有されていない場合はストレージ実体も削除される（共有時は紐づけのみ解除） | ✅ 実装済み | - | step6 共有チェック（`.exclude(problem)` バグ修正済み）+ TC-AUTO-03/05/11/15 |
| 3 | 既存画像を差し替えでき（削除＋追加）、結果が正しく反映される | ✅ 実装済み | - | TC-AUTO-11 |
| 4 | 画像の並び順を変更でき、`position` が再採番されて表示順に反映される | ✅ 実装済み | - | delete→recreate 方式 + TC-AUTO-04/10b。UNIQUE 制約回避を確認。 |
| 5 | 問題画像・解説画像それぞれで上記が機能する | ✅ 実装済み | - | TC-AUTO-10（追加）+ TC-AUTO-10b（削除・並び替え）|
| 6 | 枚数上限（各5枚）を編集時も超過できない | ✅ 実装済み | - | `MAX_IMAGES_PER_KIND = 5` 定数 + TC-AUTO-08 |
| 7 | 編集処理はトランザクションで、途中失敗時に部分更新が残らない | ✅ 実装済み | - | `@transaction.atomic`（L228）確認済み。物理削除遅延順序（step4→5→6）+ TC-AUTO-07 |
| 8 | 他組織ユーザーは対象問題を編集できない | ✅ 実装済み | - | queryset 組織スコープ + TC-AUTO-06 |
| 9 | 上記を検証する決定論テストが追加され全て PASS する | ✅ 実装済み | - | TC-AUTO-00〜17（10b 含む）18件 PASS。backend 全体43 PASS。false-green 自己検証（02/04/05/06/07/09/12/13/14/15 故障注入）記録済み。 |

## コード品質・セキュリティ指摘

### P1. 要件適合性・業務ロジックの正しさ
問題なし。イシュー「設計確認メモ」で確定した multipart 契約（`*_image_i` / `*_images_order` の `{existing}|{new}`、暗黙削除、上限5枚）を正確に実装。`order is None=無変更 / []=全削除` の二値仕様が `_reconcile_images`（`if order is None: return`）と `appendImageOrder`（openEditDialog で既存画像を items に読み込み、常に order を送信）で一貫している。

### セキュリティ
問題なし。
- SEC-1（subject 他組織付け替え拒否）: `perform_update` の先頭で `new_subject.organization_id != request.user.organization_id` を検証し 400 拒否。`ProblemService.update_problem_with_images` 呼び出し前に実行されるため物理ファイルへの書き込みは発生しない。TC-AUTO-12 で「他組織 subject 指定→400 かつ org2 ディレクトリにファイル不在」を確認済み。
- 画像保存の org/subject は `problem.subject`（永続化済み）を権威とし、`validated_data['subject']` から導出しない（L264 `authoritative_subject = problem.subject` が `problem.save()` 前に取得）。
- IDOR: `existing` UUID を当該問題の現在 link 集合（`current_assets`）に対して検証し、非メンバーは 400 拒否（TC-AUTO-09）。
- テナント境界: queryset `subject__organization == request.user.organization` で他組織問題は 404（TC-AUTO-06）。
- パストラバーサル: `sanitize_filename` / `get_storage_path` 既存対策を `_save_image_as_asset` 経由で再利用。
- 大量代入: `serializer.save()` を呼ばず `ProblemService` 経由で更新するためシリアライザの read_only 制約は直接影響しない。service 内で `choices_data = validated_data.pop('choices', None)` + 残フィールドを `setattr` する方式は既存踏襲。
- DEFAULT_PERMISSION_CLASSES が空の懸念: `ProblemViewSet` には既存の `permission_classes = [IsAuthenticated]` があることを前ラウンドで確認済み。

### ベストプラクティス
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス / パフォーマンス | `handleUpdateProblem` が `appendImageOrder` を question・explanation の両方で**常に**呼ぶため、テキストのみ編集でも `*_images_order` が必ず送信される。バックエンドの `order is None=無変更` 最適化が使われず、変更のない usage_kind でも link delete→recreate が走る（max 5行×2）。機能的に正しく、計画§13で「冪等で正しさ優先」として意図的に採用された設計判断（変更検知の誤りによる無言無視を避けるため）。前ラウンド3でも Low として記録。 | `QuizManagement.tsx` `handleUpdateProblem()` L360-361 | 改善提案（対応任意・設計判断として許容済み） |

キャッシュ無効化修正（`0e4aa69`）について: `_delete_by_pattern` に KEY_PREFIX を手書きして二重付与になっていたバグを `"problems_*"` 一本化で修正。`_delete_by_pattern` に「パターンに KEY_PREFIX を含めない」契約 docstring を追記。TC-AUTO-16（`cache_service` 直接）/ TC-AUTO-17（API 越し結合）で旧実装に戻すと FAIL することを故障注入で確認済み（false-green なし）。CI に Redis サービスを追加（`7c0727c`）しキャッシュ挙動を CI 環境でも検証可能にした点も適切。

### モダンなウェブアプリ開発
問題なし。
- N+1: `select_related('asset')` で current link 取得（L300）。delete→recreate は最大10行。共有チェックは削除候補数（最大5回）。
- ローディング/エラー状態: `disabled={isSubmitting}` による二重送信防止（L893）。`extractApiErrorMessage` ヘルパで DRF バリデーションエラー詳細をトースト表示。
- アクセシビリティ: ↑/↓/削除ボタンに `aria-label="上に移動"/"下に移動"/"削除"` 付与済み（L319/326/333）。
- Object URL メモリリーク対策: `useMemo` で生成・`useEffect` cleanup で `URL.revokeObjectURL` を呼ぶパターンが実装されている（ImageUploadArea.tsx L201-228）。

### P4. テスト妥当性・回帰防止
問題なし。
- バグ修正（編集で画像が無視される）の再発防止: TC-AUTO-01〜11 追加。キャッシュ無効化バグの再発防止: TC-AUTO-16/17 追加。
- 認可テスト: TC-AUTO-06（他組織 404）/ TC-AUTO-09（他問題アセット混入 400）/ TC-AUTO-12（越境 subject 400）が独立したテストケースとして存在。
- false-green 自己検証: 否定・認可系 TC（02/04/05/06/07/09/12/13/14/15/16/17）に故障注入し全件 FAIL を確認（`I073_auto_test.md` 記録済み）。
- テスト結果欄: `backend 18/18 PASS・全体43 PASS・frontend 7 PASS` が記録済み。

### P3. データ整合性・変更安全性
問題なし。スキーマ変更なし。トランザクション境界は `@transaction.atomic`（L228）で `update_problem_with_images` 全体を一括。物理ファイル削除は非可逆のため `_reconcile_images` 手順4（新規保存）→5（link 再構築）→6（最後に物理削除）の順序を厳守し、途中失敗時に物理削除へ到達しない設計を実装。`ProblemMediaAsset` の `(problem, usage_kind, position)` UNIQUE 制約は delete→recreate で回避。

### P5. 運用性・障害対応性
問題なし。物理削除失敗は `logger.warning` + `extra` に `problem_id`/`organization_id`/`asset_id`/`storage_key`/`error` を含む構造化ログで記録し処理続行（`delete_problem` 踏襲）。

### P8. コスト・保守負荷
問題なし。`_save_image_as_asset` 抽出で作成・更新フローの画像保存ロジックを DRY 化（C7）。`MAX_IMAGES_PER_KIND = 5` 定数でマジックナンバー排除。依存ライブラリ追加なし。

### P9. プライバシー・コンプライアンス
対象外。個人情報・未成年データ・テナント越境なし（画像コンテンツのみ、組織内閉鎖）。

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test [I073]` を実行してください。

VERDICT: OK
```
