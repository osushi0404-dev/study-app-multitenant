4ファイル＋既存ソース2ファイルをすべて読了しました。レビューを開始します。

---

```
# 計画書レビュー [I095] ── 2026-07-02 17:15

## 判定: ✅ 完了（Blocker 0件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | `_bash_write_targets` が処理する「付着形リダイレクト」（スペースなしの `echo x>/path/f` → shlex.split では `['echo', 'x>/path/f']` の単一トークンになる）の抽出アルゴリズムが計画書§4-1 に明示されていない。TC-A11 はスペース区切り形（`echo x > <別wt>/f`）と `>|<別wt>/f` の先頭付き形を対象にしているが、`echo x>/path/f` のように前トークンに`>`が付着するケースのテストが不在で実装が黙って素通しになるリスクがある。実装は可能だが、トークンへの埋め込みリダイレクト抽出ロジックを§4-1 または実装ノートに追記することを推奨。 | 計画書§4-1 `_bash_write_targets`・自動テスト TC-A11 | 修正推奨 |
| Warning | テスト妥当性 | TC-A22 の期待結果が「0（block しないが警告出力・grep で確認）」のみで、grep するパターン文字列が未指定。§4-2 に「`worktree ガードは絶対パス宛先のみ検査。cd/変数越しは非対象`」と出力文字列が記載されているが、テスト文書に転記されていない。grep パターン未指定のまま実装するとパターン選択次第で false-green になりうる。 | 自動テスト TC-A22 | 期待grep文字列を自動テスト文書に明記推奨 |
| Warning | テスト妥当性 | TC-M3 の期待結果「ブロックされる（双方向に効くこと）」が TC-M1 と比べて具体性を欠く。TC-M1 は「`[guard]` メッセージ付き」「ファイルの mtime・内容が不変」と観察方法まで指定しているが、TC-M3 には確認手段（ファイル不変確認・メッセージ有無）の記載がない。 | 手動テスト TC-M3 | 期待結果の具体化推奨 |
| Warning | テスト妥当性 | TC-M5 の期待結果「従来どおり成功する（誤ブロック無し）」が観察可能な確認方法（ファイルが変更されたことを mtime/内容で確認等）を欠く。 | 手動テスト TC-M5 | 具体的な確認手順の追記推奨 |
| Info | BP | `_edit_path(data)` が None/空を返す場合（MultiEdit/NotebookEdit でフィールド名が異なるなど予期外のケース）に `if path:` ガードで worktree チェックが黙ってスキップされ fail-open になる。実用上は Edit/Write/MultiEdit/NotebookEdit は常にパスフィールドを持つため実害なしだが、パス未取得時も fail-safe (block) にすることでより堅牢になる。 | 計画書§4-2 `main()` 分岐 | 改善提案（対応任意） |

## 補足（問題なし確認事項）
- `_segments()` は既存ソース（l.27-44）に実装済み。計画書の「追加関数」リストに不在だが欠落ではなく既存関数の活用。✅
- スパイク実証（worktree 列挙・境界一致・兄弟名 decoy）が in-session で全 PASS 確認済み。✅
- MultiEdit/NotebookEdit のフック発火（実証不能クラス）はステップ1のプレーン新規セッション目視（TC-M1/M2）に前倒しされており、条件(c)「実証不能クラスで指定実施環境＋ステップ1への前倒し明記済み」に該当。✅
- 決定論ゲートの7コマンドはすべて1行1コマンド（チェーン無し）。コマンド衛生OK。✅
- 境界一致を `realpath + os.sep` 接頭辞一致で行う設計はスパイク実証済みで、兄弟ディレクトリ名の部分一致誤判定を防止。アンカー一致原則に準拠。✅
- fail-safe（git 失敗時ブロック）・エスケープ無し（DANGER_OK=1 で解除しない）の設計がイシューの grill-me 確定事項と整合。✅
- 未カバー経路（cd/eval/変数越し）の no-silent-caps 対応（stderr 警告出力）が TC-A22 で検証される設計になっている。✅
- 影響範囲（4ファイル）がイシューの「実装対象」と完全一致。✅
- P3/P5/P8/P9 は本変更の対象外（Django/React/DB/外部API変更なし・ハーネス整備のみ）。✅
- Claude Code BP: 変更対象に `.claude/skills/` 等の指示ファイルを含まないため主要チェック項目は対象外。settings.json の変更（PreToolUse マッチャ明示化）は最小権限の方向性と整合。✅

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement [I095]` を実行してください。

VERDICT: OK
```
