すべてのファイルを読み込みました。レビューを開始します。

---

```
# 計画書レビュー [I074] ── 2026-07-05 14:35

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP（Claude Code BP） | TC-17 の grep パターン `tools[^\n]*(Write\|Edit\|Bash\|MultiEdit)` は、スクリプトや reviewer 定義ファイルのコメント行に `--tools` と `Write`/`Edit` 等が同一行に登場する場合（例: `# --tools に Write を含めない`）に false-positive を返す可能性がある。実ファイルの期待コメント構造では発生しにくいが、テスト実装時に `--tools\s+"[^"]*(?:Write\|Edit\|Bash\|MultiEdit)` 等、`--tools` 引数の引用符内限定に絞るとより堅牢になる | `I074_auto_test.md` TC-17 | 修正推奨 |
| Info | BP（Claude Code BP） | SKILL.md コマンド例の変数表記について: 既存の手順7は `$ARGUMENTS` を使用している（`docs/tests/open/$ARGUMENTS_auto_test.md`）が、新手順3.5/5.5/6.5 の計画記述は `I###` を「実イシュー番号に置換」と説明するに留まり、`$ARGUMENTS` を明示していない。実装者が `I###` リテラルを残したまま commit するリスクは低いが、手順7との記法パリティを `$ARGUMENTS` 統一で明示すると、実装ブレが一層防止できる | `plan_I074.md` §4-4「コマンド例の実値化」 | 改善提案 |
| Info | BP（Claude Code BP） | `fix-review-lib.sh` の `source ガード`（`if [ "${REVIEW_LIB_SOURCE_ONLY:-}" = "1" ]; then return 0; fi`）を「全関数定義の**後**・本体実行コードの**前**」に置く旨を計画書が明記している点は正しい。一方、実装時にこの順序を誤ると TC-01（`declare -F` で関数存在確認）が silent fail になる（`source` が途中でリターンし関数が未定義になる）。計画書に明記済みだが、実装チェックリストでの再確認を推奨 | `plan_I074.md` §4-1 `source ガード` | 改善提案 |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I074` を実行してください。

---

### 観点別詳細メモ（問題なし箇所の確認記録）

**P1. 要件適合性・業務ロジックの正しさ**
- AC 全13項目を計画書 §2〜§4-4 が網羅している（手順3.5/5.5/6.5・ゲートしきい値・差し戻し強制形態・ループ暴走ガード・軽微例外条件・診断全案+推奨評価・claude -p 不可 skip・既存手順1〜7 非後退）。スコープの超過・変更なし。問題なし。
- 既存 SKILL.md の「手順6 成功→手順7 へ」という消費箇所を「手順6.5 へ→手順7 へ」に明示的に書き換える旨が §4-4 に記載されており、概念変更の消費箇所更新が担保されている。問題なし。
- `verdict_to_gate` の「それ以外(`OK`)→PASS」設計は、`detect_fix_verdict` が BLOCKER/HIGH/OK/空文字のみを返す仕様と整合しており、未知値の素通りが生じない。問題なし。

**ベストプラクティス**
- `allowed-tools` は既存 fix-loop の `Read, Bash, Write, Edit, Glob, Grep` を維持（fix-loop はコード修正に Write/Edit が必要）。3 reviewer スクリプトは別途 `--tools "Read,Grep,Glob"` で最小権限を分離している。問題なし。
- `disable-model-invocation: true`・`argument-hint: "I###"` は既存のまま。問題なし。
- 分岐パリティ（P1）: §4-4 が既存手順と同粒度で各分岐（exit 0/exit 1/SKIP）を具体コマンド付きで記述し、R4 でパリティ確認をステップ4 に明記。問題なし。
- コマンド衛生: 手順5.5 の fail-safe 再判定で `git diff --name-only` 等を「各単体コマンド・パイプ複合しない」と明記。MEMORY 準拠。問題なし。
- 破壊的取り消し: `git checkout/restore` 等の操作なし。問題なし。
- 外部挙動依存（`claude -p`・`timeout`）: 既存 4 review スクリプトで `claude -p` パターンは実証済み。`timeout` の POSIX exit 124 動作も標準。未実証前提なし。問題なし。
- `REVIEW_LIB_SOURCE_ONLY=1 source` テストパターンは既存 `plan-issue-review.sh`（line 88）で実証済み。問題なし。

**セキュリティ**
- 3 reviewer スクリプトは `--tools "Read,Grep,Glob"` のみ（Write/Edit/Bash を与えない）。auto_test TC-17 で書込ツール非混入を回帰テスト。問題なし。
- reviewer 定義に `<instructions>` ブロックで prompt-injection ガード（diff/メモはデータ扱い）を明記。issue-reviewer.md 踏襲。問題なし。
- 新規 Web エンドポイントなし。OWASP 非該当。`DEFAULT_PERMISSION_CLASSES` 非該当。問題なし。

**モダンなウェブアプリ開発**
- Backend/Frontend/DB 変更なし。対象外。

**Claude Code ベストプラクティス（指示ファイル変更含む）**
- description（250字以内）・argument-hint・disable-model-invocation・allowed-tools: 既存設定維持・問題なし。
- 停止条件: 診断 NG でユーザー停止・連続 NG 2回でエスカレーション・手順4 承認待ちが明確な停止条件として機能。問題なし。
- SKILL.md 500行以内: 現行 31行、追記後も推定 80〜130行程度。問題なし。
- 設計メモ↔本文整合（P3 backstop）: イシュー「実装対象」テーブルに対し計画書が `fix-review-lib.sh` と `test_fix_review.sh` を追加しているが、設計確認メモ（grill-me）の「1スクリプトをパラメータ化でも可」から導出した設計選択として §設計判断表 で明示・正当化されており、矛盾なし。問題なし。

**P4. テスト妥当性・回帰防止**
- TC-05: VERDICT 行不在→空文字（false-green 回避）。問題なし。
- TC-07b: `| tail -1` の実証（中間行に正規 VERDICT があっても末尾を採る）。問題なし。
- TC-12/13: HIGH → REMAND の反証（壊した複製と正規の差分で実体を確認）。問題なし。
- TC-24: 回帰 false-green 反証（キーワード削除複製で NG を確認）。問題なし。
- 手順6 失敗→手順1、テスト NG→手順5/6 の差し戻し連鎖が設計メモで確定しており、テストレビューのスコープ（テスト十分性専用）と実装レビューのスコープ（方針整合専用）が重複なく分離されている。問題なし。

**P3. データ整合性・変更安全性**
- DB 変更なし。対象外。

**P5. 運用性・障害対応性**
- 各レビューが `docs/reviews/I###_fix_{kind}_review_<ts>.md` に記録・path-scoped commit（skip 時も「レビュー未実施」を記録し監査証跡）。問題なし。
- `timeout ${FIX_REVIEW_TIMEOUT:-300}` で claude -p ハングを阻止し、超過を SKIP に落としてループを止めない。環境変数で調整可能・冒頭コメントで説明。問題なし。
- ロールバック: 新設ファイル削除 + SKILL.md revert で完全復元可能。問題なし。

**P8. コスト・保守負荷**
- 最大3サブエージェント×ループ回数だが、連続 NG 上限=2・軽微例外簡略化・診断常時必須のみで他は条件付き。I073 の手戻りコストに対し妥当。問題なし。

**P9. プライバシー・コンプライアンス**
- 個人情報・テナントデータを扱わない。対象外。

VERDICT: OK
```
