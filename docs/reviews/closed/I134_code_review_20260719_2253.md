## 決定論ゲート実行結果（code-review.sh 自動注入）

- `grep -q '^\*\*背景\*\*:' docs/issues/templates/issue_template.md` → exit=0 ✅
- `grep -q '^\*\*目的\*\*:' docs/issues/templates/issue_template.md` → exit=0 ✅
- 高リスク判定: YES（LLM=YES / path=YES）

- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

必要な確認が完了しました。レビューを出力します。

---

```
# コードレビュー [I134] ── [2026-07-19 23:30]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | AC-1: `issue_template.md` の `## 背景/目的` 直下にプレースホルダ 2 行が存在する | ✅ 実装済み | - | 決定論ゲート exit=0（2 行とも）。Read で直接確認: 8-9 行目に両行存在・他行不変 |
| 2 | AC-2: テンプレートから新規作成したイシューで背景・目的の未記入が構造的に起きない | ✅ 実装済み | - | プレースホルダが `（…）` 形式のため issue-reviewer.md の未記入判定に載る。AC-1 成立で構造成立（計画「消費箇所」確認済み） |
| 3 | AC-3: wt-harness の `docs/issues/open/*.md` 全件にラベル 2 行が存在することを機械検証済み | ✅ 実装済み | - | Grep による独立検証: `**背景**:` 27 件・`**目的**:` 27 件（sweep 後に作成された I139 を含む全件）。TC-02 実施記録 exit 0。tracked 12 件は diff で挿入のみを確認済み |
| 4 | AC-4: GitHub open イシュー全 41 件の本文にラベル 2 行が反映済み | ✅ 実装済み | - | TC-03 実施記録 exit 0（total=41 missing=0）。formal gate 非宣言のため静的検証不可。実施者記録を根拠とする |
| 5 | AC-5: 隣 worktree にのみ存在する 25 件への反映手順が計画書に引き継ぎとして記載済み | ✅ 実装済み | - | plan_I134.md「引き継ぎ手順」: スクリプト全文＋4 件スニペット（I074/I086/I114/I122）を diff で確認済み |
| 6 | 追加ゲート: 既存本文の無改変（挿入のみ・削除ゼロ）を機械検証済み | ✅ 実装済み | - | TC-04/05/06 実施記録 exit 0。diff 上 tracked 12 件はすべて `+` 行のみ（`-` 行なし）で目視確認と整合 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | P8（ドキュメント整合性） | fix-loop のテストレビュー記録ヘッダが `# テストレビュー [I074]` になっており、正しくは `[I134]` | `docs/reviews/I134_fix_test_review_20260719_2242.md` L3 | 運用上の影響なし（ファイル名・内容は I134 で正しい）。次回同型ファイル作成時の参考として記録 |

## 高リスク判定
判定: Yes
該当条件: 【harness 固有】`scripts/claude/check-issue-background.sh` 新規追加（`scripts/claude/` 配下スクリプトの変更）

---
✅ コードレビュー完了。`/test [I134]` を実行してください。

RISK: YES
VERDICT: OK
```
