ここまでの情報をもとにレビューをまとめます。

```
# コードレビュー [I073] ── 2026-06-24 14:00

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 既存問題に画像を追加でき、保存後の再取得で反映される | ✅ 実装済み | - | `_reconcile_images` step4/5 + TC-AUTO-01 |
| 2 | 既存画像を削除でき、共有されていない場合はストレージ実体も削除される（共有時は紐づけのみ解除） | ✅ 実装済み | - | step6 共有チェック + TC-AUTO-03/05/11。ただし共有チェックに Medium 指摘あり（後述） |
| 3 | 既存画像を差し替えでき（削除＋追加）、結果が正しく反映される | ✅ 実装済み | - | TC-AUTO-11 |
| 4 | 画像の並び順を変更でき、`position` が再採番されて表示順に反映される | ✅ 実装済み | - | delete→recreate 方式 + TC-AUTO-04/10b |
| 5 | 問題画像・解説画像それぞれで上記が機能する | ✅ 実装済み | - | TC-AUTO-10（追加）+ TC-AUTO-10b（削除・並び替え）で前回 Low 指摘を解消 |
| 6 | 枚数上限（各5枚）を編集時も超過できない（超過時はバリデーションエラー） | ✅ 実装済み | - | `MAX_IMAGES_PER_KIND` 定数 + TC-AUTO-08 |
| 7 | 編集処理はトランザクションで、途中失敗時に部分更新が残らない | ✅ 実装済み | - | `@transaction.atomic` + 物理削除遅延（step4→5→6 順序） + TC-AUTO-07 |
| 8 | 他組織ユーザーは対象問題を編集できない（queryset スコープにより 404） | ✅ 実装済み | - | TC-AUTO-06 |
| 9 | 上記を検証する決定論テストが追加され全て PASS する | ✅ 実装済み（実行未確認） | - | TC-AUTO-00〜14（TC-AUTO-10b 含む）。前回 Medium/Low 指摘分（TC-13/14/10b）追加済み。テスト結果欄は `/test` 前の正常状態として Low 以下扱い |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | データ整合性 | `_reconcile_images` step6 の共有チェックに誤りがある。`.exclude(problem=problem)` は step5 で削除した `(problem=P, usage_kind=usage_kind)` リンク以外に、同一 problem の **別 usage_kind リンク**（step5 では削除していない）も除外してしまう。例: 同一 MediaAsset が problem P の 'problem' と 'explanation' 両方に紐づいている場合、'problem' kind を編集して削除しようとすると、'explanation' リンクが `.exclude` で除外されて `shared=False` となり物理削除が走り、問題の解説画像が 404 になる。前回レビューの Low #3（「冗長」）はコメント更新で対応されたが、`.exclude(problem=problem)` の誤動作自体は修正されていない。修正案: step5 で当該 `(problem, usage_kind)` リンクは既に全削除済みなので、`exclude` は不要。`shared = ProblemMediaAsset.objects.filter(asset=asset, is_deleted=False).exists()` で十分かつ正確。なお同一アセットが複数 usage_kind に紐づくケースは通常 UI では発生しにくいが、モデル制約（`unique_together=['problem','asset','usage_kind']`）上は許容されており、旧 `upload_image` API 経由の既存データでは起こり得る。 | `backend/problems/services/problem_service.py` `_reconcile_images()` L393-395 | 修正推奨（Medium） |
| Medium | モダンウェブアプリ / UX | 計画書§13「API 400 はトーストでメッセージ表示（枚数上限・不正order）」が `handleUpdateProblem` で実装されていない。catch ブロックが `toast.error('問題の更新に失敗しました')` の汎用メッセージのみで、APIレスポンスの具体的なバリデーションエラー（「最大5枚まで」「不正なJSON形式です」等）がユーザーに伝わらない。修正案: `axios` の error レスポンスから `error.response?.data` を取り出し、DRF の `ValidationError` 形式を適切にパースして toast 表示する。既存の `handleCreateProblem` も同様だが、本 PR の計画書で明示的に改善を約束しているため Medium とする。 | `frontend/src/pages/QuizManagement.tsx` `handleUpdateProblem()` L355-358 | 修正推奨（Medium） |
| Low | ベストプラクティス / パフォーマンス | `handleUpdateProblem` は `appendImageOrder` を `question`・`explanation` の両方で常に呼ぶため、テキストのみ編集した場合でも両 usage_kind の `*_images_order` が常に送信される。結果としてバックエンドの `order is None = 無変更` 最適化が使われず、変更のない usage_kind でも link の delete→recreate が走る（最大5件×2）。機能的には問題ないが、不要な DB 操作と「無変更」仕様の非活用が残る。修正案: `items` が初期状態（既存のみ、変更なし）の場合は `*_images_order` を送信しない、または order を送信しない条件分岐を追加する。 | `frontend/src/pages/QuizManagement.tsx` `handleUpdateProblem()` L345-346 | 改善提案（対応任意） |
| Low | モダンウェブアプリ / UX | 計画書§13「編集保存中はダイアログの保存ボタンを無効化（既存パターン踏襲）」が未実装。`handleUpdateProblem` の非同期処理中もボタンが有効なため重複送信のリスクがある。既存の `handleCreateProblem` も同様で `disabled` 制御なし。`react-hook-form` の `formState.isSubmitting` を使って Submit ボタンに `disabled={formState.isSubmitting}` を追加するとよい。 | `frontend/src/pages/QuizManagement.tsx` `DialogActions` 保存ボタン L877-880 | 改善提案（対応任意） |
| Info | 運用性 | subject が変更された場合、キャッシュ無効化が新 subject のみを対象としており、旧 subject のキャッシュが残る（問題一覧に古い情報が残る可能性）。前回レビューで既に Info として記録済みの既存系統問題。本 PR による回帰ではないためフォローアップイシュー候補。 | `backend/problems/views.py` L337 | 情報提供（既存挙動） |

## 前回レビュー指摘の対応状況確認
| 前回指摘 | 重大度 | 対応状況 |
|---------|--------|---------|
| URL.createObjectURL メモリリーク | Medium | ✅ `useMemo` + `useEffect` revoke で正しく修正済み |
| 同一 existing UUID 重複で 500 | Medium | ✅ TC-AUTO-13 追加 + step3 重複チェック実装で修正済み |
| existing/new 同時指定で孤児アセット | Low | ✅ TC-AUTO-14 追加 + step3 両キーチェック実装で修正済み |
| TC-AUTO-10 の解説画像 削除・並び替えカバー不足 | Low | ✅ TC-AUTO-10b `test_explanation_delete_and_reorder` で追加済み |
| `.exclude(problem=problem)` コメント更新 | Low | ⚠️ コメントは更新されたが、underlying の誤動作（同一 problem 別 usage_kind リンクを誤除外）は未修正。今回 Medium として再指摘 |

## 高リスク判定
判定: No
該当条件: なし

---
Medium が 2 件ありますが Blocker・High はありません。
「✅ コードレビュー完了。`/test [I073]` を実行してください。」

ただし、Medium #1（`.exclude` 共有チェックバグ）は `StorageService.delete_file` による非可逆操作に関わるため、`/test` 実行と並行して修正することを推奨します。

VERDICT: OK
```
