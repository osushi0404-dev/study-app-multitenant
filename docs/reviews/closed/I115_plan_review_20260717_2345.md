全4ファイルの読み込み完了。レビューを実施します。

---

```
# 計画書レビュー [I115] ── 2026-07-17 16:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | テスト妥当性 | TC-AUTO-02 が TC 一覧に独立した行として列挙されながら「独立 TC ではなく TC-AUTO-01 の実装前実行を指す」と説明されている。TC 表を読んだ実装者が「TC-AUTO-01 と別の独立テスト関数を書くべきか」と誤解するリスクがある。TDD RED 確認セクションで補足されているため実装は可能だが、TC 表の行自体を「（TDD RED 確認用・実装前に TC-AUTO-01 を先行実行）」等の注記に変えるか、TC 表から外して TDD RED 確認セクションのみに記述を集約する方が明確 | `docs/tests/open/I115_auto_test.md` TC-AUTO-02 行 | 修正推奨 |
| Info | 要件適合性 | `slug` 未指定 → `'personal'` 変換・`'register'` → `'personal'` 変換（レガシー対応）の挙動が「既存挙動不変」の範囲に含まれるが、AC・TC のいずれにもこれらのケースが明示的に列挙されていない。返却コード不変更なので回帰リスクは低いが、`test_I115_public_unauth.py` に TC-AUTO-03 の亜種として1ケース追加しておくと完全 | `docs/tests/open/I115_auto_test.md` TC-AUTO-03（parametrize は `slug=no-such-org-i115` のみ） | 改善提案（対応任意） |

## 高リスク判定
判定: Yes
該当条件:
- 認証・認可・ロール変更（`public` の権限が `IsAuthenticated` → `AllowAny` へ変更。設計意図の復旧だが権限クラス変更に該当）
- 外部公開 API 追加・変更（未認証でアクセス可能なエンドポイントの復旧）

補足: 計画書自体は高リスク「No」と自己評価しており、その根拠（元々 `@action(AllowAny)` で公開が宣言されていた復旧・科目名/説明のみ・他 action 不変・回帰テスト固定）は合理的かつ詳細に説明されている。文書品質・リスク制御ともに高水準であるため security-review は形式的な確認にとどまる可能性が高い。

推奨: `security-review [I115]` を実行してから `/implement [I115]` へ進んでください。
     （ただし計画書の認可マトリクス・OWASP チェック・テナントスコープ固定は既に充分に記述されており、security-review での主な確認項目は AC 上のリスク3〔スクレイピング/列挙〕のレート制限方針の確認に限定される見込み）

VERDICT: HIGHRISK
```
