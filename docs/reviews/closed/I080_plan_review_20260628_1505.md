全ファイル確認完了。レビューを実施します。

---

```
# 計画書レビュー [I080] ── 2026-06-28 14:05

## 判定: 差し戻し（Blocker 1件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | P4 テスト妥当性 | `--all`/`--mirror` 判定の false-green 注入 TC が auto_test.md に欠落。イシュー AC「protected 宛先 push・`--all`/`--mirror` で実際に非ゼロ終了することを判定行無効化複製との差分で注入確認」は両方の注入を要求しているが、TC-FALSEGREEN-A/B は `_push_protected_target` の無効化のみ対象。`re.search(r"\bgit\s+push\b.*\s(--all\|--mirror)\b", ...)` という独立 regex チェックの「判定行を外すと素通りになる」裏取りが存在しない。`_push_protected_target` を None に差し替えても `--all`/`--mirror` の regex は影響を受けないため、現 TC-FALSEGREEN では `--all`/`--mirror` 判定の false-green を機械的に証明できていない。 | `docs/tests/open/I080_auto_test.md` TC-FALSEGREEN-A/B（`--all`/`--mirror` 向け TC-FALSEGREEN-C/D が欠落）、イシュー AC「`--all`/`--mirror` で実際に非ゼロ終了することを…注入確認」 | `--all`/`--mirror` 向け false-green 注入 TC を追加（例: `re.search(…--all\|--mirror…)` 行をコメントアウトした複製フックで TC-P21 が exit 0 になることを確認し、実体フックで exit 2 との差分を対で示す）。実装前に auto_test.md を修正し AC と整合させること |
| Warning | BP（テスト実行可能性） | TC-S2/S3/S5 の具体的な検証コマンドが未記述。「JSON をパースして該当配列メンバを確認する Python ワンライナーを TC に記述」という注記があるが、実際のワンライナーが auto_test.md に書かれておらず、実施者（Claude）が迷わず実行できる状態ではない。合格条件（期待値）は明示されているため Blocker 対象外だが、TC 実行時に判断を要する曖昧さが残る | `docs/tests/open/I080_auto_test.md` TC-S の注記部分 | 具体的な Python ワンライナーを TC-S2/S3/S5 に追記推奨（例: `python3 -c "import json; d=json.load(open('.claude/settings.json')); assert 'Bash(git push)' in d['permissions']['allow']"` 等） |
| Warning | BP（TC-DOC3） | TC-DOC3「追記差分に `I0\d\d` 不在」の検証方法が曖昧。「追記差分」をどのコマンドで取得するか（`git diff HEAD~1 -- docs/claude-code-structure.md \| grep -E 'I0[0-9]{2}'` 等）が未記述。実施者が独自判断を要する | `docs/tests/open/I080_auto_test.md` TC-DOC3 合格条件 | 具体的な差分取得コマンド例を追記推奨 |

## 高リスク判定
判定: Yes
該当条件:
- ハーネスのツール実行権限変更（`git push *` を ask→allow 化）：Claude Code の認可権限の変更に相当
- セキュリティガード（`pretooluse_guard.py`）の大幅リファクタリング：既存の protected-push 判定ロジックを全置換するため、実装ミスが即危険 push の素通りにつながる
- 既存事故の再発リスク：guard の穴（引数なし push・refspec 等）を塞ぐ変更だが、置換改修により新たな穴が生まれるリスクがある

推奨: `/security-review [I080]` を実行してから `/implement [I080]` へ進んでください。

---
TC-FALSEGREEN の `--all`/`--mirror` 向け TC を追加したうえで、再度 `/plan-issue-review [I080]` を実施してください。

VERDICT: BLOCKER
```
