すべてのドキュメントを読み込みました。参照実装（既存スクリプト・reviewer 定義）も確認済みです。レビューを開始します。

```
# 計画書レビュー [I074] ── 2026-07-05 14:30

## 判定: 差し戻し（Blocker 1件）

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Blocker | Claude Code BP（分岐パリティ・入力明記） | SKILL.md の手順6.5 が呼び出す `fix-test-review.sh` は引数 `<test_result_path>` を要求するが、plan §4-4 にはこのファイルを「どこで・どのように生成するか」が記載されていない。(a) 手順6 を「テスト実行結果をファイルに保存する」よう変更するのか、(b) 手順6.5 の冒頭に「テスト出力を保存してから呼び出す」サブステップを置くのかが未定義。実装者が SKILL.md の手順6.5 に何を書けばよいか判断できない（AC「各レビューの入力が明記されている」に違反）。また手順6 の変更有無が影響範囲 §3 に列挙されていない。 | plan §4-2・§4-4 手順6.5、イシュー AC「テスト＝作業ツリー全差分＋テスト実行結果」 | 手順6.5 に「テスト結果をどのパスのファイルに保存するか（例: `docs/reviews/I###_fix_test_result_<ts>.md`）」か、手順6 に「出力ファイル保存サブステップ」を明記する。影響範囲にも追記する。 |
| Warning | Claude Code BP（guard 位置記述） | plan §4-1 で `REVIEW_LIB_SOURCE_ONLY` guard を「lib 先頭で return 0」と記述しているが、関数定義より前に `return 0` を置くと TC-01（関数が `declare -F` で定義済みであること）が失敗する。正確な配置は「全関数定義の後・本体実行コードの前」（既存 `code-review.sh` L203 と同型）。実装者が「先頭」を額面通り実装すると TC-01 が破綻する。 | plan §4-1 `fix-review-lib.sh`「lib 先頭で」、TC-01 | 「全ヘルパー関数定義の後・本体実行コードの前」に配置すること、と明記する。 |
| Warning | P4 テスト妥当性 | 連続 NG カウンタの**リセット条件**が未記載。エスカレーション（連続 NG=2 でユーザーへ報告）解消後にカウンタが 0 に戻るのか、次の fix-loop 呼び出しで 0 になるのかが SKILL.md update 記述に無い。SKILL.md を読むだけでは運用が曖昧。 | plan §4-4「連続 NG を数え 2 回で停止しユーザーへエスカレーション」 | SKILL.md update 記述に「エスカレーション後の次ループ開始時にカウンタをリセット（または各 SKILL 呼び出しで独立カウント）」を明記する。 |
| Warning | P4 テスト妥当性 | TC-17 の false-green 反証「混入を注入した複製は判定 NG を返す反証込み」について、bash テスト内で「複製（copy）」をどのように作成・検証するか（変数に `--tools "Read,Write,Glob"` を含む文字列を用意して grep する等）が未定義。テスト作成者が実装方法を推測しなければならない。 | I074_auto_test.md TC-17 | 「`--tools` 行に Write を注入した文字列変数に対して grep が一致することで検出できることを確認する」等、具体的な反証手順を追記する。 |
| Warning | P5 運用性 | `claude -p` の**ハング（無限待機）**に対するタイムアウト対策が計画書に記載されていない。「失敗・空は即 skip」と記述があるが、exit 非ゼロや空ではなく stdin 待機のままハングした場合には exit も空も発生せず、fix-loop が無期限ブロックされる。既存スクリプトも同問題を内包しており「同パターン踏襲」としているが、新設スクリプトがゲートとして機能する以上、既知リスクとして明示記録すべき。 | plan §11「タイムアウト/リトライ: 既存 review スクリプトはタイムアウト未設定（同パターン踏襲）」 | R リストに「R7: `claude -p` ハング → 現状は既存パターン踏襲でタイムアウトなし・headless 環境で問題が大きい場合は別イシューで対処」を追記する（スコープ外であっても既知リスクとして記録する）。 |
| Info | BP | `claude-sonnet-4-6` が `fix-review-lib.sh` にハードコードされる。plan 設計判断表に「モデル戦略変更は全 review 横断の別イシュー案件」と明記されており許容済み。 | plan §4-1、設計判断表 | 対応任意 |
| Info | P4 テスト妥当性 | TC-05「VERDICT 行不在 → OK 既定」により、`claude -p` がテキストを返しても VERDICT 行を含まない場合（reviewer のフォーマット不備等）はゲートを素通りする。`run_fix_review` の空チェックはカバーするが非空・VERDICT 欠落は通過する設計。plan に「設計判断として意図的」と記載されているが、設計リスクとして明示があってよい。 | plan §4-1 `detect_fix_verdict`「不在は OK」 | 対応任意 |

（指摘なし以外の観点）
- P1 要件適合性: 問題なし（受け入れ条件を逸脱する仕様追加なし・業務ロジック N/A）
- セキュリティ: 問題なし（`--tools "Read,Grep,Glob"` 最小権限・prompt-injection ガード設計済み・新規 Web エンドポイントなし・OWASP N/A・DEFAULT_PERMISSION_CLASSES N/A）
- モダンウェブ開発: 対象外（UI/API なし）
- P3 データ整合性: 対象外（DB 変更なし）
- P9 プライバシー: 対象外（個人情報・テナントデータ非関与）
- 外部挙動依存の未実証: 問題なし（`claude -p` パターンは既存 3 スクリプトで実証済み・exit code のゲート伝達は fix-loop step 6 の「失敗→手順1」と同型で実証済み）
- 破壊的取り消し・コマンド衛生: 問題なし（計画書にチェーン複合コマンドなし・`git checkout/restore` 記述なし）
- P8 コスト・保守負荷: 問題なし（lib+薄いラッパー構成で DRY・連続 NG 上限でトークン浪費防止）

## 高リスク判定
判定: No
該当条件: なし（認証・認可変更なし・DB 変更なし・外部公開 API なし・個人情報なし・指示ファイルのみ変更）
推奨: `/implement [I074]` を実行してください。

---
Blocker 指摘（1件）への対応:

**[Blocker] `<test_result_path>` 生成方法の明記**:
plan §4-4 の SKILL.md update 記述で「手順6.5 が呼び出す `fix-test-review.sh` に渡す `<test_result_path>` をどこで生成するか」を明確にする必要があります。以下いずれかを選択して §4-4 および影響範囲（§3）に明記してください:
- **案A（推奨）**: 手順6.5 内に「テスト実行後、出力を `docs/reviews/I###_fix_test_result_<ts>.md` に保存し、そのパスを `<test_result_path>` として渡す」サブステップを追加する。
- **案B**: 手順6 に「テスト/lint/scan の出力を `docs/reviews/I###_fix_test_result_<ts>.md` にリダイレクト保存する」変更を追加し、§3 影響範囲に「手順6 の出力キャプチャ追記」を記載する。

明確化後に再度 `/plan-issue-review [I074]` を実施してください。

VERDICT: BLOCKER
```
