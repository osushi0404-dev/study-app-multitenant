## 決定論ゲート実行結果（code-review.sh 自動注入）

(決定論ゲート宣言なし)
- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

これで必要なファイルを確認できました。レビューを以下に出力します。

---

```
# コードレビュー [I102] ── 2026-07-07 00:30

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| AC1 | 非admin（`role != 'admin'`）が参照含む全経路で HTTP 403 を返す | ✅ 実装済み | - | `ProblemViewSet.permission_classes = [IsAuthenticated, IsOrgAdmin]`（views.py:152）。カスタムアクション（`generate_ai`/`generate_adaptive`/`upload_image`/`delete_image`）に permission override なし。TC-AUTO-01/02/02b でテスト済み |
| AC2 | admin（`role == 'admin'`）は参照・更新系を 200/201 で実行できる | ✅ 実装済み | - | `IsOrgAdmin.has_permission` で `role == 'admin'` を通過。TC-AUTO-03（CRUD）・TC-AUTO-03b（AI生成・画像）でカバー。ただし TC-AUTO-03b に `delete_image` の admin 正常系が欠落（別途 Low 指摘） |
| AC3 | `OrgAdminRoute`（`user.role === 'admin'`）が新設され非admin は遮断、org admin（is_staff=False 含む）は到達できる | ✅ 実装済み | - | `OrgAdminRoute.tsx` 新設・`user.role !== 'admin'` 判定。`App.tsx:79` で `<OrgAdminRoute>` ラップ。未認証は `/login` リダイレクト、認証済み非admin は Alert 表示。`is_staff` 非依存を確認 |
| AC4 | 非admin では Layout のナビに「問題管理」が表示されない | ✅ 実装済み | - | `Layout.tsx:76-79` の `isOrgAdmin ? [...]` ブロック内に「問題管理」を移動（`isOrgAdmin = user?.role === 'admin'`） |
| AC5 | BE 認可回帰テストが追加され全て PASS する | ✅ 実装済み | - | `test_I102_problem_authz.py` 新設（TC-AUTO-01/02/02b/03/03b 対応）。テスト結果は /test フェーズ前で空白（フロー上正常） |
| AC6 | `test_I073_image_update.py` の fixture admin 化後に当該テスト群が PASS する | ✅ 実装済み | - | `env` fixture の `user`/`user2` を `role="admin"` に変更済み。`test_other_org_cannot_edit`（404）・`test_cross_org_subject_rejected`（400）の越境検証意図が保たれていることを確認 |
| AC7 | E2E インフラに非admin identity が追加され authz スモークが PASS する（`playwright.config.ts` は変更不要） | ✅ 実装済み | - | `seed_e2e.py` の `_seed_login()` に `e2e_user_c`（`role='user'`）追加。`global-setup.ts` で `user_c.json` storageState 生成。`problem-management-authz.spec.ts` 新設（`browser.newContext` でユーザー切替）。`playwright.config.ts` 変更なし（計画書 AC7 と一致）。Strict Mode 違反はコミット `a730894` で修正済み |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | P4. テスト妥当性 | `test_admin_custom_actions_not_forbidden`（TC-AUTO-03b）に `delete_image` の admin 正常系（`DELETE /api/problems/{pid}/images/delete/{asset_id}/` が `!= 403` になること）が含まれていない。自動テスト文書 TC-AUTO-03b には画像削除の検証が明記されていたが実装で漏れた。非admin=403 は TC-AUTO-02 でカバーされているため IsOrgAdmin 適用自体は担保されており、不合格ではない | `test_I102_problem_authz.py` 末尾（134行で終了） | 任意修正推奨。次 PR または /fix-loop で `client.delete(f"/api/problems/{pid}/images/delete/{DUMMY_ASSET_ID}/").status_code != 403` を追加する |
| Low | ベストプラクティス・スコープ外情報 | `frontend/src/App.tsx` の `subject-management` ルート（:84）は `<OrgAdminRoute>` でラップされていないため、直接 URL 入力で非admin も到達できる。`Layout.tsx` のナビは `isOrgAdmin` でフィルタ済みだが FE ルートガードが不在。本 PR のスコープ外（イシューで「SubjectViewSet 等は対象外」と明記）かつ BE で SubjectViewSet の認可設定が別途あると考えられるため差し戻し対象ではないが、後続イシューで対処推奨 | `frontend/src/App.tsx:84` | 対応は I102 スコープ外。後続イシューで検討 |

（Blocker・High 指摘なし）

## 高リスク判定
判定: Yes
該当条件:
- 外部公開 API（`/api/problems/*` 全エンドポイント）の認可変更（`permission_classes` 追加）
- フロントエンドルートガード新設（`OrgAdminRoute`）

セキュリティレビュー実施済み（`plan_I102.md` に結果記録・Blocker 0/High 0/Medium 1（create 越境=I103 対応済み）。本 PR 対象外の残余リスクを悪化させていないことを確認）。

---
✅ コードレビュー完了。`/test [I102]` を実行してください。

VERDICT: OK
```
