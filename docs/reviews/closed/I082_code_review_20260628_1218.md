読み込み完了。受け入れ条件の照合とコード品質チェックを実施します。

```
# コードレビュー [I082] ── 2026-06-28 15:00

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `plan-writing-rules.md` 事前調査に「不確実な外部/ハーネス挙動への依存は計画確定前にスパイクで実証・記録」が明記 | ✅ 実装済み | - | L85〜92 に新規サブセクション追加。スパイク実証・記録が明記 |
| 2 | スパイク可否の二分岐（(1)in-session可／(2)in-session不能）が明記 | ✅ 実装済み | - | L89〜90 に (1)(2) 両分岐を記述。各分岐に「調査結果への記録」「失敗時のやり直し」が明示 |
| 3 | 新スパイク規定が既存「未知リスク先行原則」を参照・拡張する形で書かれ二重定義でない | ✅ 実装済み | - | L87 に「前倒しで拡張」と明記。L13（既存宣言）を参照する形。`grep -c` で出現2回確認 |
| 4 | スパイク完了基準（実行可能な確認で再現し成立/不成立を記録した時点で完了）が明記 | ✅ 実装済み | - | L92 に「スパイク完了基準」として明記 |
| 5 | `plan-reviewer.md` に条件付き Blocker gate 観点が追加（未実証かつ実証手段未明記=Blocker／可能で未実証=Warning／不能クラスで実施環境＋ステップ1前倒し明記=問題なし） | ✅ 実装済み | - | L75 に gate bullet 追加。(a)(b)(c) の3分岐で重大度が明示。差し戻し表にも Blocker パターン例示行追加（L167） |
| 6 | test 関連ルールに「ハーネス権限/フック挙動テストはプレーン default の新規セッション or Human 目視を実施環境として明記」が追加 | ✅ 実装済み | - | L511〜521 に追記。条件付き列として追加し実施環境値が表形式で明記 |
| 7 | 「実施環境」欄が条件付き列として追加（権限/フック/権限プロンプト/settings 権限 の挙動 TC を含む文書のみ必須・通常文書省略可・既存文書遡及改修不要） | ✅ 実装済み | - | L520 に条件付き旨、L521 に遡及改修不要を明記 |
| 8 | 追記文言中にイシュー番号が含まれていない（一般形） | ✅ 実装済み | - | TC-A5 の grep 結果 OK。plan-writing-rules.md・plan-reviewer.md ともにイシュー番号なし |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス / コマンド衛生 | TC-A1〜A5 のコマンドブロックが `&&`・`\|\|` で複合化されている。code-reviewer.md の「コマンド衛生」は検証/手順コマンドへの観点だが、TC コマンドは bash スクリプトファイルとして実行される想定（MEMORY: reference_interactive_shell_grep_wrapper.md の「シェルロジック検証は bash スクリプト実行で」に沿う）であり、allowlist 不一致によるプロンプト誘発は発生しない。実害なし。 | `I082_auto_test.md` TC-A1〜TC-A5 コマンドブロック | 対応任意。bash スクリプトファイルとして実行する限り問題なし。 |
| Low | P4 / テスト妥当性 | TC-A4 の grep チェックが `ハーネス挙動テストの実施環境`・`プレーン default 新規セッション`・`遡及改修は不要` の3アンカーのみを検証しており、AC の「`Human 目視` が列値として存在」や「条件付きの明記」が TC に含まれていない。実装ファイル（L518 `Human 目視`・L520 条件付き表記）には追記済みで実害なし。 | `I082_auto_test.md` TC-A4 | 対応任意。`Human 目視` を追加アンカーに加えると AC との完全対応になる。 |
| Low | P4 / テスト妥当性 | TC-A5 が `I079\|I080` の2番号のみを否定 grep しており、AC の「等」が示す他イシュー番号（例: I081、I083 等）を検証しない。追記文言に実際に他イシュー番号が混入していないことは差分確認で確定済みのため実害なし。 | `I082_auto_test.md` TC-A5 | 対応任意。`I[0-9]\{3,\}` 等の正規表現に広げると AC と完全対応になる。 |
| Low | P4 / テスト妥当性 | 手動テスト（I082_manual_test.md）の「実結果」欄が全行 `-`（空白）。ただし `/test` スキル実施前の正常状態であり、Low 以下で扱う。 | `I082_manual_test.md` No.1〜5 | `/test` 実施時に記入予定。 |

（Blocker・High 指摘なし）

## 高リスク判定
判定: No
該当条件: なし

---
✅ コードレビュー完了。`/test [I082]` を実行してください。

VERDICT: OK
```
