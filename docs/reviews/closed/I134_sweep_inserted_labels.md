# I134 sweep 挿入ラベル一覧（恒久記録・2026-07-19）

- 目的: 計画 ステップ4-1「挿入したラベル内容一覧を記録する」の履行（敵対的レビュー周回1 Medium「一覧未記録」対応）。scratchpad の退避スナップショットはセッション終了で失われるため、挿入したラベル行の実体を本記録に固定する。
- **母集合の定義（周回3 Medium 対応で確定）**: 本記録が対象とするのは「**本イシューがラベルを挿入した全件**」であり、open/closed の状態や記録作成時点の open 集合ではない。GitHub 側の権威リストは**退避原本（`scratchpad/gh_orig/<番号>.md`）が存在する 41 件**（初回 sweep 38 ＋ レース追補 3）＋クローズ済みの #239 で、下記見出しはこれに**本イシューの挿入ではない 4 件**（後述）を含む形で列挙されている（初版が「実行時点の open 全件」から生成された名残・番号ごとの区別は下記注を参照）。
- 取得方法: GitHub 側は `gh issue view <#> --json body` でラベル 2 行を抽出。ローカル側は wt-harness の sweep 対象 25 件から同様に抽出。
- **本イシューの挿入ではない 4 件（記録には載るが sweep 対象外）**: #242(I134)・#246(I137)・#247(I138) は sweep 前からラベル保有。**#250(I139)** は本イシューの実装後に**新テンプレート（プレースホルダ入り）で起票**されたもので、退避原本も存在しない（＝テンプレート修正が機能した実例であり挿入実体ではない）。
- 注: #251〜#253(I140〜I142) は sweep 後に発生し周回1 で追補（#251/#253 はその後の隣セッションによる本文再同期で一度消失し、規定どおり冪等に再追補）。#255(I143) は周回3 で追補（レース窓の 3 例目・下記に追補済み）。#220 は周回1 Medium 対応で目的文から I121 言及を除去した後の最終形。ローカル 25 件のうち git 管理済み 12 件の挿入差分は本ブランチのコミット（5488652）でも追跡可能。
- 追補（周回2 Medium 対応）: **#239(I131)** は sweep 時点で open のため挿入対象となったが、本記録の初版作成（open 全件から生成）前にクローズされたため欠落していた。挿入実体を下記に追補する。

## GitHub open イシュー（実行時点の全件・ラベル行の実体）
### #10
**背景**: 旧運用期（イシュー管理を 00_issue.md 等のファイルで行っていた時期）の起票で、本文は「作業開始。詳細は 00_issue.md に記載します。」の 1 行のみが残っている。
**目的**: タイトルのとおり、CLAUDE.md（プロジェクト運用ルール）の完全移行を GitHub Actions ワークフローによる自動化で実現する（詳細は当時の 00_issue.md を参照）。
### #42
**背景**: 学習統計画面の科目一覧が UserSubjectAccess（ユーザーが登録時に選択した科目）でフィルタされており、「所属組織が登録している全科目を表示する」という正しい仕様と乖離している。
**目的**: 統計画面の科目一覧を組織登録済み全科目から取得するよう修正し、管理者が追加した科目が即座に統計画面へ反映される状態にする。
### #44
**背景**: ユーザーへの科目紐付けは管理者による UserSubjectAccess の直接操作しか手段が無く、ユーザー自身が科目を選択・変更できない。
**目的**: アカウント登録フローへの科目選択ステップと登録後の追加・削除 UI/API を実装し、ユーザーが自分で学習科目を管理でき、クイズ開始時に自分の科目が正しく表示される状態にする。
### #153
**背景**: I073 で保存が一覧に反映されないキャッシュ無効化バグが自動テストで検出できず手動テストで初めて発覚した（読み取り経路の検証欠如・CI に Redis 無し）ことから、I073 retro 予防処置 P1/P2 として起票された。
**目的**: 「保存後に反映される」系 AC を読み取り側レイヤ（キャッシュ・集計等）越しの決定論テストで検証する観点と、無効化の CI 実行可能テスト担保の観点を、スキル・ルール・レビューアに恒久昇格する。
### #154
**背景**: I073 の security-review で、ProblemSerializer.subject の queryset 未スコープにより自組織の問題を他組織 subject に付け替えできる越境（SEC-1）が発見され、設計段階で捕捉する定型観点が無かった（I073 retro 予防処置 P4）。
**目的**: 更新系 API で親/参照リソースの所有組織が操作者の組織と一致するかを検証する観点を、grill-me・plan-reviewer・code-reviewer・rules の 4 箇所へレビュー観点として昇格する。
### #155
**背景**: _delete_by_pattern へ渡すパターンに KEY_PREFIX を手書きする invalidate_* メソッドが二重付与で実キーと一致せず無言で無効化に失敗しており、I073 では invalidate_problems_cache のみ修正済みだった（I073 retro 是正処置 C1）。
**目的**: 残る 4 メソッドの手書きプレフィックスを除去し、set→invalidate→get の回帰テスト（故障注入付き）で各キャッシュの無効化が効くことを担保する。
### #168
**背景**: I080 の push ガードで、動的入力（変数展開・コマンド置換等）のすり抜けと兄弟ルールの非対称という 2 クラスの欠陥を、該当観点が無かったためレビュー工程（攻撃シナリオ表作成済みのセキュリティレビュー含む）が見逃した。
**目的**: コマンド/コード文字列を静的照合する防御に固有のリスク観点（動的入力の脅威モデル化・兄弟ルール一貫性・該当テスト必須化）を grill-me・reviewer・rules に追加する。
### #170
**背景**: I073 の問題編集・画像更新でフロント側の中核ロジック（multipart 組み立て・画像リスト管理）に自動テストが無く、正しさが Human のブラウザ手動 TC（MAN-01〜08）だけで担保されていた。
**目的**: サーバ不要で決定論的に検証できるフロント Jest でギャップを埋めて手動 TC を退役させ、「人がレビュー/テストしなくても品質が出る」目標運用モデルに近づける。
### #172
**背景**: I083 で force 判定を pure _segments 化した際、--all/--mirror/--repo 判定に同型の貪欲正規表現バグ（.* がセグメント区切りを跨ぎ、無関係な後続コマンドに一致して安全な push を誤 block）が兄弟ルールとして残存していることが I083 コードレビューで検出された（Medium）。
**目的**: 3 判定を _segments 化して安全 push の誤 block を根治し、決定論テストで固定する（I085 のプロセス側観点に対するコード根治側の実インスタンス）。
### #173
**背景**: I083 でレビュー OK 後の成果物変更を実装者が「低価値」と自己判断してスキップしようとし、ユーザー是正の再レビューが別の Medium・Warning を検出する事例が 3 回発生した＝レビュー済みコミットと HEAD の差分が独立の目を通らず進みかけた。
**目的**: レビュー記録に reviewed-commit SHA を刻み、「reviewed-commit ≠ HEAD なら再レビュー or 免除理由の記録」を必須ゲート化して、実装者の自己判断スキップを非権威化する。
### #175
**背景**: I084 で /code-review が VERDICT: OK を返した後、レビューア自身が「実害なし」と明記した Low 指摘へ自己判断で修正着手し、ユーザーに停止された（工程・スコープの延長。I084 retro 予防処置 P2）。
**目的**: implement スキルに「レビュー OK 後は非ブロッキング指摘（Low/改善提案）へ自己着手せず、記録して次工程前にユーザー判断を仰ぐ。自己修正は Blocker/High/Medium に限る」境界を明文化する。
### #176
**背景**: I084 で grep -L の exit code 誤認とサブシェルの変数スコープ誤認という「記憶で確定されたランタイム挙動」が計画に入り込み、下流工程（TDD・plan-review）で是正になった（I084 retro 予防処置 P1）。
**目的**: CLI フラグ/シェル構文/言語ランタイムの exit code・戻り値・スコープに依存する設計判断は最小スパイク（実行）で裏取りする do を plan-writing-rules に、未裏取りの挙動断定を High とする gate を plan-reviewer に追加する。
### #177
**背景**: 「ハーネス改善」と「学習アプリ開発」を並行で進めたいが worktree 運用の標準手順が docs に無く属人化しており、実際に main から worktree を作成して空ツリーが生成される事故も発生した（2026-07-02 設計相談セッション）。
**目的**: トラック単位の git worktree 分離・独立 Claude セッション運用・ファイルベースのセッション間引き継ぎ原則を、手順・注意点込みで runbook 化する。
### #179
**背景**: I092 の /grill-me で、セットアップ・DB・ポート等の環境事実を一次ソース（docker-compose.yml 等）に照合せず確定値として記録し、誤前提が /plan-issue 段階まで固着した実例から生まれた（I092 retro・2026-07-02）。
**目的**: grill-me に「環境・運用の事実は一次ソース照合してから確定する」必須確認を追加し（do）、plan-reviewer に整合確認観点を追加して（gate）、環境事実の誤固着を構造的に防ぐ。
### #180
**背景**: I092 の存在検証ゲートで部分一致（`main`⊂`domain`）・非近接キーワード grep による false-green が発生し、plan-issue-review の指摘で初めて検出・是正された（I092 retro・2026-07-02）。
**目的**: 決定論テストの false-green 自己検証に「誤通過を狙った decoy 入力でも NG を確認する」要求を必須化し（do）、plan-reviewer に存在系 TC の偽陰性確認観点を追加する（gate）。
### #189
**背景**: I098 の計画で、構造的に発火不能な assert（false-green ゲート）と happy path 止まりのスパイクが grill-me / plan-issue-review を素通りし、ユーザーが促した敵対的レビューで初めて発覚した（I098 retro・2026-07-05）。
**目的**: 計画中のゲート/assert の発火可能性（reachability）とスパイクの実データ・多文脈反例探索を plan-writing-rules（do）と plan-reviewer（gate）に必須化し、見せかけの検証が実装まで素通りするのを防ぐ。
### #191
**背景**: 同一イシュー番号が open/ と closed/ の双方に tracked で残存する不整合（11 番号・I042 は内容差あり）が、I098 の content-hash ゲート試作を false-positive でブロックして表面化した（I098 retro 是正処置 C1・2026-07-05）。
**目的**: 共存 11 番号の棚卸し解消・クローズ処理の除去保証・「open/closed 排他」の決定論ゲート追加により、横断チェックを妨げる不整合の再発を防ぐ。
### #197
**背景**: I102 の認可変更で、影響半径の体系列挙が無く grill が 6 周かかり、兄弟の同型穴（subject-management→I104）の見逃し・FE-BE 認可概念ズレ・E2E レスポンシブ二重 DOM 問題が都度後工程で顕在化した（I102 retro 予防処置 P1〜P3）。
**目的**: 認可・アクセス制御変更向けの影響半径チェックリストを grill-me に追加し、FE-BE 認可概念一致・E2E 二重 DOM 指針を plan/code レビュー観点に昇格する。
### #199
**背景**: I078/I102/I103 の grill 中に、問題管理（ProblemViewSet）の delete・validate_choices 業務ルール・list フィルタ・基本 create・総合 admin CRUD E2E が未カバーの既存テスト負債と判明した（I102 retro 是正処置 C2）。
**目的**: 未カバー 5 項目のテストを追加してカバレッジ負債を解消する（I078 着地後の最終面＝admin CRUD＋is_correct 露出込みを対象に実装する）。
### #206
**背景**: I103 のブランチが、分岐後に develop へ入った CI ブロッカー解消（I108 の Pillow 更新）を取り込まないまま /code-review の CI ゲートに到達し、pip-audit fail でレビューがブロックされた（I103 retro 予防処置 P3）。
**目的**: push・CI 確認の前に origin/develop を取り込んでブランチを最新化するフローステップを追加し、長寿命ブランチの base drift による CI 赤を予防する。
### #207
**背景**: I109 で track 判定の例示リストに該当しない `.github/` 成果物に推測で `track:app` が付与され、「迷えばユーザー確認」も発火しなかった（I107 retro 予防処置 P1・2026-07-15）。
**目的**: `.github/` の harness 帰属例示と未該当パスのフォールバック（推測で付与せずユーザー確認）を明文化し、分類ルールの入力空間全域性を確認するレビュー観点を plan-reviewer に追加する。
### #208
**背景**: I107 の手動テスト No6 で Human 手順が意図レベル（完全な検索クエリなし）だったため、ユーザーが期待と異なる操作を実施し確認が 1 往復増えた（I107 retro 予防処置 P2・2026-07-15）。
**目的**: 「意図ではなく操作を書く」（完全 URL・コピペ可能なクエリ・特定された操作対象）規定を plan-writing-rules に追加し（do）、操作再現性のレビュー観点を plan-reviewer に追加する（gate）。
### #214
**背景**: I115 で SubjectViewSet.get_permissions の override がアクション単位の @action(permission_classes=[AllowAny]) を黙って無効化し public が 401 になる pre-existing bug が顕在化したが、この相互作用を体系的に検知するレビュー観点が存在しなかった（I104 retro 予防処置）。
**目的**: 「権限判定メソッドを override する ViewSet では全アクションの権限評価経路を確認する」観点を plan-reviewer・code-reviewer と Django 規約に明示チェック項目として追加する。
### #216
**背景**: I109 で実装ステップ中に完了した検証（ラベル作成の確認）が手動テスト文書に未記録のまま code-review へ進み、High 指摘「M3 未記録」で修正 1 往復が発生した（I109 retro 予防処置 P2・2026-07-15）。
**目的**: implement スキルに「実装ステップが manual_test の TC を先行消化した場合は実施直後に実結果を記録する」手順を追加し、実施済み検証の記録漏れを防ぐ。
### #220
**背景**: 定期依存監査ワークフロー（dependency-audit.yml・毎日実行）が 2026-07-15 22:48 UTC に frontend の npm audit fail を検知し、runbook の 2 段階運用に従って自動起票されたイシュー。
**目的**: 検知された依存脆弱性の内容を記録し、個別の是正イシューでの解消まで追跡する。
### #221
**背景**: I078 の plan 調査（2026-07-16）で、_save_ai_problem_to_db が Problem モデルに存在しない title/description でインスタンス生成するため AI 問題生成の保存が常に 500 になる既存バグを発見した（別イシュー対応はユーザー決定・I078 retro 是正処置 C1）。
**目的**: 保存経路を実フィールドへマッピングし直して保存成功の自動テストを追加し、AI 応答の is_correct 非露出を API レベルで回帰テスト化する（I078 残余リスク Low-5 の解消）。
### #222
**背景**: I078 の Human 手動テストが、認証情報不明のアカウント・前提データ 0 件・科目未紐付けの 3 つの前提不足で 3 回小停止した（I078 retro 予防処置 P1〜P3・是正 C3。ユーザー要望「依頼前にデータの存在チェック・無ければ作成」）。
**目的**: 手動テストの前提検証を計画時ルール（plan-writing-rules）・/test 実行時チェック・plan-reviewer 観点の 3 層に組み込み、Human へ依頼する前に前提が満たされている状態にする。
### #226
**背景**: 依存脆弱性ドリフトの是正で有効に機能した I121 の模擬適用手順（lockfile コピーへの npm audit fix 適用と exit・残存件数・宣言ファイル無変更の実測）が、個別イシューの計画書にしか残っていない（I121 retro 予防処置 P2）。
**目的**: dependency-audit runbook に是正イシューの標準実施手順を汎用形で記載し、次回の是正担当（コンテキストを持たない別セッション）が手順を再発明せずに計画前実測で AC を確定できる状態にする。
### #227
**背景**: I121 の計画書に cd frontend && npm audit fix の複合コマンドが記載され、Bash allowlist 不一致による承認プロンプト誘発リスクを plan-review の Warning が実装前に指摘した（I121 retro 予防処置 P3）。
**目的**: 「計画書・テスト文書内のコマンドは単体コマンドで記載する（複合化しない）」ルールを plan-writing-rules に追加し、plan-reviewer にコマンド衛生観点を明文化する。
### #230
**背景**: I117 で一致系 AC（ドキュメント列挙↔実装パターンの一致）を突き合わせ実測せず確定し、実際には不一致で AC と計画の矛盾が plan-review の Warning まで残存した（I117 retro 予防処置 P2・2026-07-18）。
**目的**: 「2 つの成果物の一致・整合を要求する AC は機械照合を実測してから確定する」規定を grill-me に追記し（do）、実測記録の確認観点を plan-reviewer に追加する（gate）。
### #231
**背景**: I115 の retro で、高リスク判定基準が作成側スキルに無く誤った推奨を提示した・frontend 未起動のまま Human に手動テストを依頼した・未認証公開エンドポイントのレート制限観点が security-review までどこにも無い、という 3 つの不足が特定された。
**目的**: plan-issue への高リスク判定基準の明記・/test への Human 依頼前環境整備手順・grill-me/plan-reviewer へのレート制限観点の追加を do+gate の 2 層で実装する。
### #233
**背景**: I122 の /grill-me で技術用語前提の確認文に「もう少し説明して。」の差し戻しが発生し、背景から順に平易な 4 要素で出し直したところ両質問とも即決された＝初回提示フォーマットの不足が原因だった（2026-07-18 ユーザーフィードバック）。
**目的**: 「そもそも何の話か・質問ごとの背景・推奨案と理由・回答方法」の 4 要素を必須とする平易な提示フォーマットを grill-me 起点に全スキルへ横展開し、確認文の平易さを実行ごとのばらつきから解放する（記録ファイルは技術詳細のまま維持）。
### #235
**背景**: I127 の grill-me（2026-07-18）でレート制限の理想形を評価し、auth 系 django-ratelimit の 403 応答（本来 429）・設定不備の死にミドルウェア・I127 導入後の二系統併存・高コスト API（AI 生成・画像）の防御不足を、後続の最適化イシューとして起票すると決定した。
**目的**: レート制限を DRF スロットルへ一本化して django-ratelimit を撤去し、429＋Retry-After の正しい HTTP 意味論へ是正した上で、高コスト API の個別制限とインフラ層防御の要否を確定する。
### #237
**背景**: code-review.sh のゲート分類器が引用符内の `|` をパイプチェーンと誤判定し、I122 の code-review で LLM レビュー全 AC 実装済みにも関わらず誤 BLOCKER→差し戻し 1 往復が発生した（I122 retro 予防処置 P1〜P3 の同梱起票・2026-07-18）。
**目的**: 分類器をクォート考慮のトークン単位判定に精緻化して受理形を plan-writing-rules に明文化し（P1）、計画変更時の旧値 sweep チェック（P2）と issue-bootstrap の GitHub 本文再同期（P3）を併せて追加する。
### #239（sweep 時 open・記録初版作成前にクローズ・周回2 で追補）
**背景**: Organization モデルの主キー改名（organization_id→id）の参照漏れが 3 箇所残り、組織スラッグ検証 API が有効組織でも常に 500・新規登録 API が全件 400 で新規ユーザー登録が完全不能になっていた（I127 手動テスト No.4 実施中に検出・レート制限導入前からの既存バグ）。
**目的**: 参照 3 箇所を実フィールド（id）へ修正し、validate-slug と登録 API の回帰テストを追加して登録動線を回復する。
### #241
**背景**: I127 の 429 トースト追加で、api.ts インターセプタ層と SubjectService 失敗コールバック層がトースト表示の役割分担未定義のまま並存する構造問題が顕在化し、同一エラーにトースト 2 枚が同時表示された（500 でも従来から発生・2026-07-19 実測）。
**目的**: 共通 HTTP ステータスのトーストはインターセプタが一元所有し、サービス層コールバックは画面内インライン表示のみに使う役割分担を確立して二重表示を解消する（エラーと空状態の区別表示を含む）。
### #242
**背景**: I132 のイシュー本文で、`## 背景/目的` セクションに小見出し 3 つ（現状の問題・根本原因・解決方針）だけが書かれ、肝心の「背景」「目的」そのものが記載されないままレビューを通過し、ユーザー指摘で発覚した。原因はテンプレート側にあり、`issue_template.md` の `## 背景/目的` にはセクション名に対応する記述欄（プレースホルダ）が存在しないため、テンプレートを埋めるだけでは背景・目的が書かれない構造になっている。
**目的**: テンプレートに `**背景**:` / `**目的**:` のプレースホルダ行を追加し、イシュー起票時に背景・目的の記述が構造的に促されるようにする（テンプレート埋め＝抜けなし、の状態にする）。
### #244
**背景**: I086 で実装した敵対的レビューステージについて、そのステージ自身が「実行・集計・記録・最終判定合成のすべてが実装セッションの誠実な指示遵守に依存し、下流の決定論チェックが無い」穴（H1・High）を掘り当てた（I086 敵対レビュー記録・2026-07-19）。
**目的**: 敵対記録の存在・VERDICT・生出力整合を下流の決定論ゲートで機械検査し（fail-closed）、サブエージェント権限も tools frontmatter で構造的に制限して、実行者の誠実さへの依存を断つ。
### #245
**背景**: I086 の敵対的レビューが、配線テストの欠落（H5/H6）・外部コマンド出力の表現ゆれによる fail-open（H3）・意図的リテラル grep への shellcheck 誤警告という、他イシューでも再発する 3 つの型を検出したが、いずれもレビュー観点・ルールに未収載だった（I086 retro 予防処置 P2〜P4・2026-07-19）。
**目的**: 3 つの型それぞれを do（plan-writing-rules へのルール追記）＋ gate（code-reviewer のレビュー観点追加）の 2 層で収載し、計画・レビュー段階で防げる状態にする。
### #246
**背景**: FE の API クライアント（`api.ts`）には 401 応答時のトークンリフレッシュ機構（リフレッシュ成功→元リクエスト再試行・失敗→トークン破棄とログイン画面誘導・並行リクエストのリフレッシュ待ち合わせ）が実装されているが、この分岐を検証するテストが 1 件も存在しない。I132 で「モック装着インスタンスを注入した `ApiClient` で実リクエスト経路を検証する」パターンが確立し、この分岐が初めて現実的にテスト可能になったため、I132 のスコープ外（別イシュー候補）として確定していたものを起票する。
**目的**: 認証セッション継続の中核ロジック（401 リフレッシュ分岐）に回帰テストを整備し、axios 更新・インターセプタ変更・リフレッシュ処理の改修時に退行を機械検知できる状態にする。
### #247
**背景**: I132 の実装中、計画書が指定したホスト実行のビルドコマンドが、既存のビルド出力ディレクトリ（過去に Docker コンテナ内ビルドで生成され root 所有）への書き込み権限がなく EACCES で失敗し、実装途中でコンテナ実行への切り替え（計画からの実行環境逸脱）が必要になった。plan-writing-rules の「環境前提確認」はツール・コマンド・サービスの**存在確認**（`which` / `--version` / `docker compose ps` 等）のみを要求しており、「計画したコマンドが選定した環境で実際に実行可能か」（出力先の書き込み権限・ホスト/コンテナの実行環境整合）は確認対象になっていない。本プロジェクトのようにホストとコンテナの二重実行環境を持つ構成では、コンテナが生成した成果物の所有者食い違いにより、存在確認を通ったコマンドが実行時に初めて失敗する取りこぼしが構造的に起こり得る。
**目的**: 環境前提確認を「ツールが在るか」から「計画したコマンドがその環境で動くか」まで拡張し（手順側 do）、計画レビューの観点にも同項目を追加して（点検側 gate）、実行環境起因の計画逸脱を計画確定前に検出できる状態にする。
### #250
**背景**: I134 の code-review で、テスト文書に検証スクリプトの全文を参考掲載しただけで omission-lint が「宣言外の隠れゲート」と誤判定して HIGH を返し、差し戻し 1 往復が発生した。また新設した常設チェッカー `check-issue-background.sh` は allowlist（`bash scripts/claude/tests/*.sh` のみ）に入らないため、formal gate として宣言・自動実走できない構造的制約も Medium 指摘として記録された（I134 では文書側の配置変更＝案 A で当座対応し、ハーネス側の根治を本イシューへ分離した）。
**目的**: code-review.sh の機械判定を「正当なパターンを受理できる」形に精緻化する。①omission-lint が『参考掲載のスクリプト全文』と『宣言セクション外の隠れた実行可能ゲート』を区別できる状態、②`scripts/claude/check-*.sh` 型の常設チェッカーを formal gate として宣言・自動実走できる状態、の 2 つを達成し、正当な文書構成・チェッカー運用が誤 HIGH や宣言不能で妨げられないようにする。
### #251
**背景**: I131 の code-review（Low 指摘・2026-07-19）で、I131 の 3 行修正とは無関係な既存の潜在バグとして発見され、スコープ外の別イシューとして起票された。
**目的**: 登録 API の personal 組織フォールバック作成が必須 FK category 未指定で IntegrityError になるパスを修正し（方式は grill-me で確定）、personal 組織不在時の slug なし登録の回帰テストを追加する。
### #252
**背景**: I131 で organization_id 改名の適用漏れが症状箇所以外にも残存し得るのに、grill-me にバグ修正イシューの残存箇所を網羅確認する手順が無く、心がけ任せになっていることが顕在化した。
**目的**: バグ修正イシューに対する「同一根本原因・残存箇所の全件 grep 確認」（パターン特定→全件 grep→修正対象/対応不要の分類→イシュー本文反映）を grill-me の省略不可手順として明文化する。
### #253
**背景**: I131 retro 予防処置 P3（2026-07-19）。organization_id 改名漏れの AttributeError が広域 except Exception により汎用 400/500 メッセージへ丸められ、新規登録が全件不能という重大欠陥が「エラーが発生しました」表示のまま潜伏した。
**目的**: accounts API の広域 except Exception 3 箇所を「想定内例外のみ個別捕捉・想定外は伝播させてログ+500 で顕在化」へ再設計し、既存レスポンス契約を回帰テストで固定した上で、規約（Django コーディング規約のエラーハンドリング節）にも明文化する。
### #255（レース窓 3 例目・周回3 で追補）
**背景**: I140 の /test（2026-07-20）で、長期稼働 frontend コンテナの node_modules が package.json の宣言状態から乖離して Jest が失敗し、是正の npm install が稼働中 dev サーバーのビルドキャッシュを失効させて E2E も二次障害で失敗した（I140 retro 予防処置 P1）。
**目的**: /test スキルの自動テスト前段に「実行環境同期ゲート」（冪等 install → 依存変化時の dev サーバー再起動 → コンパイル成功/healthy の機械判定）を手順化し、環境要因による /test 中断と手作業の切り分けをなくす。

## wt-harness ローカル sweep 25 件（挿入したラベル行の実体）
### docs/issues/open/013.md
**背景**: 品質ゲートが Claude Code の自己チェックのみに依存しており、実装者が自分のコードを自分でレビューする客観性の低さ・実行漏れの検知不能・Policy as Code の不在という問題があった（2026-03-27 の実装レビューフロー改善議論から）。
**目的**: GitHub Actions を導入して PR 作成時に機械的・客観的な品質ゲートを設け、将来の /implement 細分化（/code-review スキル新設）の基盤を整える。
### docs/issues/open/I031.md
**背景**: issue-flow.md の採番ロジック記載が issue-bootstrap スキルの実装と乖離しており（I026 改善提案 C）、docs 参照で採番すると番号衝突・欠番が生じるリスクがある。I039 クローズ時のテストファイル残留も併せて発見された。
**目的**: docs の採番ロジック説明をスキル実装に統一し、残留ファイルの後片付けと close スキルのコメント改善で運用の一貫性を回復する。
### docs/issues/open/I032.md
**背景**: 直 push のブロックが Claude Code 経由の deny ルールと hook のみで、GitHub Web UI・他ツール経由の push を防げていなかった（I026 改善提案 I）。
**目的**: Branch protection rules の設定手順書を作成・適用し、GitHub 側でも直 push と CI 未通過マージを物理的に防ぐ二重ガードを確立する。
### docs/issues/open/I033.md
**背景**: 改善イシュー群（I029〜I036）の進捗が一目でわからない状態だった（I026 改善提案 J）。
**目的**: Milestones（Phase 1〜3）と GitHub Projects ボードを設定し、改善フェーズの進捗を可視化する。
### docs/issues/open/I034.md
**背景**: I029 の計画策定時にシークレット検出は commit 時の pre-commit hooks が業界標準と判断されたが、コード内へのシークレット直書きを防ぐ仕組みが存在しなかった。
**目的**: pre-commit フレームワークを導入し、git commit 時のシークレット検出・基本品質チェックが自動実行される状態にする。
### docs/issues/open/I036.md
**背景**: workflow.md で /retro が「任意」扱いのため省略され得る状態で、振り返り不実施による問題蓄積・同じ失敗の繰り返しリスクがあった（I026 改善提案 E）。
**目的**: /retro をテスト OK 後の必須セレモニーに変更し、継続的改善サイクルを確立する。
### docs/issues/open/I040.md
**背景**: /plan-issue が設計上の曖昧な点を Claude の「平均的な判断」で埋めて計画書を作るため、認識ギャップが /fix-loop 発生の上流原因になりうる状態だった（参考記事の Grill Me スキル解説から）。
**目的**: 計画書作成前に設計インタビュー（/grill-me）を挿入し、設計認識のギャップを実装前に解消して計画書の質を上流から担保する。
### docs/issues/open/I041.md
**背景**: 計画書の「実装手順（ステップ）」がフリーフォーマットに近く、タスクの切り方・順序付けの原則が明示されていなかった（参考記事「PRD to Issues」の設計原則から）。
**目的**: 垂直スライス・未知リスク先行・依存関係明示の原則を plan-writing-rules.md に取り込み、実装手順を体系的に構造化する。
### docs/issues/open/I042.md
**背景**: /implement スキルに TDD サイクルの回し方が明示されておらず、テストを後付けで書く習慣を防げない状態だった。
**目的**: 新規の振る舞い追加時に Red→Green→Refactor を強制する TDD ガイドを /implement に追記し、auto_test 仕様を実装前から活用する流れを標準化する。
### docs/issues/open/I043.md
**背景**: レビュースキルに停止条件・重大度区分・高リスク判定が無く、同じ問題でも OK/NG の判断がぶれる構造的欠如があった（提案資料 review_design_proposal.md）。
**目的**: 3 レビュースキルへ停止条件・重大度区分・高リスク判定を整備し、grill-me に価値・方針衝突の確認観点を追加して、レビューの判断基準を明確化しフローの安定性を高める。
### docs/issues/open/I044.md
**背景**: セキュリティ確認が /plan-issue-review の表層チェックと実装後の /code-review の 2 点しかなく、高リスク変更を設計レベルで実装前に深掘りする仕組みが無かった（提案資料 review_design_proposal.md・先行 I043 の高リスク判定出力が前提）。
**目的**: /security-review スキルを新設し、高リスク判定 Yes の変更に対して /implement 前にセキュリティ設計・攻撃シナリオの深掘りレビューが実行される状態にする。
### docs/issues/open/I047.md
**背景**: I045・I046 でレビュー側スキルを 8 観点フレームワークに整合させた一方、上流スキル（/plan-issue・/grill-me・/implement）が未更新のため「作る段階では意識されない → レビューで多発修正」という非対称が生まれていた。
**目的**: 上流スキルにも同じ 8 観点を組み込み、問題をレビュー前の段階で防止して fix-loop の発生を減らす。
### docs/issues/open/I093.md
**背景**: I092 の /grill-me で、セットアップ・DB・ポート等の環境事実を一次ソース（docker-compose.yml 等）に照合せず確定値として記録し、誤前提が /plan-issue 段階まで固着した実例から生まれた（I092 retro・2026-07-02）。
**目的**: grill-me に「環境・運用の事実は一次ソース照合してから確定する」必須確認を追加し（do）、plan-reviewer に整合確認観点を追加して（gate）、環境事実の誤固着を構造的に防ぐ。
### docs/issues/open/I094.md
**背景**: I092 の存在検証ゲートで部分一致（`main`⊂`domain`）・非近接キーワード grep による false-green が発生し、plan-issue-review の指摘で初めて検出・是正された（I092 retro・2026-07-02）。
**目的**: 決定論テストの false-green 自己検証に「誤通過を狙った decoy 入力でも NG を確認する」要求を必須化し（do）、plan-reviewer に存在系 TC の偽陰性確認観点を追加する（gate）。
### docs/issues/open/I099.md
**背景**: I098 の計画で、構造的に発火不能な assert（false-green ゲート）と happy path 止まりのスパイクが grill-me / plan-issue-review を素通りし、ユーザーが促した敵対的レビューで初めて発覚した（I098 retro・2026-07-05）。
**目的**: 計画中のゲート/assert の発火可能性（reachability）とスパイクの実データ・多文脈反例探索を plan-writing-rules（do）と plan-reviewer（gate）に必須化し、見せかけの検証が実装まで素通りするのを防ぐ。
### docs/issues/open/I101.md
**背景**: 同一イシュー番号が open/ と closed/ の双方に tracked で残存する不整合（11 番号・I042 は内容差あり）が、I098 の content-hash ゲート試作を false-positive でブロックして表面化した（I098 retro 是正処置 C1・2026-07-05）。
**目的**: 共存 11 番号の棚卸し解消・クローズ処理の除去保証・「open/closed 排他」の決定論ゲート追加により、横断チェックを妨げる不整合の再発を防ぐ。
### docs/issues/open/I110.md
**背景**: I107 の手動テスト No6 で Human 手順が意図レベル（完全な検索クエリなし）だったため、ユーザーが期待と異なる操作を実施し確認が 1 往復増えた（I107 retro 予防処置 P2・2026-07-15）。
**目的**: 「意図ではなく操作を書く」（完全 URL・コピペ可能なクエリ・特定された操作対象）規定を plan-writing-rules に追加し（do）、操作再現性のレビュー観点を plan-reviewer に追加する（gate）。
### docs/issues/open/I112.md
**背景**: I109 で track 判定の例示リストに該当しない `.github/` 成果物に推測で `track:app` が付与され、「迷えばユーザー確認」も発火しなかった（I107 retro 予防処置 P1・2026-07-15）。
**目的**: `.github/` の harness 帰属例示と未該当パスのフォールバック（推測で付与せずユーザー確認）を明文化し、分類ルールの入力空間全域性を確認するレビュー観点を plan-reviewer に追加する。
### docs/issues/open/I118.md
**背景**: I109 で実装ステップ中に完了した検証（ラベル作成の確認）が手動テスト文書に未記録のまま code-review へ進み、High 指摘「M3 未記録」で修正 1 往復が発生した（I109 retro 予防処置 P2・2026-07-15）。
**目的**: implement スキルに「実装ステップが manual_test の TC を先行消化した場合は実施直後に実結果を記録する」手順を追加し、実施済み検証の記録漏れを防ぐ。
### docs/issues/open/I121.md
**背景**: リポジトリ無変更のまま新規 advisory（websocket-driver critical / ws high）の公開で npm audit 結果がドリフトし、CI の `--audit-level=critical` ゲートが fail して全 PR がブロックされた（2026-07-16・PR #219 で実証）。
**目的**: `npm audit fix`（非破壊・semver 互換範囲のみ）で critical を解消して CI ブロックを解除する（react-scripts 固定で fix 不可の残存指摘は従来決定どおりスコープ外維持）。
### docs/issues/open/I125.md
**背景**: I117 で一致系 AC（ドキュメント列挙↔実装パターンの一致）を突き合わせ実測せず確定し、実際には不一致で AC と計画の矛盾が plan-review の Warning まで残存した（I117 retro 予防処置 P2・2026-07-18）。
**目的**: 「2 つの成果物の一致・整合を要求する AC は機械照合を実測してから確定する」規定を grill-me に追記し（do）、実測記録の確認観点を plan-reviewer に追加する（gate）。
### docs/issues/open/I128.md
**背景**: I122 の /grill-me で技術用語前提の確認文に「もう少し説明して。」の差し戻しが発生し、背景から順に平易な 4 要素で出し直したところ両質問とも即決された＝初回提示フォーマットの不足が原因だった（2026-07-18 ユーザーフィードバック）。
**目的**: 「そもそも何の話か・質問ごとの背景・推奨案と理由・回答方法」の 4 要素を必須とする平易な提示フォーマットを grill-me 起点に全スキルへ横展開し、確認文の平易さを実行ごとのばらつきから解放する（記録ファイルは技術詳細のまま維持）。
### docs/issues/open/I130.md
**背景**: code-review.sh のゲート分類器が引用符内の `|` をパイプチェーンと誤判定し、I122 の code-review で LLM レビュー全 AC 実装済みにも関わらず誤 BLOCKER→差し戻し 1 往復が発生した（I122 retro 予防処置 P1〜P3 の同梱起票・2026-07-18）。
**目的**: 分類器をクォート考慮のトークン単位判定に精緻化して受理形を plan-writing-rules に明文化し（P1）、計画変更時の旧値 sweep チェック（P2）と issue-bootstrap の GitHub 本文再同期（P3）を併せて追加する。
### docs/issues/open/I135.md
**背景**: I086 で実装した敵対的レビューステージについて、そのステージ自身が「実行・集計・記録・最終判定合成のすべてが実装セッションの誠実な指示遵守に依存し、下流の決定論チェックが無い」穴（H1・High）を掘り当てた（I086 敵対レビュー記録・2026-07-19）。
**目的**: 敵対記録の存在・VERDICT・生出力整合を下流の決定論ゲートで機械検査し（fail-closed）、サブエージェント権限も tools frontmatter で構造的に制限して、実行者の誠実さへの依存を断つ。
### docs/issues/open/I136.md
**背景**: I086 の敵対的レビューが、配線テストの欠落（H5/H6）・外部コマンド出力の表現ゆれによる fail-open（H3）・意図的リテラル grep への shellcheck 誤警告という、他イシューでも再発する 3 つの型を検出したが、いずれもレビュー観点・ルールに未収載だった（I086 retro 予防処置 P2〜P4・2026-07-19）。
**目的**: 3 つの型それぞれを do（plan-writing-rules へのルール追記）＋ gate（code-reviewer のレビュー観点追加）の 2 層で収載し、計画・レビュー段階で防げる状態にする。
