# イシュー自己完結度レビュー [I066]

## 判定: 十分

重要セクションに空欄はなく、3つの問題すべてに具体的証拠（ファイル名・行番号・コマンド出力）が添付されている。未確定のアーカイブ方針 (a)/(b) は「grill-me で1つ選定」と明示宣言されており、欠落ではない。

---

## 未記入の重要セクション

なし

---

## 指摘一覧（助言）

| 重大度 | 観点 | 指摘内容 | 該当箇所 |
|--------|------|---------|---------|
| Info | 実装対象 | `code-review.sh` と `.claude/skills/close` の変更が互いに排他的（方針 a/b 依存）で、grill-me 前には計画書のスコープが確定しない。grill-me でどちらを採るかを決定してから `/plan-issue` に進むと計画書がシンプルになる。現状でも着手はできるが、計画書内で「方針 (a) 採用時のみ」「方針 (b) 採用時のみ」と明示する必要がある。 | 実装対象テーブル 2〜3行目 |
| Info | 解決方針 | 冪等化の実装手段が「既存セクションがあれば置換、もしくは重複ヘッダを作らない」と2パターン並列で書かれており、どちらを選ぶか未確定。AC は「重複追記されない」と結果指定のみで問題ないが、grill-me か plan-issue の冒頭で1案に絞るとスムーズ。 | 解決方針 3 |
| Info | 受け入れ条件 | 「既存60件が整理方針に従ってアーカイブされている」の検証手順（どのコマンドで確認するか）が AC に書かれていない。テスト担当者が迷う可能性があるため、plan-issue 時に検証コマンドを計画書へ記載することを推奨。 | 受け入れ条件 3つ目 |

---

## 次のアクションの推奨

方針 (a)/(b) の設計決定が後工程に最も影響するため、**`/grill-me` で方針を確定してから `/plan-issue`** に進むことを推奨。すでに証拠・根本原因・スコープが揃っているため、grill-me のスコープは「アーカイブ保存先の方針選定」と「冪等化実装手段の選定」に絞れる。
