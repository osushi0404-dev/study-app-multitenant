全ファイルを読み込みました。第三者レビューを実施します。

---

```
# 計画書レビュー [I083] ── [2026-07-01 17:30]

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | P4（テスト妥当性） | Section E（TC-FP*）のテーブルに "検証" 列がなく、`runj`/`rungj` 使用が明示されていない。本文の「重要（ask と pass の判別）」ノートは `TC-S*・TC-W*・TC-FP* は必ず `runj`/`rungj`」と明記しているが、実装者がテーブルだけ見ると exit code のみチェック（= `run`）で済ませてしまい、TC-FP1〜4 の「ask も block もされず通過」という要件が false-green になるリスクがある。Section B の "exit0＋ask JSON 無し" と同様に検証列を追加し、`runj`/`0` が期待値であることをテーブル上で明示することを推奨。 | docs/tests/open/I083_auto_test.md §E テーブル | 修正推奨 |
| Warning | P4（テスト妥当性） | `runj` ヘルパーのシェルスクリプト実装例が auto_test.md に未記載。「stdout を捕捉し exit 0 + `"permissionDecision": "ask"` ならば `ASK` を返す」という仕様は示されているが、`$()` での stdout 捕捉と exit code 保存を同時に行うシェル実装は非自明（`local code=$?` のタイミング問題など）。実装例もしくは「既存 `run` ヘルパーと並置する形で `runj` の実装コードを test script に追記する」旨の明記が望ましい。 | docs/tests/open/I083_auto_test.md「ハーネス追加」節 | 修正推奨 |
| Warning | P4（テスト妥当性） | バックティック（`` ` ``）コマンド代入形（例: `` git push origin `git rev-parse HEAD` ``）の専用 TC が存在しない。`_push_is_dynamic` の動的メタ文字集合には `` ` `` が含まれており実装は対応済みだが、イシュー AC「6 回避形」には含まれていないためテストで固定されていない。false-green 注入含め TC を追加することを推奨。 | docs/tests/open/I083_auto_test.md §A | 修正推奨 |
| Info | P4（テスト妥当性） | TC-F7（ラッパー隠蔽 force `eval "git push --force origin x"` → ASK）に対応する直接の false-green TC が TC-FG section にない。TC-FG-J（`_push_is_dynamic` の eval 分岐削除）が同一コードパスをカバーするが、TC-F7 の「force あり ＋ eval ラッパー → block でなく ask になる」という挙動を壊したときの裏取り TC が明示的でない。TC-FG-J の壊した版コマンドを `eval "git push --force origin x"` にする（or 追加 TC-FG-L として独立させる）と完全性が高まる。 | docs/tests/open/I083_auto_test.md §I | 改善提案 |
| Info | BP（規約・命名 網羅性） | `docs/claude-code-structure.md` の限界注記更新は計画書に明記されているが、I080_review.md など関連レビュー文書で同一限界を参照している箇所の確認・更新が影響範囲に列挙されていない。review doc はソース管理対象ではあるが実装依存ではないため実害は低く、次回レビュー者への情報整合として参照更新の要否を確認することを推奨。 | docs/plans/open/plan_I083.md §3 影響範囲 | 改善提案 |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement [I083]` を実行してください。

---
VERDICT: OK
```
