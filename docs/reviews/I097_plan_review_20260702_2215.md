全ファイルの読み込みが完了しました。レビューを実施します。

---

```
# 計画書レビュー [I097] ── 2026-07-02 15:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP（消費箇所の取りこぼし） | `wt-new.sh` に `--port-offset N` を追加した後、`worktree.md` §3 のコマンド例（`bash scripts/claude/wt-new.sh <track> <番号> <概要> [--env-source <path>] [--up]`）に `[--port-offset N]` が含まれない stale 状態になる。計画書の実装対象・4-4 は「§6 のみ」を明示しており §3 が取りこぼしになる。TC-DOC1 も §6 の内容チェックのみで §3 コマンド例を確認していない。 | plan_I097.md §4-4・worktree.md §3 L36 | §3 のコマンド例を `[--env-source <path>] [--up] [--port-offset N]` に更新することを実装対象に追加。または TC-DOC1 に「§3 のコマンド例に `--port-offset` が追記されていること」のアサーションを追記。修正推奨 |
| Warning | BP（消費箇所の取りこぼし） | `wt-new.sh` の `usage()` 関数 Usage 文と行 65-66 コメント「`# 任意 up（既定 OFF・§6 ポート衝突のため opt-in）`」が I097 実装後に旧理由（§6 は「同時 up 可能」に変わる）として残る。wt-new.sh は実装ターゲットなので実装者が気づく可能性はあるが、計画書に更新箇所として明示されていない。 | plan_I097.md §4-3・wt-new.sh L11/L65-66 | 計画書 §4-3 の修正方針に「`usage()` 関数 Usage 文と `§6 ポート衝突` コメントも合わせて更新する」を追記推奨 |
| Warning | P4（AC4 テストギャップ） | AC4「COMPOSE_PROJECT_NAME でコンテナ/volume が分離されたまま維持される」に対し、生成 root `.env` に `COMPOSE_PROJECT_NAME` が**含まれていない**ことを検証する決定論 TC が存在しない。TC-P1〜P4 は PORT 値の存在を確認するが不在変数の確認はない。悪意ある実装ミスで `COMPOSE_PROJECT_NAME` が混入しても検知できない。 | I097_auto_test.md §A | TC-P1 または TC-P7 に「生成 `.env` に `COMPOSE_PROJECT_NAME` が含まれない（`grep` で不在検証）」のアサーションを追記推奨 |
| Warning | P4（TC-P8 期待結果が曖昧） | TC-P8（回帰）の期待結果が「`test_wt_lifecycle.sh` 55/55 維持で担保」と外部テストへの委譲のみで、`test_wt_port_offset.sh` 内の具体アサーションが明記されていない。「新 `.env` 生成が既存挙動を壊さないことを確認」が何を assert するのか実装者が判断に迷う可能性がある（例: `backend/.env` に `SECRET=stub` が残るか、exit 0 か）。 | I097_auto_test.md §A TC-P8 | TC-P8 の期待結果に「`wt-app/backend/.env` に `SECRET=stub` が存在する（コピー済）かつ exit 0」などの具体アサーションを追記推奨 |
| Info | P4（隔離環境 .gitignore 注記なし） | `test_wt_port_offset.sh` の隔離環境（temp repo）で `wt-new.sh` が root `.env` を生成した場合、既存 `test_wt_lifecycle.sh` の `.gitignore` 設定（`backend/.env\ne2e/.env.e2e` のみ）を踏襲すると root `.env` が untracked のまま残る。実 `.gitignore` には `.env`/`*.env` が定義されているため環境差異が生じる。機能上の失敗は起きにくいが auto_test.md に実装メモがない。 | I097_auto_test.md 隔離方針 | `test_wt_port_offset.sh` の `new_env()` 相当で `.gitignore` に root `.env` を追加するよう実装メモを auto_test.md に追記推奨 |
| Info | セキュリティ（pre-existing） | db（5432）/ redis（6379）のホストポートを `0.0.0.0` に公開する設計は変更前から存在するもので、本イシューで新規リスクが追加されるわけではない。オフセットによる露出面の変化もない。ただし開発環境専用設定として非ローカル環境へのデプロイ時は注意が必要（既存の設計課題）。 | docker-compose.yml L12-13, L26-27 | 対応は任意 |

## 高リスク判定
判定: No
該当条件: なし
推奨: `/implement I097` を実行してください。

---

VERDICT: OK
```
