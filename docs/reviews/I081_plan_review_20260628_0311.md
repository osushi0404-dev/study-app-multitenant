必要なファイルをすべて確認しました。レビューを実施します。

---

```
# 計画書レビュー [I081] ── 2026-06-28 11:30

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP／Claude Code BP | `_SHELL_OPS = ("&&", "||", "\|", ";")` のうち `"&&"` と `"||"` は有効な separator として機能しない。Python `shlex.split()` は `punctuation_chars` 未指定時（`shlex.split()` のデフォルト）、`&&` を 2 文字の単一トークンにまとめず `'&', '&'` の 2 トークンに分割する（`punctuation_chars` なしでは連続する非 wordchar は個別トークンになる）。`'&' in _SHELL_OPS` → False のため、`git push && git checkout dirty_file` のように `&&` 区切りで checkout が **後続** に来る複合コマンドは `_segments` が分割できず 1 段 として処理され `toks.index("git")` が最初の `git push` を拾うため checkout 部分が検出されない。`"||"` は各 `'\|'` トークンが `"\|"` に一致するため偶然 2 分割されるが `"&&"` は dead code。なお primary use case（単体 `git checkout -- dirty_file`）と `&&` 先頭 checkout（`git checkout dirty_file && other`）は正常に検出できる。また複合コマンド自体が allowlist ask を誘発するという二重防御が存在するため致命的ではない。`"&&"` dead entry がコードの意図を誤読させるリスクが主問題。 | `scripts/claude/hooks/pretooluse_guard.py`（追加予定）`_SHELL_OPS` 定義・plan §4-5 コードスニペット | 修正推奨（`"&&"` を除去し動作する `"\|"` `";"` のみにするか、`shlex(s, posix=True)` に `punctuation_chars=True` を渡して `&&` トークンを正しく得る。後者の場合 `"&&"` が有効になるが TC-G での `&&` 複合コマンドのテストケース追加も必要） |
| Info | P4 テスト妥当性 | TC-G-FALSEGREEN（判定本体を一時無効化し dirty でも exit 0 になることを裏取り）は `I081_auto_test.md` 結果表に ⏳ 追跡項目として記載されているが、`test_pretooluse_checkout_guard.sh` のスクリプトステップとしては実装されていない（"確認後に本実装へ戻す" 形の実装時ワンタイム検証）。TC-G1（clean→0）＋ TC-G3（dirty→2）の対検証が主担保として存在するため P4 基準は満たしているが、注入検証の再現可能な自動化がないと将来のリグレッション検出に弱い。 | `scripts/claude/tests/test_pretooluse_checkout_guard.sh`（新規）・`I081_auto_test.md` TC-G-FALSEGREEN | 改善提案（実装時の一時注入ステップをワンタイムで実施しその結果を auto_test.md に記録する運用で許容可。将来的には sed による一時無効化ステップを script に組み込む選択肢もある） |

## 高リスク判定
判定: No
該当条件: なし
推奨: `implement [I081]` を実行してください。

---

VERDICT: OK
```
