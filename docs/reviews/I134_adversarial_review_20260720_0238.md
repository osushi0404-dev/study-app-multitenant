# I134 敵対的レビュー記録（高リスク: scripts/claude/ 配下スクリプト追加）

- 基本レビュー: docs/reviews/I134_code_review_20260719_2253.md（FINAL_VERDICT: OK・RISK: YES）→ 本ステージ自動起動
- 方式: 反証マンデートの独立サブエージェント（.claude/review-agents/adversarial-reviewer.md 準拠・並列・実機 repro 付き）

## 周回1（2026-07-19〜20・エージェント 5 体）

観点: ①ロジック回避・実機 repro ②テストの false-green・tautology ③要件・脅威モデルの網羅漏れ ④ドキュメント整合（動的） ⑤挿入内容の忠実性・無改変（動的）

集計: **NEW_CRITICAL: 0 / NEW_HIGH: 2**（Medium 8・Low 8）

### High（2 件・修正済み)
| # | 観点 | 指摘 | 対応 |
|---|------|------|------|
| H1 | ②false-green | 無改変保証ゲート群がファイル丸ごと削除を検出できない（tc05 が現存ファイル駆動でスナップショット側を照合せず、untracked 消失は TC-02/04 にも映らない）。実測: I093.md を除いた temp repo で全ゲート緑 | tc05 を**スナップショット側駆動**に書き換え（FILE-DELETED 検出・dir 不在/走査 0 件は exit 2・I134.md は TC-04 と同一理由で除外）。注入実証: ファイル消失 → exit 1・dir 不在 → exit 2。実データ再実走 checked=25 exit 0 |
| H2 | ③要件網羅 | sweep 後に隣 worktree で I140〜I142（#251〜#253）が旧テンプレ起票されラベル無し open が 3 件発生（TC-03 再実走で missing=3 を実測）。「全 41 件」が実態と乖離し、引き継ぎ手順が最終ゲート到達不能 | 同一手順（原本退避→見出し直下挿入→--body-file・冪等）で 3 件を追補。計画に**レース窓の規定**（AC-4/TC-03 は実行時点の open 全件・新規発生分は同手順で追補・テンプレ伝播後に構造停止）を追記し、イシュー AC も件数固定を撤廃して実行時点全件へ再定義。追補後 TC-03: **total=44 missing=0**。なお追補後、隣セッションの /plan-issue I140 本文再同期で #251 が一度上書き消失 → 規定どおり再追補（レースの実例 2 件目として記録） |

### Medium（8 件・対応内訳）
- **チェッカーの fail-open（①・②）**: dir 不在/空/0 件走査でも OK exit 0 → **修正**（dir 不在 exit 2・走査 0 件 exit 1・件数出力。注入 3 方向実証）。tc06 にも期待件数引数を追加（原本保存漏れの fail-closed・注入実証済み）。
- **ゲート宣言の無警報消失（①・pre-existing）**: コメントアウト/字下げフェンスで extract=0・無警報 → ハーネス側の根治として **I139(#250) へ追記**（現状の問題 3 項目目・解決方針・AC・GitHub 同期済み）。
- **レビューチェックリストの旧値残存（④）**: I134_review.md の「見出し無し 4 件」→ **2 件へ訂正済み**。
- **挿入内容一覧の未記録（④・計画 ステップ4-1 不履行）**: **`docs/reviews/I134_sweep_inserted_labels.md` を新設**（GitHub 44 件＋ローカル 25 件のラベル実体を恒久記録）。
- **宣言外挿入＝GitHub Issue 行のドリフト同期（⑤）**: paired 11 件へ `- GitHub Issue: #NNN` 行が全文同期で追加挿入されていた → 全件自番号一致・挿入のみ（TC-06 で機械確認）・bootstrap 由来の正当メタ行のため**受容し auto_test 実施記録に明記**。
- **#220 への新情報混入（⑤）**: 挿入した目的文の「I121 等」がオフライン導出不能な会話由来 → **#220 本文から除去**（「個別の是正イシュー」へ一般化・更新済み）。
- **dependency-audit 自動起票の再生産経路（③）**: `dependency-audit-issue.sh` はテンプレート非依存のため今後もラベル無し open イシューを生成し得る → 計画「実装後追記」に**受容として記録**。根治（生成本文への定型ブロック追加）は計画外変更のためスコープ外・対応要否はユーザー判断待ち（下記「ユーザー判断事項」）。

### Low（8 件・対応内訳)
- 引き継ぎスクリプトの偽成功メッセージ → **修正**（挿入検証してから UPDATED/ERROR を出し分け）。
- #10 の忠実性がオフライン照合不能 → **manual No.4 のサンプルに #10 を追加**（Human 目視）。
- 節名引用の不一致（auto_test）→ **完全名に修正**。イシューの計画書パス誤記 → **修正・#242 同期済み**。
- 受容・記録のみ: `.MD` 拡張子ゆらぎの非走査（命名規約上実害低）／テンプレ cp 直後のプレースホルダをチェッカーは合格とする（存在チェックという AC どおり・未記入検知は issue-review が担当）／AC-2 の独立決定論検証なし（issue-reviewer の LLM 判定が担い手・静的整合は確認済み）／過去レビュー記録 2 件の本文ヘッダ時刻とファイル名の不整合（歴史的記録のため不改変・ここに記録）。

### 周回1 の再検証（修正後の全ゲート実走・2026-07-20）
TC-01 exit 0×2（GATE_VERDICT=OK・omission=OK）／TC-02 exit 0（27 件）＋注入 3 方向 NG 実証／TC-03 **total=44 missing=0**／TC-04 exit 0／TC-05 exit 0（checked=25）＋消失注入 NG 実証／TC-06 exit 0（checked=41=expected）＋件数不一致注入 NG 実証。

## ユーザー判断事項（設計判断・非ブロック）
- dependency-audit 自動起票の本文に定型の 背景/目的 ブロックを追加するか（`dependency-audit-issue.sh` の変更＝計画外・小規模）。追加しない場合、次回の scheduled 監査 fail 時にラベル無し open イシューが再生産される（本イシューの AC には影響しない・受容記録済み）。

## 周回2（対応後再レビュー・2026-07-20・エージェント 3 体）

観点: ①修正の反証・実機 repro ②テストの false-green・tautology（全数再検） ③要件網羅・ドキュメント整合（全数再検）

集計: **NEW_CRITICAL: 0 / NEW_HIGH: 1**（Medium 4・Low 5）

### High（1 件・修正済み）
| # | 観点 | 指摘 | 対応 |
|---|------|------|------|
| R2-H1 | ③整合 | 周回1 で AC-4 を「実行時点の open 全件」へ再定義したのに、判定側 TC-03 の仕様行（「全 41 件」「total=41」）と文書冒頭の対象行が旧値のままで、同一文書の実施記録（total=44）と自己矛盾（9cfd233 が TC-02/05/06 行のみ更新し TC-03 行を未更新） | auto_test の対象行・TC-03 行・注記を「実行時点の open 全件」へ是正（total は実施記録に記録する方式）。連動して イシュー本文の固定件数残存（解決方針/実装対象/スコープの 41・28 件）・計画ロールバック節（38→41 件）・レビューチェックリストの旧ゲート定義（41/13/26/38 件・旧 tc05 設計）・manual No.1（「13 件のみ」が I139 追加で実行不能）も全て整合（#242 同期済み） |

### Medium（4 件・対応内訳）
- **I134.md 自身の丸ごと削除がどのゲートにも映らない（①）** → tc04 に I134.md の存在チェックを追加（注入実証: 不在 temp repo → exit 1）。行置換の numstat 除外は設計どおり維持（管理文書・PR diff で追跡可能）。
- **tc06 の期待件数引数が非数値で無効化・checked=0 でも合格（①）** → 正の整数のみ受理（非数値/0/空 → exit 2）＋走査 0 件 exit 2 を追加（注入実証: `5x`→2・空 dir→2・`0`→2）。
- **恒久記録から #239(I131) が欠落（②・sweep 時 open→記録初版前にクローズ）** → `I134_sweep_inserted_labels.md` に #239 を追補し、母集合の取り違え（open 全件→更新した全件）を注記。
- **スナップショット基準の自己証明の限界（②）** → 受容として記録（tracked 12 件は origin/develop とバイト一致を独立実証・GitHub 側は編集履歴が恒久監査経路・untracked 分は限界を auto_test に明記）。
- （③の Medium 2 件＝チェックリスト旧ゲート定義・manual No.1 は R2-H1 の連動修正に含めて解消）

### Low（5 件・対応内訳）
- 引き継ぎスクリプトの片ラベル状態での重複挿入/偽 UPDATED（①） → skip 条件を「片ラベルでも停止（手動確認）」に変更・UPDATED 判定を両ラベル存在に強化。
- `awk -v` のバックスラッシュ・エスケープ解釈による挿入内容破損の潜在（①） → 環境変数渡し（ENVIRON）へ変更（現行 44 件の実データにバックスラッシュ 0 件を実測済み・実害は将来分のみ）。
- イシュー本文の固定件数の部分残存・I139 の「4 方向/5 方向」不整合（③） → 修正済み（#242/#250 同期済み）。
- auto_test オラクルの旧値ドリフト（②） → R2-H1 と同一事象・解消済み。

### 周回2 の再検証（修正後・2026-07-20）
tc04 v2 本走 exit 0＋不在注入 exit 1／tc06 v3 本走 41 件 exit 0＋非数値・空 dir・0 の 3 注入すべて exit 2。周回2 エージェントによる独立再実測でも周回1 修正後の全ゲート主張（TC-01〜06・注入系）に虚偽・誇張なしを確認（②の全数再検）。

## 周回3（最終収束確認・2026-07-20・エージェント 3 体＋再実行 1 体）

観点: ①周回2 修正の反証・実機 repro ②AC 充足の最終実測検証 ③文書整合の最終全数検査

集計: **NEW_CRITICAL: 0 / NEW_HIGH: 0**（Medium 2・Low 1）→ **ゼロ周回成立（新規 Critical/High 0）**

### エージェント別結果
- **①周回2 修正の反証**: NEW_CRITICAL 0 / NEW_HIGH 0。Medium 2・Low 1（下記・いずれも修正済み）。反証失敗の裏取り: 計画書フェンス内スクリプト全文と実行版 3 本＋リポジトリ実体チェッカーが**バイト一致**（cmp rc=0）・引き継ぎスクリプトの 7 分岐を gh スタブ＋temp worktree で全網羅（両ラベル skip／片ラベル停止・無改変／GH 無し skip／GH 側ラベル欠落 skip／見出し不一致 ERROR・無改変・.tmp 残骸なし／正常系 UPDATED かつ **ENVIRON 渡しでバックスラッシュがバイト無傷**）・tc04 の誤 cwd/同名ディレクトリは fail-closed・tc06 の先頭 0/空白入り引数も期待どおり。
- **②AC 充足の最終実測検証**: **反証失敗（新規指摘なし）**。AC-1/2（テンプレ grep＋run_declared_gates 直接実行 → 2 本 ALLOW・exit 0・GATE_VERDICT=OK・omission_lint OK）／AC-3（チェッカー実走 `OK: all 27 files` exit 0＋fail-closed 3 分岐の再注入）／AC-4（gh_orig 原本 41・.after 41・全 .after にラベル実在・削除行ゼロ・件数突合一致。.after の鮮度限界は既知として記載）／AC-5（隣 worktree 42 件を実測: ラベル無し 39 件すべて見出し完全一致・CRLF ゼロ・片ラベルゼロ・GitHub 無しは I074/I086/I114/I122 の 4 件ちょうどでスニペットと 1:1・各ファイルに事実アンカー実在 → **決定論的に完走し最終ゲート exit 0 に到達**）／追加ゲート（tc04 exit 0・tc05 checked=25 exit 0・tc06 代替 41 件一致）。**スナップショットの真正性**も tracked 12 件を `git show origin/develop:` とバイト比較し全一致（改竄・すり替えなし）を独立実証。
- **③文書整合の最終全数検査**: 実行中に API クレジット枯渇で**異常終了**（成果物なし）。→ 同一プロンプトで再実行し、結果を下記「③再実行」に記録。

### Medium/Low（周回3・すべて修正済み）
| # | 重大度 | 指摘 | 対応（注入実証済み） |
|---|--------|------|------|
| R3-M1 | Medium | tc04 は比較基底 `origin/develop` が解決不能（未フェッチ・shallow・ref 名タイポ）だと git の fatal を awk が空入力で飲み込み **exit 0 で合格に化ける**。周回1〜2 の fail-closed 強化が tc04 の「入力欠損」モードにだけ未適用 | `git rev-parse --verify --quiet origin/develop` を前置し不解決なら **exit 2**。注入: ref を持たない temp repo → exit 2 を実測 |
| R3-M2 | Medium | tc06 の期待件数ゲートは「ファイル不存在」しか捕捉せず、**0 バイト原本は checked に数えられ diff が全行追加扱いで素通り**（退避時の gh 失敗の痕跡＝削除検出とロールバック原本が同時に無効化される） | `[ ! -s "$o" ]` で **EMPTY-ORIGINAL → exit 1**。注入: 0 バイト原本 1 件混入（件数一致条件下）→ exit 1 を実測。実データは 0 バイト 0 件/41 件を確認 |
| R3-L1 | Low | 周回2 の整数検証は**桁あふれ数字列**（全桁数字で case 通過）で `[ -ne ]` がエラー→照合不発の false-green を再現 | 桁数上限（6 桁）を追加し **exit 2**。注入: 23 桁引数 → exit 2 を実測 |

### ③再実行（文書整合の最終全数検査）
初回はクレジット枯渇で異常終了したため、周回3 修正（ceaa5f9）まで反映した確定状態に対して同一プロンプトで再実行。結果 **NEW_CRITICAL 0 / NEW_HIGH 0**（Medium 5・Low 1）。

| # | 重大度 | 指摘 | 対応（すべて実施済み） |
|---|--------|------|------|
| R3-M2b | Medium | 周回2/3 で強化した tc04/tc06 の fail-closed 仕様が auto_test の **TC 表（合否仕様の層）に未反映**で、スクリプト全文＞チェックリスト＞TC 表の 3 層ドリフト（TC-02/05 は明記済みで同一文書内が非対称） | TC-04/TC-06 行に fail-closed の exit コードを明記・レビューチェックリストも同期 |
| R3-M3 | Medium | 恒久記録の母集合定義が自己矛盾（見出し 45 件 vs 退避原本 41 件・「更新した全件へ是正」の宣言と本体の不一致）。さらに **#250(I139) が新テンプレ起票＝本イシューの挿入ではない**のに除外注記から漏れ、挿入実体と誤読させる | 母集合の定義を「本イシューがラベルを挿入した全件」と明記し、権威リスト（退避原本 41 ＋ #239）と「挿入ではない 4 件（#242/#246/#247/**#250**）」を分離記載 |
| R3-M4 | Medium | 引き継ぎ手順の件数（「未追跡 28 件中 GitHub 対応あり 24 件」）が隣 worktree の現況（未追跡 30・未保有 27・GH 対応 23・GH 無し 4）と不一致。原因は I131/I140 のクローズと**レース窓 3 例目 I143(#255) の発生**（生きている文書に未記載） | 手順から固定件数を撤廃し「実行時点の実態に従う・完了判定は最終ゲート exit 0 到達」へ変更。#255 は追補し記録（TC-03 total=44 missing=0） |
| R3-M5 | Medium | 周回2 の固定件数 sweep の取りこぼし: 計画書の**実装手順ステップ3-4** と**設計判断表**に「41 件」固定が残存（計画時記録の欄ではなく現行手順/設計判断の欄） | 2 箇所とも「実行時点の open 全件」表現へ是正 |
| R3-M6 | Medium | **AC-5 だけ対応 TC が存在しない**（決定論層に載らず目視チェックのみ）。M4 の件数乖離が最終収束確認まで検出されなかったのはこの穴に対応 | **TC-07 を新設**（引き継ぎスニペット集合 ↔ 隣 worktree 実態の機械照合）。本走 exit 0（4 件一致）・注入 exit 1・dir 不在 exit 2 を実証。commit 済みファイル（develop 配布分）の除外も実装 |
| R3-L2 | Low | manual No.1 の未追跡ファイル説明に、本イシュー中に生成された敵対レビュー記録・I139 の issue-review 記録が織り込まれていない | 期待値を現況に合わせて更新 |

反証失敗（健全を実測確認）: 退避原本 41 件と記録の番号集合一致・記録のラベル文面 140 行と実体の全件一致・41 件の更新差分に削除行/ラベル重複ゼロ・ラベル位置が全件見出しの 3 行以内・宣言外挿入は文書化どおり 11 件のみで全件自番号一致・計画書コード例とチェッカー実体のバイト一致・omission-lint 相当スキャン 0 ヒット・隣 worktree 全 42 件が見出し完全一致（引き継ぎ awk の偽成功パスに落ちるファイルゼロ）・参照先の実在。

## 最終結果

- **周回1**: NEW_CRITICAL 0 / NEW_HIGH 2 → 修正
- **周回2**: NEW_CRITICAL 0 / NEW_HIGH 1 → 修正
- **周回3**: NEW_CRITICAL 0 / **NEW_HIGH 0** → **ゼロ周回成立＝ステージ通過**（周回3 で出た Medium 8・Low 2 もすべて修正または受容記録として処理済み）
- エージェント総数: 12 体（周回1: 5・周回2: 3・周回3: 3＋再実行 1）／上限 15 体・3 周以内で収束
- 最終ゲート実測（2026-07-20・全 fail-closed 注入つき）: TC-01 exit 0×2・TC-02 exit 0（27 件）・TC-03 **total=44 missing=0**・TC-04 exit 0・TC-05 checked=25 exit 0・TC-06 checked=41=expected exit 0・**TC-07 exit 0**（スニペット 4 件一致）

## ユーザー判断事項（設計判断・非ブロック）
- dependency-audit 自動起票（`dependency-audit-issue.sh`）の本文に定型の 背景/目的 ブロックを追加するか。追加しない場合、次回の scheduled 監査 fail 時にラベル無し open イシューが再生産される（本イシューの AC には影響しない・受容記録済み）。別イシュー化または I139 の grill 時の同梱判断が候補。

VERDICT: OK
