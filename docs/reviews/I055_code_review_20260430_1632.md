# コードレビュー [I055] ── [2026-04-30 18:00]

## 受け入れ条件の照合

| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `git commit` 時に Ruff / bandit / ESLint が自動実行され、エラーがあればコミットがブロックされる | ✅ 実装済み | - | `.pre-commit-config.yaml` に ruff（v0.15.12）・bandit（1.7.9）・eslint の 3フックを追加。TC-03 で全フック pass 確認済み |
| 2 | CI の `frontend-lint` ジョブで `npm audit --audit-level=critical` が実行される | ✅ 実装済み | - | `ci.yml` に `npm audit --audit-level=critical --omit=dev` を追加。TC-07 で exit 0 確認済み |
| 3 | CI の `backend-lint` ジョブで `pip-audit` が実行される | ✅ 実装済み | - | `pip-audit -r requirements.txt` を `backend-lint` ジョブに追加。TC-06・TC-07 で確認済み |
| 4 | bandit の除外設定が `backend/.bandit`（INI 形式）に集約され、CI の CLI 引数と一致している | ✅ 実装済み | - | `backend/.bandit` 新規作成。`ci.yml` の bandit CLI 除外引数（`-x ./*/migrations/,...`）は削除済み。TC-05 で確認済み |
| 5 | `detect-secrets` の JWT 検出状況を確認し、対応方針（追加 or 不要）を記録している | ✅ 実装済み | - | `.secrets.baseline` に `JwtTokenDetector` が追加確認済み。`docs/runbooks/pre-commit.md` に記録済み |
| 6 | `/implement` SKILL.md 手順2 から手動 lint・audit の実行指示が「pre-commit / CI が自動実行」に書き換えられている | ✅ 実装済み | - | 手動 lint 手順が「lint・セキュリティスキャン・依存関係 CVE チェックは pre-commit（commit 時）と CI（PR 時）が自動実行する」に置き換え済み |
| 7 | `docs/runbooks/pre-commit.md` のフック一覧が最新の状態に更新されている | ✅ 実装済み | - | Ruff・bandit・ESLint の 3フックを追加。detect-secrets JWT 対応方針（`--force-use-all-plugins` 必須の注意書き含む）を記録済み |
| 8 | コーディング規約ドキュメントに linter / 手動確認の区分が明示されている | ✅ 実装済み | - | `rules/ultimate_django_coding_standards.md`・`rules/react-coding-standards-integrated.md` の冒頭に「自動強制範囲」「手動レビュー対象」セクションを追加済み |
| 9 | CI の全ジョブが pass する | ✅ 実装済み | - | TC-01（Backend 25 passed）・TC-02（Frontend 7 passed）実施済み。前回レビュー（I055_code_review_20260430_1600.md）で Medium 指摘だった TC-15・TC-16 の結果空白は最新コミット（c54c1cd）で解消済み。各 TC 実行結果テーブルに TC-15 PASS・TC-16 PASS が記録されている |
| 10 | `docs/runbooks/plan-writing-rules.md` の事前調査セクションに lint/audit/scan 系ツール導入原則が追加されている | ✅ 実装済み | - | 「lint/audit/scan 系ツール導入時の副作用事前調査（必須）」セクションが plan-writing-rules.md に追加済み。TC-15 で確認済み |
| 11 | `.claude/review-agents/code-reviewer.md` に CI-aware テスト結果基準が追加されている | ✅ 実装済み | - | P4 セクションに「CI が全ジョブ pass していれば Low 以下で扱う」条件付き基準が追記済み。TC-16 で確認済み |

## コード品質・セキュリティ指摘

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | ベストプラクティス | `backend/problems/utils.py` 行261-264 の implicit string concatenation で、1行目は `f"..."` だが2行目は f-prefix なしの `"..."` になっている。現状は変数埋め込みがなく正しく動作するが、将来の変更時に `{variable}` を2行目に追加しても f-prefix がなければリテラルとして展開される silent bug のリスクがある。前回コードレビュー（I055_code_review_20260430_1600.md）でも Low で指摘されており、本レビュー時点でも未修正のまま残っている | `backend/problems/utils.py` 行261-264 | 修正推奨（2行目に `f` prefix を付けるか、単一 f-string にまとめること） |
| Low | P4. テスト妥当性 | TC-03b（コミットブロック異常系）のインプット `import os,sys,re` が Ruff E/F/W ルールで検出されないため exit 0 になっており、「エラーがあればブロックされる」の異常系証拠として弱い。前回コードレビューで Low 指摘済みだが引き続き未解消。`x = 1` 等 F841（未使用変数）など確実に検出されるルール違反に変更することを推奨 | `docs/tests/open/I055_auto_test.md` TC-03b | 改善提案（次のイシューで対応可） |
| Low | P8. コスト・保守負荷 | `backend-lint` CI ジョブで `pip install -r backend/requirements-dev.txt`（21行）のみインストール後、`pip-audit -r requirements.txt` ステップを実行しているが、pip-audit の対象は本番依存（requirements.txt）でありこれは未インストールの状態でも静的スキャンとして機能する。一方 `Check migration drift` ステップ（33行）で `pip install -r requirements.txt` を別途実行しており、2段階インストールになっている。意図は理解できる（本番依存のインストールを最小化）が、コメントでその設計意図（pip-audit は未インストール状態でもスキャン可能なため）を明記すると保守性が上がる | `.github/workflows/ci.yml` 21-35行 | 改善提案 |
| Low | 運用性 | `docs/reviews/open/I055_review.md` の「変更概要」「変更点」「実装完了日」等の欄が空白のまま残っている。コードレビューとテストドキュメントには詳細な記録があるが、このサマリーレビューファイルが未記入のため将来の振り返りで参照されたときに混乱する可能性がある | `docs/reviews/open/I055_review.md` | 改善提案（対応任意） |

（指摘内容すべて重大度 Medium 以下）

## 高リスク判定

判定: No
該当条件: なし

---

✅ コードレビュー完了。`/test [I055]` を実行してください。
