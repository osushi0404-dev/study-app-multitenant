## 決定論ゲート実行結果（code-review.sh 自動注入）

(決定論ゲート宣言なし)- 高リスク判定: YES（LLM=YES / path=YES）

- omission-lint: HIGH
- FINAL VERDICT（gate/omission/LLM 合成）: HIGH

---

全ファイルを確認しました。レビューを出力します。

---

```
# コードレビュー [I146] ── [2026-08-06 12:00]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| AC-1 | CONTRIBUTING.md 存在・(a)PR非受付+理由/(b)Issue歓迎+対応不保証/(c)SECURITY.md誘導、英日両対応 | ✅ 実装済み | - | TC-06 PASS。英語先頭・日本語後段。3セクション全て確認済み |
| AC-2 | SECURITY.md 存在・GitHub Private vulnerability reporting 誘導・メールアドレス記載なし | ✅ 実装済み | - | TC-06 PASS。「Report a vulnerability」への誘導あり。メール不在 grep 確認済み |
| AC-3 | Private vulnerability reporting が有効（API `{"enabled":true}` 返却） | ❌ 未実施 | High | TC-07 ⏸ 保留。現状 `{"enabled":false}`。`gh api -X PUT repos/osushi0404-dev/study-app-multitenant/private-vulnerability-reporting` がユーザー承認待ちで未実行。omission-lint: HIGH と一致 |
| AC-4 | `.github/pull_request_template.md` 冒頭1行追加・既存項目無改変 | ✅ 実装済み | - | TC-06 PASS。`> 外部からの PR は受け付けていません（CONTRIBUTING.md リンク）。`1行追加、既存 `## 概要` 以降は無改変 |
| AC-5 | `pr-base-sync.sh` cross-repo PR→exit 1（sync/final 両モード） | ✅ 実装済み | - | T17（sync）/ T18（final）PASS。MODE 分岐（L88/L100）より前の L23-36 に配置。両モード共通で必ず走る |
| AC-6 | 取得失敗・空値・想定外値でも exit 1（fail-closed） | ✅ 実装済み | - | T19（照会失敗 `||` で捕捉）/ T20（空値→`case *`）/ T21（想定外値→`case *`）PASS |
| AC-7 | T1〜T16 従来挙動不変 | ✅ 実装済み | - | TC-01 PASS (21/21 cases, 30 assertions)。スタブ `*isCrossRepository*` 分岐追加で既存ケースへの副作用なし |
| AC-8 | guard: merge=block / ready・edit・review --approve=ask / read-only・close・comment・review --comment=素通し | ✅ 実装済み | - | G1〜G12 PASS。`_GH_PR_BLOCK_VERBS`/`_GH_PR_ASK_VERBS`/`_GH_PR_APPROVE_FLAGS` の定数分岐が正確 |
| AC-9 | guard MCP: merge=block / update・review(APPROVE)=ask | ✅ 実装済み | - | G13〜G16 PASS。`_MCP_PR_BLOCK_TOOLS`/`_MCP_PR_ASK_TOOLS` + event=APPROVE 絞り込みが正確 |
| AC-10 | settings.json PreToolUse matcher に MCP 3ツール登録・既存2エントリ無改変 | ✅ 実装済み | - | TC-06 PASS。`grep -c pretooluse_guard.py .claude/settings.json` = 3（既存2+新規1）。既存 Bash / Edit matcher は L143-162 で確認 |
| AC-11 | gh 照会失敗時→block ではなく ask 降格 | ✅ 実装済み | - | G17（`GH_FAIL_CROSS=1`）= ASK PASS。`_pr_is_cross_repo` が `None, None` 返却→`_guard_cross_repo` → `_ask()` 経路 |
| AC-12 | 自前 PR への操作挙動不変 | ✅ 実装済み | - | G2/G18/G19 PASS。`cross is False → return`（素通し）が正確 |
| AC-13 | SKILL.md step 0 に `isCrossRepository` 確認 + true 時 STOP 手順記載 | ✅ 実装済み | - | TC-06 PASS。コマンド `gh pr view <PR番号> --json isCrossRepository -q .isCrossRepository` + STOP 指示 + workflow.md 参照リンクが同粒度で記載 |
| AC-14 | workflow.md に外部PR扱い記載（Actions承認しない・追従なし・方針close） | ✅ 実装済み | - | TC-06 PASS。5ステップ記載。L253「Actions の実行を承認しない」明記 |
| AC-15 | 両テスト全PASS・push guard 回帰なし | ✅ 実装済み | - | TC-01(21/21)/TC-03(pass=34 fail=0)/TC-08(push guard pass=87/worktree pass=27/checkout pass=14) |
| AC-16 | 否定・回帰TC が false-green でない（注入で NG になる） | ✅ 実装済み | - | TC-02 PASS（`sed` 注入版で T17/T18 NG・非ゼロ終了）。TC-04 PASS（FG-A/C/D が exit 0、FG-B/E が exit 2）。FG-C は `return False, None`（2-タプル）で正確 |
| AC-17 | MCP で pull_number 取得不能→照会せず ask 降格 | ✅ 実装済み | - | G23 PASS。`require_ident=True` かつ `not ident` → `_ask()` 直行（gh 照会せず） |
| AC-18 | 対象外 MCP ツール→素通し・gh 照会なし | ✅ 実装済み | - | G24/G24b PASS。`_mcp_pr_target` の else 節で `return None`→ガードスキップ。`GH_LOG` ゼロ行を確認 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| High | P1 要件適合性 | AC-3 未達: Private vulnerability reporting の有効化が未実施。TC-07 が⏸保留（現状 `{"enabled":false}`）。イシューの受け入れ条件に明記されたインフラ設定が未適用であり、SECURITY.md で誘導している機能が実際には使えない状態。omission-lint: HIGH と一致。 | `docs/tests/open/I146_auto_test.md` TC-07 / 計画書 §6 ステップ 4-5 | ユーザー承認のうえ `gh api -X PUT repos/osushi0404-dev/study-app-multitenant/private-vulnerability-reporting` を実行し TC-07 を PASS させてから再レビュー |
| Low | ベストプラクティス | `_guard_cross_repo` 内で `_ask()` / `_block()` 呼び出し直後に明示的 `return` がない。両関数は内部で `sys.exit()` を呼ぶため実害はない（コードは unreachable）が、意図を明確にするためのコメントまたは `return` の追加が望ましい | `pretooluse_guard.py` L516-519, L521-527 | 改善提案（機能上の問題なし） |
| Low | ベストプラクティス（設計記録） | `gh pr merge`（fork PR）は `DANGER_OK=1` で迂回可能（block が `if not danger_ok:` 内側に配置されているため）。イシューでは「方針上ありえず不可逆」と記述されているが、実装は明示エスケープを許容している。計画書 §5-1 の配置根拠に意図的設計として明記済みであり、danger-ops.md の既存枠組みとの整合は取れている。 | `pretooluse_guard.py` L582, L647-649 / 計画書 §5-1 配置の根拠 | 設計判断として記録済み。修正不要 |

## 高リスク判定
判定: Yes
該当条件:
- 【harness 固有】hooks・ガード・権限設定の変更（`scripts/claude/hooks/pretooluse_guard.py`・`.claude/settings.json`・`scripts/claude/pr-base-sync.sh`）
- 【harness 固有】指示階層の変更（`.claude/skills/close/SKILL.md`）

---
High のみ: 「`/fix-loop [I146]` を実行してください。fix-loop 完了後は `/code-review [I146]` に戻ってください。」

具体的にすべき操作:
1. ユーザー承認のうえ `gh api -X PUT repos/osushi0404-dev/study-app-multitenant/private-vulnerability-reporting` を実行
2. TC-07 を再実行して `{"enabled":true}` を確認し、`I146_auto_test.md` の TC-07 欄を ✅ で更新する

RISK: YES
VERDICT: HIGH
```
