## 決定論ゲート実行結果（code-review.sh 自動注入）

- `bash scripts/claude/tests/test_wt_lifecycle.sh` → exit=0 ✅
- `bash -n scripts/claude/wt-new.sh` → exit=0 ✅
- `bash -n scripts/claude/wt-remove.sh` → exit=0 ✅
- `grep -q "wt-new" docs/runbooks/worktree.md` → exit=0 ✅
- `grep -q "wt-remove" docs/runbooks/worktree.md` → exit=0 ✅

- omission-lint: OK
- FINAL VERDICT（gate/omission/LLM 合成）: OK

---

# コードレビュー [I096] ── [2026-07-02 14:45]

## 受け入れ条件の照合
| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | 作成スクリプトが引数検証（番号=数字・track/概要 は許可文字のみ）と worktree path/branch の既存衝突検査を行い、不正時は明示エラーで停止する | ✅ 実装済み | - | `wt-new.sh` L28-30: `grep -Eq` allowlist・L40-41: path/branch 事前検査。TC-N7(a/b/c)・TC-N8・TC-N9 で決定論検証済み（決定論ゲート exit=0） |
| 2 | worktree の作成先は primary checkout の親ディレクトリから導出する（/mnt/c/app を直書きしない） | ✅ 実装済み | - | `wt-new.sh` L33-36: `git worktree list --porcelain \| sed -n 's/^worktree //p' \| head -1` → `dirname`。TC-N1 が `/mnt/c/app` 直書き時に失敗する設計で検出保証 |
| 3 | 作成スクリプトが基点を `origin/develop` に固定し、main 等の誤基点で空ツリーを作れない | ✅ 実装済み | - | `wt-new.sh` L53: `git worktree add "$WT_PATH" -b "$BRANCH" origin/develop`。TC-N2 で `only-develop-file` 在 / `only-main-file` 無を確認 |
| 4 | 作成スクリプトが backend .env を primary から新 worktree へコピーし、欠落/コピー失敗時は明示エラーで停止する。`--env-source` で source を上書きできる | ✅ 実装済み | - | `wt-new.sh` L44-45 (欠落停止・orphan 非作成)、L56-57 (コピー後存在確認)、L19-21 (`--env-source`)。TC-N3・N4・N10 で検証。TC-FG2 で false-green 反証 |
| 5 | 作成スクリプトは既定で up せず、--up 指定時のみ docker compose up -d する | ✅ 実装済み | - | `wt-new.sh` L16 `DO_UP=0`・L66-68 条件分岐。TC-N5 (up 未呼出)・TC-N6 (up 呼出) で確認 |
| 6 | 撤去スクリプトが未コミット変更・未 push コミットを検出したら --force 不使用で中止する | ✅ 実装済み | - | `wt-remove.sh` L28-34: `git status --porcelain` + `git branch -r --contains HEAD`。TC-R4・TC-R5 で検証。DIRTY 変数への代入で git 失敗時 `set -e` が fail-safe に働く |
| 7 | 撤去スクリプトの `docker compose down -v` は DANGER_OK=1 未設定時は実行されず明示エラーで停止する | ✅ 実装済み | - | `wt-remove.sh` L41-50: `if [ "${DANGER_OK:-}" = "1" ]`。TC-R1 + TC-FG1 (除去複製では未設定でも down される) で false-green 反証済み。決定論ゲート exit=0 |
| 8 | 撤去スクリプトが `docker compose down -v`（当該ディレクトリ）→ `git worktree remove` の順で実行する | ✅ 実装済み | - | `wt-remove.sh` L43: `cd "$WT_PATH" && docker compose down -v`・L53: `git worktree remove`。TC-R3 で docker stub の `present=yes`（WT が `down` 実行時に存在）を確認 |
| 9 | `COMPOSE_PROJECT_NAME` が global export されている場合に警告が出る | ✅ 実装済み | - | `wt-new.sh` L47-49・`wt-remove.sh` L37-38。TC-C1 で確認（警告のみ・処理継続） |
| 10 | 上記を検証する自動テストが `scripts/claude/tests/` に追加され PASS（Docker デーモン非依存） | ✅ 実装済み | - | `test_wt_lifecycle.sh` 55/55 PASS（2026-07-02）。temp git repo + docker PATH スタブで実 Docker デーモン非依存。false-green 注入 TC-FG1・FG2 で否定・停止の実効性を反証。決定論ゲート exit=0 |
| 11 | `docs/runbooks/worktree.md` §3/§5 がスクリプト利用手順に更新されている（手動手順は fallback として保持） | ✅ 実装済み | - | §3 に推奨スクリプト例（L31-43）＋「fallback（スクリプトを使わない手動手順）」（L45-59）・§5 に推奨注記（L79）＋(B)(C)(D) に機械化旨1行ずつ追記（L90-92）。手動手順は削除せず保持。runbook grep 決定論ゲート exit=0 |

## コード品質・セキュリティ指摘
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Low | ベストプラクティス | `[ -n "$SRC_ROOT" ]` は `dirname "$(dirname ...)"` が常に非空文字を返すため恒真。実害はないが冗長。 | `wt-new.sh` L61 | 次回以降の改善余地（当該 PR では対応不要） |

（上記1件を除き指摘なし）

## 高リスク判定
判定: No
該当条件: なし（Django/React アプリコード変更なし。開発ハーネスのスクリプト追加のみ。`docker compose down -v` は `DANGER_OK=1` ゲートで既存 danger-ops 枠組みに整合済み。`.env` は中身を読まず `cp`+`test -f` のみ・deny リストと非衝突。入力は allowlist で検証しコマンドインジェクション面を絞済み）

---

✅ コードレビュー完了。`/test [I096]` を実行してください。

VERDICT: OK
