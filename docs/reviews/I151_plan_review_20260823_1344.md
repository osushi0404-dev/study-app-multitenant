すべてのファイルは本セッションのコンテキストに含まれているため、Read ツールの実行は省略し、直接レビューを行います。

---

```
# 計画書レビュー [I151] ── [2026-08-23 00:00]

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP/コマンド衛生 | TC-AUTO-03/10/11A/11B/12/14/15/16 で `&&` とパイプによる複合コマンドが使われている。TC-AUTO-15（`pip freeze > /tmp/... && grep ...`）や TC-AUTO-11A（宣言確認の `&&` チェーン）は分割可能な部分を束ねており、Claude Code の bash allowlist に一致しない場合は承諾プロンプトが発生し自動テスト工程が止まるリスクがある。合否を 1 つの exit code に集約する必要性があるケース（TC-AUTO-03 の 3 条件同時判定など）は一定の合理性があるため Blocker とはしない | `docs/tests/open/I151_auto_test.md` 全 TC のコマンド欄 | 修正推奨：ログ保存は単体コマンドで完結させ（`docker compose exec -T backend pip freeze > /tmp/i151_freeze.txt`）、判定の grep は別コマンドとして実行する方式に揃えることで承諾プロンプトを回避できる |
| Warning | P4/テスト妥当性 | TC-AUTO-11A の実インストール確認（`pip freeze` 照合）で `django-cors-headers==4.9.0` と `django-extensions==4.1` の 2 パッケージが漏れている。宣言側（`requirements.txt` の grep）には両パッケージが含まれているが、実インストール側の照合から除外されており、再ビルド漏れ検出の網羅性が不完全 | `docs/tests/open/I151_auto_test.md` TC-AUTO-11A「実インストール側」コマンド | 修正推奨：`pip freeze` 照合に `grep -qx 'django-cors-headers==4.9.0'` と `grep -qx 'django-extensions==4.1'` を追加する。なお `django-extensions` は `pip freeze` で `django-extensions==4.1` と表記されることを事前に確認すること |

（上記 2 件以外の観点すべてで問題なし）

## 各観点の詳細

### P1. 要件適合性・業務ロジックの正しさ
問題なし。受け入れ条件 AC-01〜AC-20 のすべてが計画書の AC 対応表・テスト文書の TC に漏れなく対応している。スコープ外の仕様追加・変更なし。マルチテナントの絞り込みは自前実装で guardian に依存していないことが §2-4 の調査で確認済み。

### ベストプラクティス
コマンド衛生に関しては上記 Warning 参照。それ以外：責務分離・Fat View・外部 API 依存の未実証・「未知リスク」の外部前提については、Docker の環境前提・TC-AUTO-09 の `-e DB_NAME=migrate_check` 挙動・TC-AUTO-14 の `gh pr checks --json` 判定式の動作すべてが §2（機構の事前検証）で実測済み（問題なし）。

### セキュリティ
問題なし。guardian は `AUTHENTICATION_BACKENDS` に未登録のため除去しても認可判定経路に影響しない（§2-4・§16）。`DEFAULT_PERMISSION_CLASSES` への影響なし（新規 View/ViewSet 追加なし）。`pip-audit` が本体・開発依存の双方で 0 件（§2-3 計画時点実測）。計画書 §15 で実装後の `/security-review` 実施を明記済み。OWASP A06 の是正が本イシューの目的で整合している。

### モダンなウェブアプリ開発
問題なし。API 契約・フロントエンドのコードに変更なし。TC-AUTO-12（Jest 10 件）と TC-AUTO-13（E2E）で無退行確認。

### Claude Code ベストプラクティス
対象外（指示ファイルへの変更を含まない）。

### P4. テスト妥当性・回帰防止
TC-AUTO-11A の実インストール確認の不完全性は上記 Warning 参照。それ以外：TC-AUTO-03 の件数・skip/xfail/deselect の 3 条件同時判定は適切。TC-AUTO-04/05/06/07 の否定系 TC はすべて false-green 検証（実装前状態またはダミー注入で exit 1 になることを確認）が実施済みで記録されている（問題なし）。合否基準が合格時に exit 0・不合格時に非ゼロとなることも全 TC で確認済み。

### P3. データ整合性・変更安全性
問題なし。新規マイグレーション生成なし（事前実測 `No changes detected`）。guardian_* テーブルは意図的残置で計画書 §10 に明記。マイグレーション検証は使い捨て DB（TC-AUTO-09）＋CI E2E（TC-AUTO-13）の 2 経路で担保（決定 7-A・§10）。ロールバック手順は §8 に段階別で明記。

### P5. 運用性・障害対応性
問題なし。celery の成功まで確認する方針（TC-AUTO-10）・`pip freeze` 記録（TC-AUTO-15）・`imghdr` の `DeprecationWarning` は Python 3.11 では影響なしと記録（§2-8）。

### P8. コスト・保守負荷
問題なし。依存が 1 つ減り保守対象が縮小する。

### P9. プライバシー・コンプライアンス
対象外（個人情報・テナントデータの取り扱いに変更なし）。

---

## 高リスク判定
判定: Yes
該当条件:
- 認証・認可ライブラリ（django-guardian）の依存除去を含む
- 複数の主要依存パッケージのメジャーバージョン更新（DRF 3.15→3.18 / django-redis 5→7 / django-filter 23→26）

推奨: `guardian` は `AUTHENTICATION_BACKENDS` に未登録で実認可に使われていなかったことは実測済み。計画書 §15 でも実装後に `/security-review` を実施する旨（承認ポイント S-4）が明記されています。`/implement [I151]` 実行後、自動テスト・CI の全確認が完了したら `/security-review [I151]` を実行してから PR をマージしてください。

VERDICT: HIGHRISK
```
