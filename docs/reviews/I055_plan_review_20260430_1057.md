# 計画書レビュー [I055] ── 2026-04-30 00:00

## 判定: ✅ 完了

## レビュー指摘一覧

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP | ステップ6の bandit pre-commit フックで `exclude` オプションが `files: ^backend/` フィルタと `exclude: ^backend/.*/migrations/\|^backend/.*/tests/` で二重定義されているが、`backend/.bandit` の `exclude_dirs = migrations,tests` と重複している。pre-commit 側の `exclude` オプションを削除して `.bandit` に一元化するほうが管理箇所が減る。ただし `files: ^backend/` フィルタは backend スコープ絞り込みとして意味があるので残すこと | `plan_I055.md` ステップ6 bandit フック設定 | 修正推奨（実装可能） |
| Warning | セキュリティ | ステップ8 の CI 設定で `backend-lint` ジョブに `pip-audit -r requirements.txt` があるが、ステップ4-1 で pytest を `requirements-dev.txt` に移動した後も `requirements-dev.txt` 中の pip-audit 依存のみ評価しており、`requirements.txt` 変更後の CVE を CI で網羅するかどうかの確認フロー（`-r requirements.txt -r requirements-dev.txt` 両ファイル指定）が曖昧。本番依存のみスキャン（requirements.txt のみ）が意図なら明記が必要 | `plan_I055.md` ステップ8 pip-audit ステップ | 修正推奨（意図の明示） |
| Warning | モダン | ステップ5の `npm audit fix` 後に `npm audit --audit-level=critical` でゼロ件になることが期待されているか、11件の react-scripts 依存が残る前提でゼロ件確認は `critical` 閾値のみで pass するのかが計画書内では記述があるが、自動テスト文書（TC-07）では `npm audit --audit-level=critical` の期待値が「記述が存在する」（CI ファイル内の記述確認）にとどまり、実際に audit が pass することの検証 TC が欠落している。ただし手動テスト No.4 で同等確認がある | `I055_auto_test.md` TC-07 / `I055_manual_test.md` No.4 | 修正推奨（auto TC に実行確認を追加） |
| Info | BP | ステップ6b の `call_command` 置き換え実装コードで `except Exception as e:` をブロードキャッチしているが、ステップ6b の説明では B110（bare except）修正の一環として `except (ValueError, OSError):` のような限定 except への変更を方針としている。`_analyze_error` では呼び出し先が `analyze_logs` command であり内部で様々な例外が発生しうるため一定の理由はあるが、コード例との一貫性がない点は実装者が混乱する可能性がある | `plan_I055.md` ステップ6b `_analyze_error` コード例 | 改善提案（対応は任意） |
| Info | BP | 自動テスト文書に `TC-03b` （コミットブロック異常系）が含まれているが、期待値が「exit code が非ゼロ」のみで「どのフックが検出したか（Ruff/bandit/ESLint のいずれか）」のログ確認が期待値に含まれていない。振る舞いテスト観点では exit code だけで十分だが、デバッグ容易性のためフック名のログ出力確認を追加すると品質が上がる | `I055_auto_test.md` TC-03b | 改善提案（対応は任意） |

## 高リスク判定

判定: No
該当条件: なし
推奨: `/implement [I055]` を実行してください。
