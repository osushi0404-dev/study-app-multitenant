全ファイルの Read 完了。レビューを開始します。

---

```
# 計画書レビュー [I080] ── 2026-06-28 15:45

## 判定: ✅ 完了

## レビュー指摘一覧
| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Warning | BP（コマンド衛生） | TC-DOC3 が 3段パイプ `git diff origin/develop -- docs/claude-code-structure.md \| grep -E '^\+' \| grep -E 'I0[0-9]{2}'` になっており、メモリ `feedback_atomic_allowlisted_commands` の「パイプで束ねない」に抵触。Claude が Bash ツールで実行すると `Bash(git diff *)` allow とマッチしても複合形として ask プロンプトが発生しうる。`git diff ... > /tmp/doc_diff.txt` → `grep -E '^\+' /tmp/doc_diff.txt > /tmp/doc_added.txt \|\| true` → `grep -E 'I0[0-9]{2}' /tmp/doc_added.txt` の3ステップに分割可能。ただし TC-DOC は auto_test.md 上の個別確認コマンドであり、失敗条件が壊れるわけではない | `docs/tests/open/I080_auto_test.md` TC-DOC3 検証コマンド | 分割コマンドへの書き換えを推奨（CI 組み込みや Claude 実行時のプロンプト回避のため） |
| Info | BP（TC 合否判定の方向性） | TC-DOC1・TC-DOC3 は「grep がマッチなし（非ゼロ終了）＝合格」という反転 convention。TC-DOC2 は「マッチあり（exit 0）＝合格」と逆向き。それぞれ「合格条件」列に明文化されているため誤解は防がれているが、同一 TC-DOC 群で成功条件の方向が異なる点は将来的なメンテ時の混乱源になりうる | `docs/tests/open/I080_auto_test.md` TC-DOC1・TC-DOC2・TC-DOC3 | TC 冒頭または各行備考に「非ゼロ終了が合格」と明記するコメントを 1行追加推奨 |

### 前回 Blocker（20260628_1505）の解消確認
- **Blocker（P4）: TC-FALSEGREEN-C/D 欠落** → 現 `I080_auto_test.md` に TC-FALSEGREEN-A/B（`_push_protected_target` 無効化）・TC-FALSEGREEN-C/D（`--all`/`--mirror` regex 無効化）が両方追加されており解消 ✓
- **Warning（TC-S ワンライナー未記述）** → TC-S2/S3/S4/S5 すべてに Python ワンライナーが明記されており解消 ✓
- **Warning（TC-DOC3 コマンド未記述）** → 具体的な diff+grep コマンドが追記されており「コマンド不在」は解消。ただし新たに上記「コマンド衛生 Warning」が派生 ✓（降格相当）

### 主要設計の正当性確認（Blocker 候補の事前確認）
- **宛先正規化ロジック（`_norm_push_dest`）**: `+develop`→`develop`・`HEAD:develop`→`develop`・`refs/heads/main`→`main`・`HEAD:refs/heads/main`→`main` の各変換パスを確認。完全一致判定により `develop-fix`/`main-backup` が誤遮断しない ✓
- **引数なし push の現ブランチ判定**: `git push`・`git push origin`・`git push -u origin` いずれも `refspecs = []` 分岐で `_current_branch()` を参照するコード経路を確認 ✓
- **`git -C /path push origin develop` の検出**: `_segments` がトークン全体を渡し `toks.index("push")` で正しく後続引数を取得するため TC-P27 が通る ✓
- **DANGER_OK escape hatch**: `danger_ok = raw.lstrip().startswith("DANGER_OK=1 ")` により、deny エントリは `DANGER_OK=1 ` プレフィックスでマッチ外れ・フックは `if not danger_ok:` ブロックをスキップ、という二重解除経路が整合 ✓
- **false-green 注入の手法**: TC-FALSEGREEN-A の `sed 's/dest = _push_protected_target(cmd)/dest = None/'` は `if dest:` を常 False 化する正しい無効化。TC-FALSEGREEN-C の `sed 's/(--all|--mirror)/(--xxall|--xxmirror)/'` は BRE では括弧が literal として機能しソース中の `(--all|--mirror)` 文字列を正確に書き換える ✓
- **未実証部分の扱い**: settings.json 権限レイヤー挙動を「稼働中セッションでは実証不能」と認識し、プレーン default 新規セッション＋ステップ1ゲートを manual_test TC-M1〜M3 に明記。review 基準 (c)「実証不能クラス・指定実施環境＋ステップ1前倒し明記」に該当 ✓

## 高リスク判定
判定: Yes
該当条件:
- ハーネスのツール実行権限変更（`git push *` を ask→allow 化）：Claude Code の認可権限の変更に相当
- セキュリティガード（`pretooluse_guard.py`）の既存 protected-push 判定ロジックを全置換するリファクタリング：実装ミスが即 protected push の素通りに直結
- 既存事故の再発リスク：guard の穴を塞ぐ変更だが、置換改修により新たな穴が生まれるリスクがある

推奨: `/security-review [I080]` を実行してから `/implement [I080]` へ進んでください。

VERDICT: HIGHRISK
```
