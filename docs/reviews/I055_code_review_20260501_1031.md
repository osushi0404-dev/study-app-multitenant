# コードレビュー [I055] ── [2026-04-30 16:30]

## 受け入れ条件の照合

| # | 受け入れ条件 | 実装状況 | 重大度 | 備考 |
|---|------------|---------|--------|------|
| 1 | `git commit` 時に Ruff / bandit / ESLint が自動実行され、エラーがあればコミットがブロックされる | ✅ 実装済み | - | `.pre-commit-config.yaml` に ruff・bandit・ESLint フックを追加。TC-03 全フック pass、TC-17 で F841 exit 1 確認済み |
| 2 | CI の `frontend-lint` ジョブで `npm audit --audit-level=critical` が実行される | ✅ 実装済み | - | `ci.yml` frontend-lint に `npm audit --audit-level=critical --omit=dev` を追加。TC-07 で exit 0 確認済み |
| 3 | CI の `backend-lint` ジョブで `pip-audit` が実行される | ✅ 実装済み | - | `pip-audit -r requirements.txt` を backend-lint に追加。TC-06 で存在確認済み |
| 4 | bandit の除外設定が `backend/.bandit`（INI 形式）に集約され、CI の CLI 引数と一致している | ✅ 実装済み | - | `backend/.bandit` 作成済み、ci.yml の `-x` 引数削除済み。TC-05・TC-11 で確認済み |
| 5 | `detect-secrets` の JWT 検出状況を確認し、対応方針を記録している | ✅ 実装済み | - | `.secrets.baseline` に `JwtTokenDetector` が plugins_used に存在。`pre-commit.md` に記録済み |
| 6 | `/implement` SKILL.md 手順2 から手動 lint・audit の実行指示が書き換えられている | ✅ 実装済み | - | 「pre-commit と CI が自動実行する」に書き換え済み |
| 7 | `docs/runbooks/pre-commit.md` のフック一覧が最新の状態に更新されている | ✅ 実装済み | - | Ruff・bandit・ESLint フックの追加と JWT 対応方針が記録されている |
| 8 | コーディング規約ドキュメントに linter / 手動確認の区分が明示されている | ✅ 実装済み | - | `rules/` 2ファイルの冒頭に「自動強制範囲」「手動レビュー対象」セクションを追加済み |
| 9 | CI の全ジョブが pass する | ✅ 実装済み | - | TC-01 25 passed（2026-04-30）、TC-02 7 passed（2026-04-30）。フロー上テスト結果欄は /test 実施前の正常状態 |
| 10 | `docs/runbooks/plan-writing-rules.md` の事前調査セクションに lint/audit/scan 系ツール導入原則が追加されている | ✅ 実装済み | - | TC-15 PASS。「lint/audit/scan 系ツール導入時の副作用事前調査（必須）」セクション追加済み |
| 11 | `.claude/review-agents/code-reviewer.md` に CI-aware テスト結果基準が追加されている | ✅ 実装済み | - | TC-16 PASS。P4 セクションに条件付き基準追記済み |
| 12 | TC-03b 異常系インプットが F841 を発動するコードに修正され exit 1 確認済み | ✅ 実装済み | - | TC-17 PASS（2026-05-01）。F841 exit 1 確認済み |
| 13 | `backend/problems/utils.py` 行261-264 の implicit string concatenation が解消されている | ✅ 実装済み | - | TC-18 PASS（2026-05-01）。両行に f-prefix 付与済み |
| 14 | `plan-issue/SKILL.md` に依存関係変更時の Docker 影響確認チェックが追加されている | ✅ 実装済み | - | TC-19 PASS（2026-05-01）。109行に記述存在 |
| 15 | `plan-issue/SKILL.md` の文書品質ゲートに lint 異常系テストのインプット事前確認が追加されている | ✅ 実装済み | - | TC-20 PASS（2026-05-01）。141行に記述存在 |

## コード品質・セキュリティ指摘

| 重大度 | 観点 | 指摘内容 | 該当箇所 | 対応 |
|--------|------|---------|---------|------|
| Medium | ベストプラクティス | `backend/studylogs/mistake_analysis.py` の E501 修正（行318・335・398）で、implicit string concatenation の f-prefix なし問題が再発している。utils.py の TC-18 修正では両行に f-prefix を付けることで解消したが、mistake_analysis.py では1行目が `f"..."` で2行目が `"..."` のままになっており、utils.py の旧状態と同じパターン。将来 {variable} を2行目に書くとサイレントにリテラルとして展開される。同一 PR 内で修正した問題と同じパターンが別ファイルで残存している | `backend/studylogs/mistake_analysis.py` 行318-320、333-335、396-398 | 修正推奨（2行目に `f` プレフィックスを付与すること） |
| Medium | P8. コスト・保守負荷（計画書ルール遵守） | `docs/plans/open/plan_I055.md` のステップ1（行97-100）とステップ4-0（行180）に実行コマンドが本文内にそのまま残っている。本イシューで制定した「ステップ本文に検証コマンドを書かない・TC に昇格する」ルール（plan-writing-rules.md への規則追加、TC-12 で確認済み）に計画書自身が違反している。完了済みステップの記録としての位置づけではあるが、自己矛盾として残存している | `docs/plans/open/plan_I055.md` ステップ1（行97-100）・ステップ4-0（行180） | 修正推奨（`→ TC-03 参照（実施済み）` 等の注記形式に書き換えること） |
| Low | セキュリティ | `backend-lint` CI ジョブの pip-audit スキャン対象が `requirements.txt`（本番依存のみ）に限定されており、`requirements-dev.txt`（ruff, bandit, pip-audit 自身等）の既知 CVE は検知されない。計画書コメントに根拠はあるが、設計判断テーブル（セクション10）への昇格がなく、未来のメンテナにとって判断根拠が不明瞭。過去の計画書レビュー（20260430_1537・20260501_1014）でも Warning として指摘済みだが未対応 | `docs/plans/open/plan_I055.md` ステップ8 pip-audit コメント・セクション10 設計判断テーブル | 改善提案（設計判断テーブルに昇格させること） |
| Low | ベストプラクティス | `docs/runbooks/pre-commit.md` 行50-52 で「`JwtTokenDetector` は有効（`--force-use-all-plugins` でベースライン生成済み）」と記載されているが、`.pre-commit-config.yaml` の detect-secrets フック args は `['--baseline', '.secrets.baseline']` のみで `--force-use-all-plugins` は含まれていない。実際には baseline の `plugins_used` に `JwtTokenDetector` が含まれているため検出は有効だが、ドキュメントの記載が「フラグで有効化」という誤解を招く表現になっている。ベースライン再生成時の運用コマンドとして正しいが、フック設定側との関係説明が不足している | `docs/runbooks/pre-commit.md` 行50-52 | 改善提案（「baseline の `plugins_used` 経由で有効」という説明に補足すること） |

## 高リスク判定

判定: No
該当条件: なし

---

✅ コードレビュー完了。`/test I055` を実行してください。
