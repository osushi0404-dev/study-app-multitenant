## 基本情報
- **計画書ID**: plan_I048
- **関連イシュー**: #99
- **作成根拠資料**: I046（レビュースキル8観点整合の実績）・docs/proposals/review_perspective_framework.md
- **実装後評価**: （未作成）
- **作成日**: 2026-04-15

---

# plan_I048: 上流スキルの部分カバー観点を8観点フレームワークに整合させる

## 1. 背景/目的

I047 で上流スキル（`/grill-me`・`/plan-issue`・`/implement`）に P3・P5・P8（ゼロカバー観点）を追加した。
しかし P1・P2・P4・P6・P7（部分カバー観点）は依然として断片的なカバーのみ存在する状態が残っている。

I046 でレビュースキルの同観点を整備した実績をもとに、上流スキルの対応箇所を明示・補完し、
上流→下流で8観点フレームワークが一貫して機能する状態に仕上げる。

## 2. 受け入れ条件

- [ ] `/grill-me` に P1・P4 の設計確認観点が追加されている（P7 は既存「既存方針との衝突がないか」の拡張で対応）
- [ ] `/plan-issue` に P1・P4・P6・P7 の確認項目が補完されている
- [ ] `plan-writing-rules.md` に条件付きセクション13（P6 性能・UX設計）が追加されている
- [ ] `/implement` に P1・P6・P7 の確認項目が補完されている
- [ ] 追加内容が I046 で整備したレビュー観点と対応している（上流・下流で同じ観点を参照できる）
- [ ] 既存のスキル指示文と矛盾・重複がない

## 3. 影響範囲

- Backend: なし
- Frontend: なし
- DB: なし
- Config/Infra: なし
- スキルファイル:
  - `.claude/skills/grill-me/SKILL.md`
  - `.claude/skills/plan-issue/SKILL.md`
  - `.claude/skills/implement/SKILL.md`
  - `.claude/skills/close/SKILL.md`（retro 予防処置 #1/#2 対応）
- Runbook:
  - `docs/runbooks/plan-writing-rules.md`

## 4. 変更点一覧

### 4-1. `.claude/skills/grill-me/SKILL.md`

| 変更種別 | 変更内容 |
|---------|---------|
| 修正 | 必須確認「既存方針との衝突がないか」の括弧内に P7 観点を追加（アンチパターン回避・新技術採用理由・状態管理設計の一貫性） |
| 追加 | 質問の観点に P1 項目：業務ロジック・マルチテナント制約 |
| 追加 | 質問の観点に P4 項目：テスト戦略 |

### 4-2. `.claude/skills/plan-issue/SKILL.md`

| 変更種別 | 変更内容 |
|---------|---------|
| 追加 | 「要件適合性・業務ロジックチェック（P1）」ブロックを承認前チェックに追加 |
| 追加 | 「テスト計画チェック（P4）」ブロックを承認前チェックに追加 |
| 追加 | 「性能・UX設計チェック（P6）」ブロックを承認前チェックに追加 |
| 追加 | 「設計品質チェック（P7）」ブロックを承認前チェックに追加 |
| 確認 | P2（セキュリティ）は既存「セキュリティ・ベストプラクティスチェック」で I046 整合済みのため変更なし |

### 4-3. `docs/runbooks/plan-writing-rules.md`

| 変更種別 | 変更内容 |
|---------|---------|
| 追加 | 条件付きセクション13「性能・UX設計（フロントエンド変更または性能要件がある場合のみ）」を追加 |

### 4-4. `.claude/skills/implement/SKILL.md`

| 変更種別 | 変更内容 |
|---------|---------|
| 追加 | 既存「P3・P5 実装確認チェック」を「P1・P3・P5・P6・P7 実装確認チェック」に拡張 |

### 4-5. `.claude/skills/close/SKILL.md`（retro 予防処置 #1）

| 変更種別 | 変更内容 |
|---------|---------|
| 追加 | ファイル移動前に `ISSUE_NUM` が3桁の数字であることを検証するバリデーションを追加 |
| 修正 | `mv` コマンドを `git mv` に置き換え（4箇所）。削除側（open/）と追加側（closed/）を同時にステージングし、I047 で発生した「open/ 削除の未ステージング」問題を防止する |

### 4-6. `.claude/skills/plan-issue/SKILL.md` への追記（retro 予防処置 #2）

| 変更種別 | 変更内容 |
|---------|---------|
| 追加 | テスト文書（`I###_manual_test.md`）生成指示に「実施者」判定基準を追記（Claude 可: ファイル確認・コマンド実行・ファイル比較 / Human のみ: ブラウザ操作・外部ツール・UX 感覚確認） |

## 5. 実装手順

**垂直スライス原則**: スキルファイルを単体で変更する（各ファイルが独立完結するため並行実施可能）。

### ステップ1: `grill-me/SKILL.md` の変更

**修正対象**: 必須確認 3 番目の箇条書き（既存方針との衝突がないか）

変更前:
```
- 既存方針との衝突がないか（既存アーキテクチャ・設計指針・運用ルールに反していないか）
```

変更後:
```
- 既存方針との衝突がないか（既存アーキテクチャ・設計指針・運用ルールに反していないか・旧来のアンチパターンを惰性で踏襲していないか・新技術を採用する場合は採用理由が明確か・状態管理の設計方針が既存コードと一貫しているか）
```

**追加対象**: 質問の観点の末尾に以下を追加:
```
- 業務ロジック・マルチテナント制約（ビジネスロジックを変更する場合）: マルチテナントの閲覧範囲・操作範囲の制約・ステータス遷移の条件・エッジケースでの業務ルール
- テスト戦略（新規の振る舞いを追加する場合）: TDD 適用範囲・テストレベルの選択（ユニット/結合/E2E）・再発防止テストの要否・認可テストの有無
```

### ステップ2: `plan-writing-rules.md` の変更

条件付きセクション12 の後（「重要:」ブロックの前）に以下を追加:

```markdown
13) 性能・UX設計（フロントエンド変更または性能要件がある場合のみ）
    - パフォーマンス懸念（N+1・キャッシュ・ページネーション設計）の対策方針
    - ローディング・空状態・エラー状態の表示方針
    - 破壊的操作（削除等）の確認導線設計
    - アクセシビリティ対応方針（キーボード操作・aria属性・コントラスト等）
```

### ステップ3: `plan-issue/SKILL.md` の変更

**追加対象**: 承認前チェックブロック群に P1・P4・P6・P7 ブロックを追加。
配置順は code-review スキルの観点順序（P1 → P2 → P4 → P3 → P6 → P7）に合わせる。

追加 P1 ブロック（セキュリティチェックの前に配置）:
```markdown
**要件適合性・業務ロジックチェック（必須）**
計画書の変更点について以下を確認する:
- 計画書が受け入れ条件の範囲を超えた仕様追加・仕様変更を含んでいないか
- マルチテナント・組織スコープの閲覧範囲・操作範囲の制約が計画書に明示されているか（該当する場合）
- ステータス遷移・承認条件・更新可否条件が計画書で正しく定義されているか（該当する場合）
- エッジケース・境界値での業務ルールが計画書で考慮されているか
```

追加 P4 ブロック（セキュリティチェックの後・データ整合性チェックの前に配置）:
```markdown
**テスト計画チェック（必須）**
計画書のテスト計画について以下を確認する:
- バグ修正の場合、再発防止テストがテスト計画に含まれているか
- テストレベルの選択（ユニット/結合/E2E の使い分け）が計画書に明示されているか
- 認証・認可・テナント境界を検証するテストケースが計画に含まれているか（認可変更がある場合）
```

追加 P6 ブロック（データ整合性チェックの後に配置）:
```markdown
**性能・UX設計チェック（該当する場合）**
計画書の変更点について以下を確認する:
- フロントエンド変更がある場合: ローディング・空状態・エラー状態の表示方針が計画書に含まれているか
- フロントエンド変更がある場合: 破壊的操作（削除等）の確認導線の設計があるか
- パフォーマンス懸念（N+1・キャッシュ・ページネーション等）が計画書で考慮されているか（大量データ・外部API連携がある場合）
- 上記のいずれかに該当する場合: セクション13（性能・UX設計）が計画書に含まれているか
該当なし（コード変更なし・UIなし・データ量や外部API懸念なし等）の場合は「P6 影響なし」と明記する。
```

追加 P7 ブロック（P6 チェックの後・設計判断チェックの前に配置）:
```markdown
**設計品質チェック（必須）**
計画書の設計判断について以下を確認する:
- 旧来のアンチパターン（Django: Fat View・Raw SQL 乱用、React: Props drilling・巨大コンポーネント等）を踏襲しない設計か
- null / undefined / 空値の扱い方針が設計で統一されているか（複雑なデータ処理がある場合）
- 例外処理・エラーハンドリングの方針が計画書に含まれているか（外部API・複雑な処理がある場合）
- 設定値・定数がハードコードされない方針になっているか
```

**変更後の承認前チェック順序**: P1 → P2 → P4 → P3/P5/P8 → P6 → P7 → 設計判断の明示（code-review の観点順序と整合）

### ステップ4: `implement/SKILL.md` の変更

**修正対象**: 既存「P3・P5 実装確認チェック」ブロックを置き換え、必須項目（P1・P7）と条件付き項目（P3・P5・P6）を明確に分離する:

変更後のブロック全体:
```markdown
**必須確認（P1・P7）**
要件適合性（P1）:
- 実装が受け入れ条件の範囲を超えた仕様追加・機能追加を含んでいないか
- マルチテナント・組織スコープの閲覧範囲・操作範囲の制約が実装で正しく確保されているか（該当する場合）
- ステータス遷移・承認条件・更新可否条件が実装で正しく制御されているか（該当する場合）

設計品質（P7）:
- 旧来のアンチパターン（Fat View・Raw SQL 乱用・Props drilling 等）が実装で踏襲されていないか
- null / undefined / 空値の扱いが実装で一貫しているか
- 設定値・定数がハードコードされていないか

**条件付き確認（P3・P5・P6）**
DB変更がある場合（P3）:
- ForeignKey の on_delete 設定が計画書の方針通りか（CASCADE/PROTECT/SET_NULL）
- インデックスが必要な箇所に設定されているか
- トランザクション境界が計画書の設計通りか（atomic ブロック等）
- 冪等性が必要な処理に二重送信・重複実行対策があるか

外部API・非同期処理・バッチがある場合（P5）:
- 構造化ログに request_id・user_id・tenant_id が含まれているか
- 外部API呼び出しにタイムアウトが設定されているか
- 失敗時のフォールバック・エラーハンドリングが計画書の方針通りか

フロントエンド変更または大量データ・外部API連携がある場合（P6）:
- ローディング・空状態・エラー状態の表示が実装されているか（フロントエンド変更がある場合）
- 破壊的操作（削除等）に確認導線が実装されているか（フロントエンド変更がある場合）
- N+1 クエリ・不要な全件取得が発生していないか

DB変更なし・外部API/非同期処理なし・フロントエンド変更なし・データ量懸念なしの場合は条件付き確認をスキップしてよい。
```

### ステップ5: `close/SKILL.md` の変更（retro 予防処置 #1）

手順1の `ISSUE_NUM="###"` 行の直後に以下のバリデーションを追加:
```bash
if ! [[ "$ISSUE_NUM" =~ ^[0-9]{3}$ ]]; then
  echo "⚠️ ISSUE_NUM が3桁の数字ではありません: $ISSUE_NUM"
  exit 1
fi
```

`mv` を `git mv` に置き換え（4箇所）:
- `mv docs/issues/open/I${ISSUE_NUM}.md docs/issues/closed/` → `git mv ...`
- `mv docs/issues/open/${ISSUE_NUM}.md docs/issues/closed/` → `git mv ...`
- `mv "docs/plans/open/plan_I${ISSUE_NUM}.md" docs/plans/closed/` → `git mv ...`
- for ループ内の `mv "$f" docs/tests/closed/` → `git mv "$f" docs/tests/closed/`
- for ループ内の `mv "$f" docs/reviews/closed/` → `git mv "$f" docs/reviews/closed/`

### ステップ6: `plan-issue/SKILL.md` への実施者判定基準追記（retro 予防処置 #2）

テスト文書生成の指示（生成物リストの近く）に以下を追記:
```markdown
テスト文書（I###_manual_test.md）の「実施者」欄は以下の基準で判定する:
- Claude で実施可: ファイルの内容確認（Read ツール）・コマンド実行・ログ確認（Bash ツール）・ファイル間の比較・差分確認
- Human のみ可: ブラウザ操作・画面の目視確認・外部ツール（Figma・Slack 等）の操作・操作感・UX の感覚的な確認
```

## 6. テスト計画

### 自動テスト
- 対象なし（スキルファイル・Runbook の変更のみ）

### 手動テスト
- 各スキルファイルを読んで、追加箇所が既存コンテキストと矛盾・重複しないことを目視確認
- I046 のレビュースキルの各観点と上流スキルの各観点が対応関係にあることを確認

## 7. ロールバック

git revert で当該コミットを取り消す。スキルファイルの変更のみのため副作用なし。

## 8. Risk & 回避策

| リスク | 回避策 |
|--------|--------|
| 新チェックブロックが冗長で実運用で無視される | 既存ブロックとの整合を確認し、条件付き（「該当する場合」）で明示して必要な場合のみ使用させる |
| チェック項目が多くなりすぎて可読性が下がる | 各ブロックを明確なラベル（P1/P3/P5/P6/P7）で区分し、スキップ条件を明示する |

## 9. 承認ポイント

```
セキュリティ影響: なし（スキルファイル・Runbook のみ変更）
P3/P5/P8 影響: なし
P6 影響: なし（実装なし）

設計判断:
- P7 は既存「既存方針との衝突がないか」を拡張する形（イシューに明記）
- P6 は条件付きセクション13 として plan-writing-rules.md に追加する（イシューに明記）
- plan-issue チェック順: P1 → P2 → P4 → P3/P5/P8 → P6 → P7（code-review の観点順序と整合）
- implement は必須（P1・P7）と条件付き（P3・P5・P6）を明確に分離したブロック構造に置き換える

確認事項:
- [ ] 4ファイルの変更内容と配置位置に問題がないか
- [ ] 追加する各チェック項目が I046 レビュースキルの対応観点と整合しているか
```
