# plan_I046: レビュースキルの観点を8観点フレームワークに全面整合させる

## 基本情報
- **計画書ID**: plan_I046
- **関連イシュー**: #95
- **作成根拠資料**: docs/proposals/review_perspective_framework.md（P1・P2・P4・P6・P7 詳細確認項目）
- **実装後評価**: （未作成）
- **作成日**: 2026-04-15

---

## 背景/目的

I045 でゼロカバー観点（P3・P5・P8）を `/plan-issue-review` および `/code-review` に追加した。
本イシューは部分カバー観点（P1・P2・P4・P6・P7）の不足項目を補完し、8観点フレームワークへの全面整合を完了させる。

変更後のスキルは「受け入れ条件照合だけでは検出できない仕様追加・業務ロジック誤りの検出（P1）」「セキュリティ深掘り（P2補完）」「テスト計画・実装の妥当性確認（P4）」「性能/UX/アクセシビリティの補完（P6）」「設計・実装品質の補完（P7）」を網羅したレビューを行えるようになる。

---

## 受け入れ条件

- [ ] `/plan-issue-review` に P1・P2・P4・P6・P7 の不足項目が追加されている
- [ ] `/code-review` に P1・P2・P4・P6・P7 の不足項目が追加されている
- [ ] proposals の詳細確認項目と追加内容が一致している
- [ ] 既存の確認項目と重複・矛盾がない
- [ ] I045 で追加した P3・P5・P8 観点と境界が明確に区別されている
- [ ] 新規追加セクションの末尾記述が plan-issue-review / code-review 各ファイルの既存パターンと一致している

---

## 影響範囲

- Backend: なし
- Frontend: なし
- DB: なし
- Config/Infra: なし
- スキルファイル:
  - `.claude/skills/plan-issue-review/SKILL.md`（137行 → 約185行）
  - `.claude/skills/code-review/SKILL.md`（105行 → 約145行）

---

## 変更点一覧

### 変更A: `.claude/skills/plan-issue-review/SKILL.md`

#### A-1. P1 セクション追加（ベストプラクティスの直前に挿入）

```
**P1. 要件適合性・業務ロジックの正しさ:**
- 受け入れ条件の範囲を超えた仕様追加・仕様変更が計画書に含まれていないか
- マルチテナント・組織スコープの閲覧範囲・操作範囲の制約が計画書に明示されているか（該当する場合）
- ステータス遷移・承認条件・更新可否条件が計画書で正しく定義されているか（該当する場合）
- エッジケース・境界値での業務ルールが計画書で考慮されているか
- 画面/API/DB で業務ルールの解釈が一致しているか
問題がなければ「問題なし」と記載する。
```

#### A-2. ベストプラクティス セクションへ P7 項目追加（既存3項目の後に追加）

```
- null / undefined / 空値の扱い方針が設計で統一されているか（複雑なデータ処理がある場合）
- 例外処理・エラーハンドリングの方針が計画書に含まれているか（外部API・複雑な処理がある場合）
- 設定値・定数がハードコードされない方針になっているか
- 既存のアンチパターン（Django: Fat View・Raw SQL 乱用、React: Props drilling・巨大コンポーネント等）を踏襲しない設計か
```

#### A-3. セキュリティ セクションへ P2 項目追加（既存4項目の後に追加）

OWASPの記述を拡充し、追加項目を追記：
- OWASP行: 「XSS・CSRF・SQLi・認可不備等」→「XSS・CSRF・SQLi・認可不備・SSRF・Path Traversal・Open Redirect 等」
- 追加4項目:
```
- JWT/セッション管理の設計方針が計画書に含まれているか（認証・セッション変更がある場合）
- レート制限・ブルートフォース対策が必要なエンドポイント（認証EP等）に計画書で明示されているか
- 監査ログが必要な操作に記録の設計があるか（重要操作・権限変更等）
- Cookie・CORS・HTTPS・セキュリティヘッダーの設定方針が計画書にあるか（外部公開API・認証変更がある場合）
```

#### A-4. モダンなウェブアプリ開発 セクションへ P6 項目追加（既存4項目の後に追加）

```
- ローディング・空状態・エラー状態の表示方針が含まれているか（Frontend変更がある場合）
- 破壊的操作（削除等）に確認導線の設計があるか（Frontend変更がある場合）
- キャッシュ戦略が妥当か（パフォーマンス要件がある場合）
```

#### A-5. P4 セクション追加（Claude Code ベストプラクティス の後に挿入）

```
**P4. テスト妥当性・回帰防止:**
- バグ修正の場合、再発防止テストがテストケースに含まれているか
- テストが実装詳細に依存しすぎない形で設計されているか（インターフェース/振る舞いテスト推奨）
- 認可テスト（テナント境界・ロール別アクセス制御の検証）がテストケースに含まれているか（認可変更がある場合）
問題がなければ「問題なし」と記載する。
```

---

### 変更B: `.claude/skills/code-review/SKILL.md`

#### B-1. P1 セクション追加（ベストプラクティスの直前に挿入）

```
**P1. 要件適合性・業務ロジックの正しさ:**
- 受け入れ条件の範囲を超えた仕様追加・機能追加が実装に含まれていないか
- マルチテナント・組織スコープの閲覧範囲・操作範囲の制約が実装で正しく確保されているか（該当する場合）
- ステータス遷移・承認条件・更新可否条件が実装で正しく制御されているか（該当する場合）
- エッジケース・境界値での業務ルールが実装で考慮されているか
```

#### B-2. ベストプラクティス セクションへ P7 項目追加（既存3項目の後に追加）

```
- null / undefined / 空値の扱いが実装で一貫しているか
- 例外処理・エラーハンドリングが統一された方針で実装されているか
- 設定値・定数がハードコードされていないか（APIエンドポイント・マジックナンバー等）
- 旧来のアンチパターン（Django: Raw SQL 乱用・Fat View、React: Props drilling・巨大コンポーネント等）が踏襲されていないか
```

#### B-3. セキュリティ セクションへ P2 項目追加（既存5項目の後に追加）

OWASPの記述を拡充し、追加項目を追記：
- OWASP行: 「SQLインジェクション・XSS・CSRF 等 OWASP Top 10 相当のリスク」→「SQLインジェクション・XSS・CSRF・SSRF・Path Traversal・Open Redirect 等 OWASP Top 10 相当のリスク」
- 追加4項目:
```
- JWT/セッション管理が設計方針通りに実装されているか（認証・セッション変更がある場合）
- レート制限・ブルートフォース対策が必要なエンドポイントに実装されているか（該当する場合）
- 監査ログが必要な操作に記録の実装があるか（重要操作・権限変更等）
- Cookie・CORS・HTTPS・セキュリティヘッダーが適切に設定されているか（外部公開API・認証変更がある場合）
```

#### B-4. モダンなウェブアプリ開発 セクションへ P6 項目追加（既存4項目の後に追加）

```
- ローディング・空状態・エラー状態の表示が実装されているか（Frontend変更がある場合）
- 破壊的操作（削除等）に確認導線が実装されているか（Frontend変更がある場合）
- エラーメッセージがユーザーに分かりやすく表示されているか（Frontend変更がある場合）
```

#### B-5. P4 セクション追加（モダンなウェブアプリ開発 の後に挿入）

```
**P4. テスト妥当性・回帰防止:**
変更に対してテストが適切に書かれているかを確認する:
- バグ修正の場合、再発防止テストが実装されているか
- テストが実装詳細に依存しすぎていないか（インターフェース/振る舞いを検証しているか）
- モック過多で実態を検証できていない状態になっていないか
- 認可テスト（テナント境界・ロール別アクセス制御の検証）が実装されているか（認可変更がある場合）
```

---

## 実装手順

### ステップ1: plan-issue-review/SKILL.md を変更する

1. A-1: P1 セクションを「ベストプラクティス」セクションの直前に挿入
2. A-2: ベストプラクティス セクションに P7 の4項目を追加
3. A-3: セキュリティ セクションの OWASP 行を拡充 + P2 の4項目を追加
4. A-4: モダン セクションに P6 の3項目を追加
5. A-5: P4 セクションを「Claude Code ベストプラクティス」セクションの後に挿入

### ステップ2: code-review/SKILL.md を変更する

1. B-1: P1 セクションを「ベストプラクティス」セクションの直前に挿入
2. B-2: ベストプラクティス セクションに P7 の4項目を追加
3. B-3: セキュリティ セクションの OWASP 行を拡充 + P2 の4項目を追加
4. B-4: モダン セクションに P6 の3項目を追加
5. B-5: P4 セクションを「モダンなウェブアプリ開発」セクションの後に挿入（P3 の前）

### ステップ3: commit/push

---

## セクション末尾記述の一貫性ルール

I045 レトロ予防処置より: 各スキルファイル既存セクションの末尾パターンに揃える。

- **plan-issue-review**: 新規セクション（P1・P4）は末尾に「問題がなければ「問題なし」と記載する。」を含める（P3・P5・P8 の既存パターンに準拠）
- **code-review**: 新規セクション（P1・P4）は末尾なし（P3・P5・P8 の既存パターンに準拠。冒頭の「問題がなければ「問題なし」と記載する」が全観点に適用）

---

## テスト計画

### 自動テスト
ファイルの内容確認（Claude が実施）:
- plan-issue-review/SKILL.md に P1・P4 セクションが存在する
- plan-issue-review/SKILL.md に P2/P6/P7 の追加項目が含まれる
- code-review/SKILL.md に P1・P4 セクションが存在する
- code-review/SKILL.md に P2/P6/P7 の追加項目が含まれる
- 両ファイルが 500 行以内である

### 手動テスト
Claude が既存テストケース文書（I046_manual_test.md）に基づいて確認する。

---

## ロールバック

スキルファイルの変更のみ。git revert で即時ロールバック可能。

---

## Risk & 回避策

| リスク | 対策 |
|--------|------|
| 既存確認項目との重複 | proposals の「含めないもの」定義を参照して境界を確認する |
| SKILL.md が 500 行を超える | 実装前に行数を確認し、超える場合は supporting files へ一部を切り出す |
| P3/P5/P8 との境界が曖昧になる | 各観点の説明に「含まない観点」を適宜注記する |

---

## セキュリティ考慮

スキルファイル（Markdown）のみの変更。コード・DB・インフラへの変更なし。セキュリティ影響なし。

---

## 設計判断（イシューに明記 / 仮定で決めた）

| 判断事項 | 区別 | 内容 |
|---------|------|------|
| P1/P4 の挿入位置 | proposals + I045 パターンから導出 | P1 は最も基本的なチェックのため既存セクションの前に、P4 は Claude Code BP の後に配置 |
| 末尾記述パターン | I045 の実装から導出 | plan-issue-review は末尾あり、code-review は末尾なし（既存 P3/P5/P8 に準拠） |
| OWASP 行の拡充 | proposals P2 の内容から導出 | SSRF・Path Traversal・Open Redirect を既存行に追記（削除でなく追記） |

---

## 承認ポイント

- [ ] 変更A (plan-issue-review): P1/P4 追加、P2/P6/P7 拡充の内容と挿入位置
- [ ] 変更B (code-review): P1/P4 追加、P2/P6/P7 拡充の内容と挿入位置
- [ ] 末尾記述パターンの方針（plan-issue-review: あり、code-review: なし）
