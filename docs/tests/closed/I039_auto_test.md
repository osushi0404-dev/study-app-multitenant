# I039 自動テスト仕様

## 対象
スキル間ファイル名参照の不整合修正

## 自動テスト対象

**なし** — 今回の変更対象はすべてドキュメントファイル（`.md`）および Claude Code スキル定義ファイル（`.claude/skills/**`）のみ。
コード・API・DB の変更はないため、自動テスト（pytest / Jest）の追加・変更は不要。

---

## CI チェック

変更ファイルはすべて `.md` ファイルのため、既存 CI（Python テスト・Frontend ビルド）への影響なし。

CI パスを確認してからマージすること（PR チェック: `gh pr checks 67`）。
