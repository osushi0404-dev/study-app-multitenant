# I073 手動テスト計画（フロント編集ダイアログ / UX）

- **関連イシュー**: #150 / **計画書**: docs/plans/open/plan_I073.md
- **対象**: `frontend/src/pages/QuizManagement.tsx` 編集ダイアログ・`ImageUploadArea.tsx`
- **前提**: Docker でアプリ起動（`docker compose up`）。管理画面にログインし、画像付き問題が1件以上ある状態。

| No | 手順 | 期待結果 | 実施者 | 実結果 | 備考 |
|---:|------|----------|--------|--------|------|
| TC-MAN-01 | 既存の画像付き問題を編集ダイアログで開く | 「問題用画像」「解説用画像」欄に**既存画像がサムネイル表示**される（編集前から見える）。各サムネに ↑/↓/削除ボタンが表示される。 | Human | OK | 現状は既存画像が表示されない（回帰確認の起点） |
| TC-MAN-02 | 画像を1枚追加して保存→再度開く | 追加した画像が末尾に表示され、保存後の再オープンでも反映される。 | Human | OK | |
| TC-MAN-03 | 既存画像を1枚削除して保存→再度開く | 削除した画像が消え、残りが正しく表示される。保存前は削除がローカルのみ（保存して初めて確定）。 | Human | OK | 破壊的操作の確認導線（保存まで取り消し可）を体感確認 |
| TC-MAN-04 | ↑/↓ ボタンで並び順を入れ替えて保存→再度開く | 表示順が入れ替わり、保存後も維持される。先頭画像の↑・末尾画像の↓は押せない（無効化）。 | Human | OK | |
| TC-MAN-05 | 画像を差し替え（1枚削除＋1枚追加）して保存 | 差し替え後の画像セット・順序が正しく反映される。 | Human | OK | |
| TC-MAN-06 | 6枚目を追加しようとする | 「最大5枚」のメッセージが出て追加できない。問題用・解説用それぞれ独立して5枚上限。 | Human | OK | クライアント側上限。サーバ側上限は TC-AUTO-08 |
| TC-MAN-07 | 解説用画像で TC-02〜04 相当を実施 | 解説用画像でも追加・削除・並び替えが機能する。 | Human | OK | |
| TC-MAN-08 | 画像を変更せずテキスト/選択肢のみ編集して保存 | テキスト・選択肢が更新され、**既存画像は保持**される（消えない）。 | Human | OK | R5（order非送信＝無変更）の体感確認 |
| TC-MAN-09 | アクセシビリティ: ↑/↓/削除ボタンに `aria-label` が付与されているか（DevTools で確認） | 各ボタンに「上に移動」「下に移動」「削除」相当の `aria-label` がある。 | Claude | **OK** | `ImageUploadArea.tsx` L319/327/335 に `aria-label="上に移動"/"下に移動"/"削除"` を確認（2026-06-24） |

## 完了条件
- TC-MAN-01〜09 が全て OK。
- 並び替え・削除・追加・差し替えが問題用/解説用の両方で動作し、保存後の再取得で一貫表示される。

## 実施記録（2026-06-26）
- TC-MAN-01〜08（Human・ブラウザ操作）: **全て OK**（キャッシュ修正後の再テスト。前回 NG だった TC-MAN-02「追加→参照反映」も OK）。
- TC-MAN-09（Claude・aria-label）: OK。
