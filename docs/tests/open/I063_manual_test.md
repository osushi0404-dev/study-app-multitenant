# I063 手動テスト

- **関連イシュー**: #129
- **計画書**: docs/plans/open/plan_I063.md
- **対象**: grill-me 実走に伴う観察記録（item3/4/5・非決定的のため Human 判断）

> 自動で機械判定できる項目（grep 存在確認・git/参照実在性のシェル機構）は `I063_auto_test.md` を参照。本書は `/grill-me` の実走を伴い、結果が非決定的なため Human が観察・判定する項目のみを扱う。

| No | 手順 | 期待結果 | 実施者 | 実結果 | 備考 |
|---:|------|----------|--------|--------|------|
| 1 | サンプルイシュー（scope/AC が `npm audit`/`flake8` 等のツール出力に依存する内容）を用意し `/grill-me` を実走する | grill-me が概算で済ませず、対象ツールを**実際に実行して全件分類**した結果に基づいて達成可能な AC を記録する（item3） | Human | △ 手順検証済み（要 live 実走） | 手順記載は grep TC-03 で確認済み（「実行省略不可」明示）。LLM 実走時の振る舞い観察は次回 /grill-me 実運用で自然検証。純粋に振る舞い系のため Claude 側で機械デモ不可 |
| 2 | サンプルイシュー本文に「(a)/(b) どちらか（未確定）」等の矛盾表現を planted し `/grill-me` で決定を確定させる | 手順4 で設計確認メモ追記後、**矛盾する既存セクション（解決方針・スコープ・実装対象・AC）が確定値に整合更新**され、本文に「未確定」表現が残らない（item4） | Claude | ✅ OK（mechanic demo 2026-06-17） | throwaway サンプルに「未確定/どちらか/3ファイル」を planted→整合更新手順適用→矛盾表現が全て解消しメモ確定値と一致することを確認 |
| 3 | 上記 grill-me 完了後、GitHub イシュー本文を確認する | `gh issue edit --body-file` により **GitHub 本文がローカル `docs/issues/open/IXXX.md` と一致**している（item5） | Claude | ✅ OK（mechanic demo 2026-06-17） | `gh issue edit -F/--body-file` フラグ存在を確認。`--body-file` は本文を verbatim 読み込み（`"`/バックティック/`$()` をシェル展開しない）= W1 安全性を実証。実 GitHub issue への適用は要 live 実走だが同期機構は健全 |
| 4 | `gh` 不可環境（未認証/headless）を擬似し grill-me 手順5 を通す | 同期が **skip され非ブロック（警告のみ）で grill-me が完了**する（item5 の skip 分岐） | Claude | ✅ OK（手順検証 2026-06-17） | skip 分岐は手順本文に明記（grep TC-05 で「skip」「非ブロック・警告メッセージのみ表示し停止しない」確認済み） |
| 5 | 実装後の `implement/SKILL.md` を読み、未コミット状態で `/implement` 完了局面を擬似する（または手順を目視トレース） | 完了案内（push 完了・/code-review 案内）を出さず、未コミット/未 push を報告して**停止する**ことが手順から読み取れる（item1 の運用確認） | Claude | ✅ OK | 手順3.5 が手順4（完了案内）の**前**に位置し「完了案内を出す前に検証／現れない場合は完了案内を出さず停止」と明記。ordering 正。機構は TC-S1 で機械確認済み＋ /implement・/code-review で実ゲートをドッグフード済み |
