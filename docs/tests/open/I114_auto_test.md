# I114 自動テスト: /retro 対ユーザー出力の平易化（記録・詳細フォーマット維持）

実行コマンドは末尾の「決定論ゲート（自動実走）」セクションに宣言（`test_i114_retro_plain_output.sh` の単独実行）。

結果:
- backend: 非該当（アプリコード変更なし・pytest 不要）
- frontend: 非該当（アプリコード変更なし・Jest 不要）
- E2E: 非該当（アプリ挙動変更なし）
- 決定論ゲート: **実行済み（実装時 2026-07-15・`RESULT: OK (14/14)`・exit 0）**。TDD Red（計画時・G1〜G10 全 NO-HIT）→ Green（実装後・14/14 OK）を確認。スクリプト単位の false-green 注入検証も NG 検知を確認（TC-02 実施記録参照）

## テストケース

### TC-01: SKILL.md の平易化ルール文言＋既存フォーマット維持（test_i114_retro_plain_output.sh 内・14 項目）
検証対象: `.claude/skills/retro/SKILL.md`（grep -Fq・固定文字列）

| # | 検証項目 | 判定文字列 | 期待値 |
|---|---------|-----------|--------|
| G1 | 提示順（平易版→詳細版の併記） | `平易版を先に提示し、その後に詳細版` | ヒット（exit 0） |
| G2 | 置換原則（省略ではない） | `平易な言葉に置換する（省略ではない）` | ヒット |
| G3 | 経緯・理由の保持 | `過度な要約で情報を削らない` | ヒット |
| G4 | 記録は詳細維持 | `詳細フォーマットを維持` | ヒット |
| G5 | 記録ファイル新設なし | `本スキルへの記録ファイル新設はしない` | ヒット |
| G6 | 5 Whys 経過維持・用語のみ平易化 | `経過（Why の連鎖）の提示を維持` | ヒット |
| G7 | コマンド名・選択肢記号は対象外 | `平易化の対象外` | ヒット |
| G8 | 文例（変換前） | `変換前` | ヒット |
| G9 | 文例（変換後） | `変換後` | ヒット |
| G10 | 手順3 の併記指示 | `平易版 → 詳細版の順で併記` | ヒット |
| R1 | 是正処置の詳細表ヘッダ維持（無改変） | `\| # \| カテゴリ \| 根本原因 \| 対処内容 \|` | ヒット |
| R2 | 予防処置の詳細表ヘッダ維持（無改変） | `\| # \| カテゴリ \| 根本原因 \| 予防策 \| 対象ファイル \|` | ヒット |
| R3 | 5 Whys 内部手続き維持（無改変） | `5 Whys の実施（最低 3 回）` | ヒット |
| R4 | 手順5 承認ゲート維持（無改変） | `**承認ゲート**` | ヒット |
| — | スクリプト合計判定 | test_i114_retro_plain_output.sh 全体 | **exit 0・`RESULT: OK (14/14)`** |

AC との対応: G1/G10=AC1、G2=AC2、G3=AC3、G4/G5=AC4、G6=AC5、G7=AC6、G8/G9=AC7、スクリプト全体=AC8（決定論チェック）。R1〜R4 は「詳細フォーマット・分析手続きの無改変」の再発防止検証。

### TC-02: false-green 注入検証（計画時ベースラインで実施済み＋実装ステップ2 で再実施）
**方式**: 実ファイルは一切改変しない。G系は「実装前の SKILL.md」自体が失敗状態（文言不在）。R系は該当行を削除した一時コピー（scratchpad）を作り、検証対象を `I114_TEST_SKILL=<コピー>` で差し替えて確認する。

| 手順 | 期待値 |
|------|--------|
| 実装前の SKILL.md に G1〜G10 を grep（TDD Red） | **全件 NO-HIT（exit 1）** — 文言不在を確実に検知する |
| R1〜R4 の該当行を削除した一時コピーに grep | **全件 NO-HIT（exit 1）** — 既存フォーマットの欠落を確実に検知する |
| （実装ステップ2）`I114_TEST_SKILL=<R系破壊コピー> bash scripts/claude/tests/test_i114_retro_plain_output.sh` | **NG 行を出力し exit 1** |

実施記録（計画時 2026-07-15・実ファイル無改変・scratchpad/i114_baseline_grep.sh）:
- G1〜G10: 現状 SKILL.md に対して **全 10 件 NO-HIT（exit 1）** — ゲートは不在を検知できる（TDD Red 確認済み）
- R1〜R4: 現状 SKILL.md で **全 4 件 HIT**、削除注入コピーで **全 4 件 NO-HIT（exit 1）** — 維持検証は false-green ではない
- スクリプト単位の注入再実施（`I114_TEST_SKILL` 差し替え）は実装ステップ2 で実施し、この文書に追記する

実施記録（実装ステップ2・2026-07-15・実ファイル無改変・scratchpad/i114_injection_test.sh）:
- 注入1: R1（是正処置表ヘッダ）行を削除した一時コピーを `I114_TEST_SKILL` で差し替え → `NG: R1 是正処置の詳細表ヘッダ維持`・`RESULT: NG (1 件)`・**exit 1**（検知 OK）
- 注入2: G2（置換原則）行を削除した一時コピーを差し替え → `NG: G2 置換原則（省略ではない）`・`RESULT: NG (1 件)`・**exit 1**（検知 OK）
- 判定: 本ゲートは false-green ではない（壊れた状態を確実に不合格にする）

### TC-03: イシュー本文整合（実装ステップ3）
| 検証項目 | 判定方法 | 期待値 |
|---------|---------|--------|
| テストスクリプトが実装対象に反映 | `grep -qF "test_i114_retro_plain_output.sh" docs/issues/open/I114.md` | ヒット（exit 0） |
| スクリプト実在＋構文 | `bash -n scripts/claude/tests/test_i114_retro_plain_output.sh` | exit 0 |

## 決定論ゲート（自動実走）
```bash
bash scripts/claude/tests/test_i114_retro_plain_output.sh
```
